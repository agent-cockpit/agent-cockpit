{
  "name": "agent-cockpit",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "postinstall": "node-gyp rebuild --directory node_modules/.pnpm/node-pty@1.1.0/node_modules/node-pty 2>/dev/null || true",
    "dev": "pnpm --parallel -r dev",
    "test": "vitest",
    "test:watch": "vitest",
    "build": "pnpm --filter @cockpit/ui exec vite build",
    "typecheck": "tsc --noEmit",
    "sync-map": "pnpm exec tsx scripts/sync-map.ts",
    "copy-faces": "cd packages/ui && pnpm exec tsx ../../scripts/copy-faces.ts"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^3.0.0",
    "tsx": "latest"
  }
}
