{
  "name": "agent-cockpit",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "vitest",
    "test:watch": "vitest",
    "build": "pnpm -r build",
    "typecheck": "tsc --noEmit",
    "sync-map": "rsync -av --delete \"assets/raw/Cockpit Map Export/\" packages/ui/public/map/",
    "copy-faces": "cd packages/ui && pnpm exec tsx ../../scripts/copy-faces.ts"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^3.0.0",
    "tsx": "latest"
  }
}
