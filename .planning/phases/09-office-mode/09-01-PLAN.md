---
phase: 09-office-mode
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/components/office/spriteStates.ts
  - packages/ui/src/hooks/useLocalStorage.ts
  - packages/ui/src/sprites/agent-sheet.png
  - packages/ui/src/__tests__/spriteStates.test.ts
  - packages/ui/src/__tests__/useLocalStorage.test.ts
autonomous: true
requirements:
  - OFFICE-01
  - OFFICE-04

must_haves:
  truths:
    - "deriveAgentState returns correct AgentAnimState for each NormalizedEvent type"
    - "deriveAgentState returns 'blocked' when session.pendingApprovals > 0 regardless of last event"
    - "deriveAgentState returns 'completed' when session.status is 'ended'"
    - "useLocalStorage reads initial value from localStorage on mount"
    - "useLocalStorage persists updated value to localStorage on set"
    - "useLocalStorage returns defaultValue when localStorage is empty or throws"
  artifacts:
    - path: "packages/ui/src/components/office/spriteStates.ts"
      provides: "AgentAnimState type + deriveAgentState function"
      exports: ["AgentAnimState", "deriveAgentState", "STATE_CSS_CLASSES"]
    - path: "packages/ui/src/hooks/useLocalStorage.ts"
      provides: "Generic localStorage-backed React state hook"
      exports: ["useLocalStorage"]
    - path: "packages/ui/src/sprites/agent-sheet.png"
      provides: "Placeholder sprite sheet (avoids 404 in jsdom tests)"
    - path: "packages/ui/src/__tests__/spriteStates.test.ts"
      provides: "Unit tests for deriveAgentState"
    - path: "packages/ui/src/__tests__/useLocalStorage.test.ts"
      provides: "Unit tests for useLocalStorage"
  key_links:
    - from: "packages/ui/src/components/office/spriteStates.ts"
      to: "AgentSprite.tsx (Plan 02)"
      via: "AgentAnimState type + STATE_CSS_CLASSES map imported in Plan 02"
      pattern: "export.*AgentAnimState"
    - from: "packages/ui/src/hooks/useLocalStorage.ts"
      to: "OfficePage.tsx (Plan 03)"
      via: "useLocalStorage('cockpit.office.positions', {}) call"
      pattern: "export.*useLocalStorage"
---

<objective>
Install dnd-kit dependencies, create Wave 0 test scaffolds that are RED, then drive spriteStates.ts and useLocalStorage.ts to GREEN using TDD.

Purpose: Establish the two pure-logic contracts (state derivation + localStorage persistence) that Plan 02 and Plan 03 depend on, with tests proving correctness before any UI is wired.
Output: Green tests for spriteStates and useLocalStorage; placeholder sprite sheet; dnd-kit packages installed.
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-office-mode/09-RESEARCH.md

<interfaces>
<!-- Key types the executor needs. From packages/ui/src/store/index.ts -->
```typescript
export type SessionStatus = 'active' | 'ended' | 'error'

export interface SessionRecord {
  sessionId: string
  provider: 'claude' | 'codex'
  workspacePath: string
  startedAt: string
  status: SessionStatus
  lastEventAt: string
  pendingApprovals: number
}
```

<!-- From @cockpit/shared — NormalizedEvent types used in deriveAgentState -->
<!-- event.type values: 'session_start' | 'session_end' | 'tool_call' | 'file_change' |
     'approval_request' | 'approval_resolved' | 'memory_read' | 'memory_write' |
     'subagent_spawn' | 'subagent_complete' | 'provider_parse_error' -->
<!-- ToolCallEvent has .toolName: string -->
</interfaces>
</context>

<feature>
  <name>spriteStates — event-to-animation-state mapping</name>
  <files>packages/ui/src/components/office/spriteStates.ts, packages/ui/src/__tests__/spriteStates.test.ts</files>
  <behavior>
    - deriveAgentState(session {status:'ended'}, any lastEvent) → 'completed'
    - deriveAgentState(session {pendingApprovals:1}, any lastEvent) → 'blocked'
    - deriveAgentState(session {status:'active', pendingApprovals:0}, undefined) → 'waiting'
    - deriveAgentState(session, {type:'session_start'}) → 'planning'
    - deriveAgentState(session, {type:'tool_call', toolName:'read_file'}) → 'reading'
    - deriveAgentState(session, {type:'tool_call', toolName:'write_to_file'}) → 'coding'
    - deriveAgentState(session, {type:'tool_call', toolName:'bash'}) → 'testing'
    - deriveAgentState(session, {type:'tool_call', toolName:'unknown_tool'}) → 'coding' (default)
    - deriveAgentState(session, {type:'file_change'}) → 'coding'
    - deriveAgentState(session, {type:'memory_read'}) → 'reading'
    - deriveAgentState(session, {type:'memory_write'}) → 'planning'
    - deriveAgentState(session, {type:'approval_request'}) → 'blocked'
    - deriveAgentState(session, {type:'provider_parse_error'}) → 'failed'
    - deriveAgentState(session, {type:'subagent_spawn'}) → 'planning'
    - deriveAgentState(session, {type:'subagent_complete'}) → 'waiting'
    - STATE_CSS_CLASSES maps each AgentAnimState to a CSS class string like 'sprite-planning'
    - AgentAnimState union type covers all 8 states
  </behavior>
  <implementation>
    Create `packages/ui/src/components/office/` directory.
    Export `AgentAnimState` as a TypeScript union type of 8 string literals.
    Export `deriveAgentState(session: SessionRecord, lastEvent: NormalizedEvent | undefined): AgentAnimState` — implement the switch/if logic from the research patterns. Priority order: status==='ended' → 'completed', pendingApprovals>0 → 'blocked', no lastEvent → 'waiting', then switch on lastEvent.type. For tool_call events check toolName.toLowerCase() for read/view/grep/search → 'reading', write/edit/create/apply → 'coding', test/run/exec/bash → 'testing', else → 'coding'.
    Export `STATE_CSS_CLASSES: Record<AgentAnimState, string>` — maps each state to `sprite-{state}`.
    Import `NormalizedEvent` from `@cockpit/shared`. Import `SessionRecord` from `../../store/index.js`.
  </implementation>
</feature>

<feature>
  <name>useLocalStorage — generic localStorage-backed React state</name>
  <files>packages/ui/src/hooks/useLocalStorage.ts, packages/ui/src/__tests__/useLocalStorage.test.ts</files>
  <behavior>
    - useLocalStorage('key', 'default') returns ['default', setter] when localStorage is empty
    - useLocalStorage('key', 42) returns [42, setter] when localStorage key is absent
    - After setter(newValue), calling localStorage.getItem('key') returns JSON.stringify(newValue)
    - useLocalStorage('key', {}) returns stored object when key exists in localStorage
    - useLocalStorage('key', {}) returns defaultValue when localStorage.getItem throws (SSR-safe catch block)
    - setter accepts both a direct value and an updater function: setter(prev => ({...prev, id: {x:1,y:1}}))
    - The hook signature: useLocalStorage<T>(key: string, defaultValue: T): [T, (updater: T | ((prev: T) => T)) => void]
  </behavior>
  <implementation>
    Create `packages/ui/src/hooks/` directory (may already exist from prior phases — check).
    Implement as described in Research Pattern 4. Use `useState` with lazy initializer that reads localStorage. `setAndPersist` writes to localStorage via JSON.stringify after updating state. Wrap both read and write in try/catch. Use `useCallback([key])` for stable setter reference.
    Test with jsdom's built-in localStorage (vitest + jsdom provides it). Clear localStorage in beforeEach to avoid test bleed.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Install dnd-kit + create placeholder sprite sheet</name>
  <files>packages/ui/src/sprites/agent-sheet.png</files>
  <action>
    1. Install dnd-kit packages: `pnpm --filter @cockpit/ui add @dnd-kit/core @dnd-kit/utilities`
    2. Check if `@radix-ui/react-hover-card` is already installed (check packages/ui/node_modules/@radix-ui/ or package.json). If absent, install: `pnpm --filter @cockpit/ui add @radix-ui/react-hover-card`
    3. Create directory `packages/ui/src/sprites/` and place a 128×256 px PNG placeholder there. The PNG must be valid (not a stub empty file) — use a programmatically generated 1×1 white pixel PNG encoded as base64, OR create a minimal 8×4 colored grid using the ImageData approach. The simplest valid approach: use Node.js to write a minimal valid PNG binary (the 67-byte minimal PNG header + IDAT for a 1×1 transparent pixel is sufficient to avoid 404/parse errors in tests). Write the base64 of a 1×1 transparent PNG to the file.

    Minimal valid 1x1 transparent PNG as base64 (known good value):
    `iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNkYPhfDwAChwGA60e6kgAAAABJRU5ErkJggg==`

    Decode and write to `packages/ui/src/sprites/agent-sheet.png`.

    Use Node.js one-liner to do this: `node -e "const fs=require('fs');const b64='iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNkYPhfDwAChwGA60e6kgAAAABJRU5ErkJggg==';fs.mkdirSync('packages/ui/src/sprites',{recursive:true});fs.writeFileSync('packages/ui/src/sprites/agent-sheet.png',Buffer.from(b64,'base64'))"`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const b=fs.readFileSync('packages/ui/src/sprites/agent-sheet.png');console.assert(b.length>0,'empty');console.assert(b[0]===137,'not PNG')" && pnpm --filter @cockpit/ui ls @dnd-kit/core 2>/dev/null | grep -q 'dnd-kit' || node -e "require('./packages/ui/node_modules/@dnd-kit/core')"</automated>
  </verify>
  <done>agent-sheet.png is a valid PNG file with byte 0 = 0x89 (PNG magic byte); @dnd-kit/core and @dnd-kit/utilities appear in packages/ui/package.json dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD spriteStates.ts — AgentAnimState + deriveAgentState</name>
  <files>packages/ui/src/components/office/spriteStates.ts, packages/ui/src/__tests__/spriteStates.test.ts</files>
  <behavior>
    - Test: session.status==='ended' → 'completed' (regardless of lastEvent)
    - Test: pendingApprovals>0 → 'blocked' (regardless of lastEvent type)
    - Test: status==='active', pendingApprovals===0, lastEvent=undefined → 'waiting'
    - Test: type==='session_start' → 'planning'
    - Test: type==='tool_call', toolName='read_file' → 'reading'
    - Test: type==='tool_call', toolName='write_to_file' → 'coding'
    - Test: type==='tool_call', toolName='bash' → 'testing'
    - Test: type==='tool_call', toolName='unknown' → 'coding'
    - Test: type==='file_change' → 'coding'
    - Test: type==='memory_read' → 'reading'
    - Test: type==='memory_write' → 'planning'
    - Test: type==='approval_request' → 'blocked'
    - Test: type==='provider_parse_error' → 'failed'
    - Test: STATE_CSS_CLASSES['planning'] === 'sprite-planning'
    - Test: STATE_CSS_CLASSES has an entry for each of the 8 states
  </behavior>
  <action>
    RED phase: Create `packages/ui/src/__tests__/spriteStates.test.ts` with all tests listed in behavior block. Use `it.todo()` stubs initially if needed to confirm file compiles, then write real `it()` tests that will fail without the implementation. The test file imports `deriveAgentState` and `STATE_CSS_CLASSES` from `../components/office/spriteStates.js`. Run `pnpm --filter @cockpit/ui test --run spriteStates` — confirm tests fail (RED).

    GREEN phase: Create `packages/ui/src/components/office/spriteStates.ts`:
    - Export `AgentAnimState` union type: `'planning' | 'coding' | 'reading' | 'testing' | 'waiting' | 'blocked' | 'completed' | 'failed'`
    - Export `STATE_CSS_CLASSES: Record<AgentAnimState, string>` mapping each state to `'sprite-' + state`
    - Export `deriveAgentState(session: SessionRecord, lastEvent: NormalizedEvent | undefined): AgentAnimState` — implement priority chain: (1) session.status==='ended' → 'completed', (2) session.pendingApprovals>0 → 'blocked', (3) !lastEvent → 'waiting', (4) switch on lastEvent.type with tool_call subcases checking toolName.toLowerCase().
    - Import `NormalizedEvent` from `@cockpit/shared`. Import `SessionRecord` from `../../store/index.js`.
    Run tests again — confirm GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run spriteStates</automated>
  </verify>
  <done>All spriteStates tests pass; deriveAgentState and STATE_CSS_CLASSES exported from spriteStates.ts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TDD useLocalStorage — generic localStorage hook</name>
  <files>packages/ui/src/hooks/useLocalStorage.ts, packages/ui/src/__tests__/useLocalStorage.test.ts</files>
  <behavior>
    - Test: hook returns defaultValue when key absent from localStorage
    - Test: hook returns stored value when key exists (reads JSON.parse of stored string)
    - Test: setter(newValue) — localStorage.getItem(key) equals JSON.stringify(newValue) afterward
    - Test: setter(prev => newValue) — functional updater works, produces correct value
    - Test: hook returns defaultValue when localStorage.getItem throws (simulate by mocking)
    - Test: hook returns defaultValue when stored JSON is malformed (corrupted value)
  </behavior>
  <action>
    RED phase: Create `packages/ui/src/__tests__/useLocalStorage.test.ts`. Use `renderHook` from `@testing-library/react`. Clear localStorage in `beforeEach(() => localStorage.clear())`. Write tests importing from `../hooks/useLocalStorage.js`. Run `pnpm --filter @cockpit/ui test --run useLocalStorage` — confirm RED.

    GREEN phase: Create `packages/ui/src/hooks/useLocalStorage.ts` implementing the hook as described in Research Pattern 4. Use `useState` with lazy initializer, `useCallback` for stable setter. Both the read (lazy init) and write paths must be wrapped in try/catch. Run tests — confirm GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run useLocalStorage</automated>
  </verify>
  <done>All useLocalStorage tests pass; hook exported from hooks/useLocalStorage.ts</done>
</task>

</tasks>

<verification>
Full test suite after all tasks: `pnpm --filter @cockpit/ui test --run`
All spriteStates and useLocalStorage tests green.
No regressions in existing tests.
</verification>

<success_criteria>
- @dnd-kit/core and @dnd-kit/utilities in packages/ui/package.json dependencies
- packages/ui/src/sprites/agent-sheet.png is a valid PNG binary
- AgentAnimState exported from spriteStates.ts with 8 states
- deriveAgentState passes all 15 behavior tests
- STATE_CSS_CLASSES maps each state to sprite-{state}
- useLocalStorage passes all 6 behavior tests
- pnpm --filter @cockpit/ui test --run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/09-office-mode/09-01-SUMMARY.md`
</output>
