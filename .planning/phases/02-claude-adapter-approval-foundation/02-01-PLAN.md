---
phase: 02-claude-adapter-approval-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/adapters/claude/hookServer.ts
  - packages/daemon/src/adapters/claude/hookParser.ts
  - packages/daemon/src/adapters/claude/riskClassifier.ts
  - packages/daemon/src/db/database.ts
  - packages/daemon/src/__tests__/hook-server.test.ts
autonomous: true
requirements: [DAEMON-04, APPR-02]

must_haves:
  truths:
    - "POSTing a SessionStart hook payload to the HTTP server causes a session_start NormalizedEvent to be emitted on the eventBus"
    - "POSTing a non-blocking PreToolUse payload causes a tool_call NormalizedEvent to be emitted and the server responds 200 immediately"
    - "POSTing a blocking PreToolUse payload causes an approval_request NormalizedEvent to be emitted and the HTTP response is held open (not ended)"
    - "Each approval_request event has actionType and riskLevel populated from tool_name + tool_input heuristics"
    - "The same Claude Code session_id always maps to the same UUID sessionId across multiple hook calls"
    - "The approvals and always_allow_rules tables exist in the database after openDatabase()"
  artifacts:
    - path: "packages/daemon/src/adapters/claude/hookServer.ts"
      provides: "createHookServer() — HTTP server on COCKPIT_HOOK_PORT that holds response for approval hooks"
      exports: ["createHookServer", "resolveApproval"]
    - path: "packages/daemon/src/adapters/claude/hookParser.ts"
      provides: "parseHookPayload() — maps Claude Code JSON to NormalizedEvent"
      exports: ["parseHookPayload"]
    - path: "packages/daemon/src/adapters/claude/riskClassifier.ts"
      provides: "classifyRisk() — derives actionType + riskLevel from tool_name + tool_input"
      exports: ["classifyRisk"]
    - path: "packages/daemon/src/db/database.ts"
      provides: "openDatabase() with approvals + always_allow_rules tables migrated"
    - path: "packages/daemon/src/__tests__/hook-server.test.ts"
      provides: "Unit + integration tests for hook parsing, risk classification, session ID mapping"
  key_links:
    - from: "packages/daemon/src/adapters/claude/hookServer.ts"
      to: "packages/daemon/src/adapters/claude/hookParser.ts"
      via: "parseHookPayload() called inside req.on('end') handler"
      pattern: "parseHookPayload"
    - from: "packages/daemon/src/adapters/claude/hookParser.ts"
      to: "packages/daemon/src/adapters/claude/riskClassifier.ts"
      via: "classifyRisk() called for PreToolUse and PermissionRequest hooks"
      pattern: "classifyRisk"
    - from: "packages/daemon/src/adapters/claude/hookServer.ts"
      to: "eventBus"
      via: "onEvent callback injected at construction time"
      pattern: "onEvent\\(parsed\\)"
---

<objective>
Build the Claude Code hook adapter: HTTP server that ingests all Claude Code lifecycle hooks, parses them into NormalizedEvent objects, classifies risk for tool calls, maintains a session ID mapping cache, and migrates the SQLite schema to include the approvals and always_allow_rules tables.

Purpose: DAEMON-04 — the daemon cannot see Claude Code sessions until a hook adapter exists. APPR-02 — every pending approval must carry type and risk classification. This is the foundation the approval round-trip (Plan 02) builds on.
Output: Three new adapter files + extended database.ts + test suite.
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-claude-adapter-approval-foundation/02-RESEARCH.md
@.planning/phases/01-daemon-core/01-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/daemon/src/eventBus.ts:
```typescript
import { EventEmitter } from 'node:events';
import type { NormalizedEvent } from '@cockpit/shared';

class DaemonEventBus extends EventEmitter {
  emit(event: 'event', data: NormalizedEvent): boolean;
  on(event: 'event', listener: (data: NormalizedEvent) => void): this;
}

export const eventBus: DaemonEventBus;
```

From packages/shared/src/index.ts (exported types):
```typescript
export { NormalizedEventSchema, BaseEvent, SessionStartEvent, SessionEndEvent,
  ToolCallEvent, FileChangeEvent, ApprovalRequestEvent, ApprovalResolvedEvent,
  SubagentSpawnEvent, SubagentCompleteEvent, MemoryReadEvent, MemoryWriteEvent,
  ProviderParseErrorEvent } from './events.js';
export type { NormalizedEvent } from './events.js';
```

From packages/daemon/src/db/database.ts (existing — MUST be extended):
```typescript
export function openDatabase(dbPath: string): Database.Database;
// Currently creates: events table only
// Phase 2 adds: approvals table + always_allow_rules table inside the same db.exec()
```

From packages/daemon/src/db/queries.ts:
```typescript
export function persistEvent(db: Database.Database, event: NormalizedEvent): NormalizedEvent & { sequenceNumber: number };
export function getEventsSince(db: Database.Database, lastSeenSequence: number): Array<NormalizedEvent & { sequenceNumber: number }>;
```
</interfaces>
</context>

<feature>
  <name>Claude Code Hook Adapter</name>
  <files>
    packages/daemon/src/__tests__/hook-server.test.ts,
    packages/daemon/src/adapters/claude/hookParser.ts,
    packages/daemon/src/adapters/claude/riskClassifier.ts,
    packages/daemon/src/adapters/claude/hookServer.ts,
    packages/daemon/src/db/database.ts
  </files>
  <behavior>
    Test 1: parseHookPayload({ hook_event_name: 'SessionStart', session_id: 'abc', cwd: '/proj', ... }) returns event with type: 'session_start', provider: 'claude', workspacePath: '/proj'
    Test 2: parseHookPayload called twice with same session_id returns events with the same UUID sessionId
    Test 3: parseHookPayload({ hook_event_name: 'SessionEnd', session_id: 'abc' }) returns type: 'session_end'
    Test 4: parseHookPayload({ hook_event_name: 'PreToolUse', tool_name: 'Bash', tool_input: { command: 'ls' } }) returns type: 'tool_call', requiresApproval: false
    Test 5: parseHookPayload({ hook_event_name: 'PostToolUse', tool_name: 'Bash', tool_input: { command: 'ls' } }) returns type: 'tool_call'
    Test 6: parseHookPayload({ hook_event_name: 'SubagentStart', ... }) returns type: 'subagent_spawn'
    Test 7: parseHookPayload({ hook_event_name: 'SubagentStop', ... }) returns type: 'subagent_complete'
    Test 8: classifyRisk('Bash', { command: 'rm -rf /tmp' }) returns { actionType: 'shell_command', riskLevel: 'critical' }
    Test 9: classifyRisk('Bash', { command: 'curl https://example.com' }) returns { actionType: 'network_access', riskLevel: 'high' }
    Test 10: classifyRisk('Write', { path: 'foo.ts' }) returns { actionType: 'file_change', riskLevel: 'low' }
    Test 11: classifyRisk('MyMcpTool', {}) returns { actionType: 'mcp_tool_call', riskLevel: 'medium' }
    Test 12: HTTP POST to /hook with SessionStart payload emits event on injected onEvent callback and responds 200
    Test 13: HTTP POST to /hook with high-risk Bash PreToolUse calls onDecisionNeeded and does NOT close the response
    Test 14: resolveApproval(approvalId, 'allow') closes the held response with correct PreToolUse permissionDecision envelope
    Test 15: resolveApproval on expired approvalId is a no-op (does not throw)
    Test 16: openDatabase(':memory:') creates approvals and always_allow_rules tables (verified via PRAGMA table_list)
  </behavior>
  <implementation>
    RED phase: Create packages/daemon/src/__tests__/hook-server.test.ts with all 16 tests. Run — must all fail (files don't exist yet).

    GREEN phase (implement in this order — each depends on the previous):

    1. packages/daemon/src/adapters/claude/riskClassifier.ts
       - Export: classifyRisk(toolName: string, toolInput: Record<string, unknown>): { actionType: ActionType; riskLevel: RiskLevel; whyRisky: string }
       - ActionType = 'shell_command' | 'file_change' | 'network_access' | 'sandbox_escalation' | 'mcp_tool_call' | 'user_input'
       - RiskLevel = 'low' | 'medium' | 'high' | 'critical'
       - Rules: Bash + /rm\s+-rf|sudo|chmod 777|curl.*\|.*sh/ → critical; Bash + /curl|wget|ssh|git push|npm publish/ → high network; Bash default → medium shell; Write/Edit → low file_change; unknowns → medium mcp_tool_call
       - Built-in tool list: ['Bash', 'Write', 'Edit', 'Read', 'Glob', 'Grep', 'WebFetch', 'WebSearch', 'Agent', 'AskUserQuestion']
       - Export: requiresHumanApproval(toolName: string, toolInput: Record<string, unknown>): boolean — true if riskLevel is 'high' or 'critical'

    2. packages/daemon/src/adapters/claude/hookParser.ts
       - Define type HookPayload: { session_id: string; hook_event_name: string; cwd?: string; tool_name?: string; tool_use_id?: string; tool_input?: Record<string, unknown>; agent_id?: string; transcript_path?: string; permission_mode?: string }
       - Export: parseHookPayload(payload: HookPayload): { event: NormalizedEvent; requiresApproval: boolean }
       - Session ID cache: module-level Map<string, string> sessionIdCache — on first sight of session_id, call randomUUID() and store; reuse on subsequent calls
       - Mapping rules: SessionStart → session_start with workspacePath: cwd; SessionEnd → session_end; PreToolUse (non-blocking) → tool_call; PostToolUse → tool_call; SubagentStart → subagent_spawn; SubagentStop → subagent_complete; Notification → tool_call (informational)
       - For PreToolUse with requiresHumanApproval=true: return event type 'approval_request' with actionType + riskLevel + whyRisky from classifyRisk(), proposedAction = JSON.stringify(tool_input), affectedPaths = [], requiresApproval = true
       - For PermissionRequest hooks: always return approval_request with requiresApproval=true
       - All NormalizedEvent objects: sessionId = sessionIdCache lookup, provider = 'claude', schemaVersion = 1, timestamp = new Date().toISOString()

    3. packages/daemon/src/db/database.ts (EXTEND existing — do not replace)
       - Inside the existing db.exec() string, append the approvals and always_allow_rules table definitions AFTER the events table:
         - approvals: approval_id TEXT PRIMARY KEY, session_id TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'pending', action_type TEXT NOT NULL, risk_level TEXT NOT NULL, proposed_action TEXT NOT NULL, affected_paths TEXT, why_risky TEXT, created_at TEXT NOT NULL, decided_at TEXT, decision_reason TEXT
         - always_allow_rules: id INTEGER PRIMARY KEY, session_id TEXT NOT NULL, tool_name TEXT NOT NULL, pattern TEXT NOT NULL, created_at TEXT NOT NULL

    4. packages/daemon/src/adapters/claude/hookServer.ts
       - Export: createHookServer(port: number, onEvent: (event: NormalizedEvent) => void, onDecisionNeeded: (approvalId: string, event: NormalizedEvent) => void): http.Server
       - Module-level Map<string, { res: ServerResponse; timer: ReturnType<typeof setTimeout>; hookEventName: string }> named pendingApprovals
       - On POST /hook: parse body, call parseHookPayload(), if requiresApproval: store in pendingApprovals + call onDecisionNeeded, do NOT call res.end(); else: call onEvent, respond 200 {}
       - APPROVAL_TIMEOUT_MS = parseInt(process.env['COCKPIT_APPROVAL_TIMEOUT_MS'] ?? '60000', 10)
       - On timeout: delete from Map FIRST, then build deny envelope, call res.end (check res.writableEnded before writing)
       - Export: resolveApproval(approvalId: string, decision: 'allow' | 'deny' | 'always_allow', reason?: string): void
         - Delete from Map FIRST (claim), clearTimeout, check res.writableEnded, then call res.end with correct envelope
         - PreToolUse envelope: { hookSpecificOutput: { hookEventName: 'PreToolUse', permissionDecision: 'allow'|'deny', permissionDecisionReason? } }
         - PermissionRequest envelope: { hookSpecificOutput: { hookEventName: 'PermissionRequest', decision: { behavior: 'allow'|'deny' } } }
         - always_allow maps to 'allow' in the permissionDecision field
       - Non-POST requests: respond 405
       - Bad JSON body: respond 400
       - server.listen(port) called inside createHookServer before return
  </implementation>
</feature>

<verification>
```
cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit && pnpm run test -- --run --reporter=verbose
```

All 16 new hook-server tests green. Existing 25 tests remain green. Total suite passes.
</verification>

<success_criteria>
- 16 hook-server tests pass (all behaviors specified in behavior block)
- Existing 25 Phase 1 tests remain green
- openDatabase(':memory:') creates approvals + always_allow_rules tables
- resolveApproval on unknown approvalId is a no-op (no throw, no crash)
- HTTP server responds 405 for non-POST, 400 for bad JSON, 200 for valid hooks
</success_criteria>

<output>
After completion, create `.planning/phases/02-claude-adapter-approval-foundation/02-01-SUMMARY.md`
</output>
