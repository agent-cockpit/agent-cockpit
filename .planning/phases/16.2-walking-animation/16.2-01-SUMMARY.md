---
phase: 16.2-walking-animation
plan: 01
subsystem: game
tags: [gamestate, player, animation, vitest, typescript]

# Dependency graph
requires:
  - phase: 16.1-map-rendering-camera-zoom
    provides: GameState.ts player interface with x/y/direction
  - phase: 16-player-controls
    provides: movePlayer() pure function pattern
provides:
  - animTime: number field on GameState player interface and singleton
  - WALK_FRAME_DURATION_MS and WALK_FRAME_COUNT constants from PlayerInput.ts
  - animTime accumulation logic in movePlayer() (advance on move, reset on stop)
  - 4 automated tests covering animTime advance, accumulate, reset, and col wrap formula
affects:
  - 16.2-02 (draws walk frame using animTime)
  - 16.2-03 (player sprite walk animation uses animTime)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "animTime accumulated as raw ms total — frame col derived at render time via Math.floor(animTime / WALK_FRAME_DURATION_MS) % WALK_FRAME_COUNT"
    - "isMoving derived from key booleans (up || down || left || right) before INV_SQRT2 multiplication — clean and readable"

key-files:
  created: []
  modified:
    - packages/ui/src/game/GameState.ts
    - packages/ui/src/game/PlayerInput.ts
    - packages/ui/src/game/__tests__/PlayerInput.test.ts
    - packages/ui/src/game/__tests__/GameState.test.ts

key-decisions:
  - "animTime accumulated as total ms — plans 02/03 derive col via Math.floor(animTime/150)%4 at render time, not stored"
  - "isMoving uses key booleans directly (up||down||left||right) rather than post-normalization dx/dy for clarity"

patterns-established:
  - "Animation timing: raw ms accumulator in state, formula computed at render site"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-04-12
---

# Phase 16.2 Plan 01: Walking Animation State Summary

**animTime: number added to GameState.player with advance/reset logic in movePlayer() — 4 new tests, 41 game tests green**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-12T14:55:45Z
- **Completed:** 2026-04-12T14:57:30Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Added `animTime: number` to `GameState` player interface and initialized to `0` in singleton
- Exported `WALK_FRAME_DURATION_MS = 150` and `WALK_FRAME_COUNT = 4` constants from `PlayerInput.ts`
- Extended `movePlayer()` parameter type to include `animTime` — advances by `deltaMs` when any key held, resets to `0` when stopped
- Added 4 animTime test cases (advance, accumulate, reset, col-wrap formula) — all pass; updated `makePlayer()` helper and GameState.test.ts assertion

## Task Commits

Each task was committed atomically:

1. **Task 1: Add animTime to GameState + PlayerInput constants and logic** - `f3bd4e4` (feat)
2. **Task 2: Add animTime test cases to PlayerInput.test.ts** - `143787e` (test)

## Files Created/Modified
- `packages/ui/src/game/GameState.ts` - Added `animTime: number` to player interface and `animTime: 0` to singleton
- `packages/ui/src/game/PlayerInput.ts` - Added `WALK_FRAME_DURATION_MS`, `WALK_FRAME_COUNT` exports; extended `movePlayer()` type and added animTime logic
- `packages/ui/src/game/__tests__/PlayerInput.test.ts` - Updated import, updated `makePlayer()`, added `movePlayer — animTime` describe block with 4 tests
- `packages/ui/src/game/__tests__/GameState.test.ts` - Updated player assertion to include `animTime: 0`

## Decisions Made
- animTime stored as raw ms total; frame column derived at render time (`Math.floor(animTime / WALK_FRAME_DURATION_MS) % WALK_FRAME_COUNT`) — keeps state simple
- `isMoving` uses key booleans directly (`up || down || left || right`) rather than post-normalization dx/dy values — cleaner and equivalent

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated GameState.test.ts player assertion to include animTime**
- **Found during:** Task 2 (full test suite run)
- **Issue:** Existing test `toEqual({ x: 1472, y: 1376, direction: 'south' })` failed because `gameState.player` now also has `animTime: 0`; this was directly caused by our Task 1 change
- **Fix:** Updated assertion to `toEqual({ x: 1472, y: 1376, direction: 'south', animTime: 0 })`
- **Files modified:** `packages/ui/src/game/__tests__/GameState.test.ts`
- **Verification:** Full game test suite 41/41 pass
- **Committed in:** `143787e` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug caused by our own interface change)
**Impact on plan:** Required fix — our GameState change broke a direct assertion on the same object. No scope creep.

## Issues Encountered
None — pre-existing 3 failures in ApprovalInbox/approvalsSlice/eventsSlice tests confirmed present before our changes (verified with git stash baseline).

## Next Phase Readiness
- `animTime` is now in GameState.player and populated every frame when moving — Plans 02 and 03 can read it immediately
- `WALK_FRAME_DURATION_MS` and `WALK_FRAME_COUNT` exported for use in render sites
- All 41 game tests passing; no TypeScript errors in modified files

---
*Phase: 16.2-walking-animation*
*Completed: 2026-04-12*
