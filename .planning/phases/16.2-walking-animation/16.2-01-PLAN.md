---
phase: 16.2-walking-animation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/game/GameState.ts
  - packages/ui/src/game/PlayerInput.ts
  - packages/ui/src/game/__tests__/PlayerInput.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "animTime accumulates deltaMs on every frame the player is moving"
    - "animTime resets to exactly 0 when no movement keys are held"
    - "col formula Math.floor(animTime / 150) % 4 wraps cleanly from frame 3 back to frame 0"
    - "TypeScript compiles without error — player type and singleton both include animTime"
  artifacts:
    - path: "packages/ui/src/game/GameState.ts"
      provides: "Updated player interface with animTime: number"
      contains: "animTime: number"
    - path: "packages/ui/src/game/PlayerInput.ts"
      provides: "WALK_FRAME_DURATION_MS, WALK_FRAME_COUNT constants + animTime logic in movePlayer()"
      exports: ["WALK_FRAME_DURATION_MS", "WALK_FRAME_COUNT", "movePlayer"]
    - path: "packages/ui/src/game/__tests__/PlayerInput.test.ts"
      provides: "Automated tests for animTime advance, reset, and col wrapping"
      contains: "animTime"
  key_links:
    - from: "packages/ui/src/game/PlayerInput.ts movePlayer()"
      to: "player.animTime"
      via: "mutates animTime field when dx!==0 || dy!==0"
      pattern: "player\\.animTime"
    - from: "packages/ui/src/game/GameState.ts singleton"
      to: "animTime: 0"
      via: "initial value in gameState.player"
      pattern: "animTime: 0"
---

<objective>
Add animTime: number to GameState.player, advance it inside movePlayer() when moving and reset it to 0 when stopped, then cover the new behavior with unit tests.

Purpose: This is the prerequisite state change that Plans 02 and 03 both depend on. Without animTime in GameState, the render sites have no data to drive frame selection.
Output: Updated GameState.ts + PlayerInput.ts with animTime logic; PlayerInput.test.ts with four new test cases all passing.
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.2-walking-animation/16.2-RESEARCH.md

<interfaces>
<!-- Current GameState.ts — animTime is MISSING from player, must be added -->
From packages/ui/src/game/GameState.ts:
```typescript
export interface GameState {
  camera: CameraState
  player: { x: number; y: number; direction: string }
  npcs: Record<string, { x: number; y: number }>
  tick: number
}

export const gameState: GameState = {
  camera: { x: 0, y: 0, targetX: 0, targetY: 0, viewportW: 0, viewportH: 0, zoom: 2 },
  player: { x: 1472, y: 1376, direction: 'south' },
  npcs: {},
  tick: 0,
}
```

From packages/ui/src/game/PlayerInput.ts — current movePlayer() signature:
```typescript
export function movePlayer(
  player: { x: number; y: number; direction: string },
  keys: ReadonlySet<string>,
  deltaMs: number,
): void
```

From packages/ui/src/game/__tests__/PlayerInput.test.ts — makePlayer() helper must also be updated:
```typescript
function makePlayer(x = 0, y = 0, direction = 'south') {
  return { x, y, direction }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add animTime to GameState + PlayerInput constants and logic</name>
  <files>packages/ui/src/game/GameState.ts, packages/ui/src/game/PlayerInput.ts</files>
  <behavior>
    - When movePlayer() is called with at least one movement key (KeyW/S/A/D or ArrowUp/Down/Left/Right), player.animTime increases by deltaMs
    - When movePlayer() is called with no movement keys held (dx===0 and dy===0), player.animTime is set to exactly 0
    - movePlayer() accepts player with animTime: number in its type; TypeScript compilation succeeds
  </behavior>
  <action>
    Step 1 — GameState.ts: Add animTime to the player interface and singleton.

    Interface before:
      player: { x: number; y: number; direction: string }
    Interface after:
      player: { x: number; y: number; direction: string; animTime: number }

    Singleton before:
      player: { x: 1472, y: 1376, direction: 'south' }
    Singleton after:
      player: { x: 1472, y: 1376, direction: 'south', animTime: 0 }

    Step 2 — PlayerInput.ts: Export two new constants at the top of the file (after existing imports and PLAYER_SPEED):
      export const WALK_FRAME_DURATION_MS = 150
      export const WALK_FRAME_COUNT = 4

    Step 3 — PlayerInput.ts: Extend the movePlayer() player parameter type to include animTime:
      player: { x: number; y: number; direction: string; animTime: number }

    Step 4 — PlayerInput.ts: At the end of movePlayer(), after the direction update, add animTime logic:
      const isMoving = dx !== 0 || dy !== 0
      if (isMoving) {
        player.animTime += deltaMs
      } else {
        player.animTime = 0
      }

    Note: dx and dy are already computed earlier in movePlayer() (before diagonal normalization). "isMoving" must be derived from the raw integer dx/dy BEFORE the INV_SQRT2 multiplication, so they are still 0 or ±1. The existing code sets dx/dy from boolean arithmetic then multiplies — compute isMoving from the pre-multiplied values by checking whether up/down/left/right booleans produce any net movement. Simplest approach: capture dx and dy before the INV_SQRT2 line and check them there, OR check the booleans directly:
      const isMoving = up || down || left || right
    This is equivalent and cleaner.
  </action>
  <verify>
    <automated>cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui && npx vitest run src/game/__tests__/PlayerInput.test.ts</automated>
  </verify>
  <done>All existing PlayerInput tests pass without modification (game logic unchanged). TypeScript reports no errors on GameState.ts and PlayerInput.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add animTime test cases to PlayerInput.test.ts</name>
  <files>packages/ui/src/game/__tests__/PlayerInput.test.ts</files>
  <behavior>
    - animTime advances by deltaMs when player is moving (any key held)
    - animTime resets to 0 when no keys pressed (even if animTime was large)
    - animTime stays 0 when called with empty key set
    - col formula Math.floor(animTime / WALK_FRAME_DURATION_MS) % WALK_FRAME_COUNT wraps at frame 3 → frame 0
  </behavior>
  <action>
    Step 1 — Update makePlayer() helper to include animTime:
      function makePlayer(x = 0, y = 0, direction = 'south', animTime = 0) {
        return { x, y, direction, animTime }
      }

    Step 2 — Add WALK_FRAME_DURATION_MS and WALK_FRAME_COUNT to the import from PlayerInput.js:
      import { PLAYER_SPEED, WALK_FRAME_DURATION_MS, WALK_FRAME_COUNT, attachInput, detachInput, getKeysDown, movePlayer } from '../PlayerInput.js'

    Step 3 — Add a new describe block "movePlayer — animTime" with these four test cases:

    Test 1 (advance when moving):
      it('animTime advances by deltaMs when a key is held', () => {
        const player = makePlayer(SAFE_X, SAFE_Y)
        movePlayer(player, keys('KeyW'), 200)
        expect(player.animTime).toBe(200)
      })

    Test 2 (accumulates across calls):
      it('animTime accumulates across multiple frames when moving', () => {
        const player = makePlayer(SAFE_X, SAFE_Y)
        movePlayer(player, keys('KeyW'), 150)
        movePlayer(player, keys('KeyW'), 150)
        expect(player.animTime).toBe(300)
      })

    Test 3 (resets when stopped):
      it('animTime resets to 0 when no keys are held', () => {
        const player = makePlayer(SAFE_X, SAFE_Y, 'south', 999)
        movePlayer(player, keys(), 16)
        expect(player.animTime).toBe(0)
      })

    Test 4 (col wraps at frame 3):
      it('col formula wraps from frame 3 back to frame 0', () => {
        // At animTime = WALK_FRAME_DURATION_MS * WALK_FRAME_COUNT, col should be 0 again
        const cycleMs = WALK_FRAME_DURATION_MS * WALK_FRAME_COUNT
        const col = Math.floor(cycleMs / WALK_FRAME_DURATION_MS) % WALK_FRAME_COUNT
        expect(col).toBe(0)
        // Frame 3 is at (WALK_FRAME_COUNT - 1) * WALK_FRAME_DURATION_MS
        const frame3 = Math.floor((WALK_FRAME_DURATION_MS * 3) / WALK_FRAME_DURATION_MS) % WALK_FRAME_COUNT
        expect(frame3).toBe(3)
      })
  </action>
  <verify>
    <automated>cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui && npx vitest run src/game/__tests__/PlayerInput.test.ts</automated>
  </verify>
  <done>All existing tests pass. Four new animTime tests pass. Total test count in file increases by at least 4. No TypeScript errors.</done>
</task>

</tasks>

<verification>
Full suite must be green after Plan 01:
  cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui && npx vitest run

Check TypeScript compilation:
  cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui && npx tsc --noEmit
</verification>

<success_criteria>
- player interface in GameState.ts includes animTime: number
- gameState singleton initializes animTime: 0
- movePlayer() advances player.animTime by deltaMs when any movement key held
- movePlayer() resets player.animTime to 0 when no movement keys held
- WALK_FRAME_DURATION_MS = 150 and WALK_FRAME_COUNT = 4 exported from PlayerInput.ts
- All existing PlayerInput tests pass
- Four new animTime tests pass
</success_criteria>

<output>
After completion, create .planning/phases/16.2-walking-animation/16.2-01-SUMMARY.md
</output>
