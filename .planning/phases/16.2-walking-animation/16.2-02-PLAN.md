---
phase: 16.2-walking-animation
plan: 02
type: execute
wave: 2
depends_on:
  - 16.2-01
files_modified:
  - packages/ui/src/pages/OfficePage.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Player character sprite visually cycles through walk frames when WASD is held"
    - "Player sprite snaps back to frame 0 (col=0) the moment WASD is released"
    - "Frame selection is deterministic: col = Math.floor(player.animTime / WALK_FRAME_DURATION_MS) % WALK_FRAME_COUNT"
    - "No regression in NPC rendering or tilemap rendering"
  artifacts:
    - path: "packages/ui/src/pages/OfficePage.tsx"
      provides: "Player render site using computed col instead of hardcoded 0"
      contains: "WALK_FRAME_DURATION_MS"
  key_links:
    - from: "packages/ui/src/pages/OfficePage.tsx render()"
      to: "gameState.player.animTime"
      via: "col computed from animTime then used as source-x in ctx.drawImage"
      pattern: "player\\.animTime"
    - from: "packages/ui/src/pages/OfficePage.tsx"
      to: "packages/ui/src/game/PlayerInput.ts"
      via: "import { WALK_FRAME_DURATION_MS, WALK_FRAME_COUNT }"
      pattern: "WALK_FRAME_DURATION_MS"
---

<objective>
Wire the player render site in OfficePage.tsx to use gameState.player.animTime for column selection instead of the hardcoded 0.

Purpose: This is the only change that makes the player character visually animate. Plan 01 accumulates animTime; this plan reads it at draw time.
Output: OfficePage.tsx line 114 replaced — player sprite cycles through walk frames on WASD.
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16.2-walking-animation/16.2-RESEARCH.md
@.planning/phases/16.2-walking-animation/16.2-01-SUMMARY.md

<interfaces>
<!-- OfficePage.tsx Layer 2 player render block — line 108-115 (the target) -->
From packages/ui/src/pages/OfficePage.tsx:
```typescript
// Layer 2: Player sprite (existing code, coordinates unchanged)
const pImg = playerImgRef.current
if (pImg?.complete && pImg.naturalWidth > 0) {
  const px = gameState.player.x - gameState.camera.x
  const py = gameState.player.y - gameState.camera.y
  const row = DIRECTION_ROWS[gameState.player.direction as Direction] ?? 0
  ctx.drawImage(pImg, 0, row * 64, 64, 64, px, py, 64, 64)
  //                   ^ col=0 hardcoded — this is the only line to change
}
```

From packages/ui/src/game/PlayerInput.ts (after Plan 01):
```typescript
export const WALK_FRAME_DURATION_MS = 150
export const WALK_FRAME_COUNT = 4
```

From packages/ui/src/game/GameState.ts (after Plan 01):
```typescript
player: { x: number; y: number; direction: string; animTime: number }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import walk constants and replace col=0 in OfficePage player render</name>
  <files>packages/ui/src/pages/OfficePage.tsx</files>
  <action>
    Step 1 — Update the PlayerInput import at line 13 to include the new constants:
    Before:
      import { attachInput, detachInput, getKeysDown, movePlayer } from '../game/PlayerInput.js'
    After:
      import { attachInput, detachInput, getKeysDown, movePlayer, WALK_FRAME_DURATION_MS, WALK_FRAME_COUNT } from '../game/PlayerInput.js'

    Step 2 — In the render() method, locate the player sprite draw block (Layer 2, around line 110-115). Replace the single drawImage call:
    Before:
      ctx.drawImage(pImg, 0, row * 64, 64, 64, px, py, 64, 64)
    After:
      const col = Math.floor(gameState.player.animTime / WALK_FRAME_DURATION_MS) % WALK_FRAME_COUNT
      ctx.drawImage(pImg, col * 64, row * 64, 64, 64, px, py, 64, 64)

    That is the complete change. Do not touch the NPC render block (Layer 1) or any other code.

    DO NOT use tick-based col for the player — animTime is time-based for frame-rate independence.
    DO NOT make col a module-level variable — compute it inline inside the if-block.
  </action>
  <verify>
    <automated>cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui && npx vitest run</automated>
  </verify>
  <done>
    Full test suite passes (no regressions).
    TypeScript compiles cleanly: npx tsc --noEmit reports no errors.
    OfficePage.tsx imports WALK_FRAME_DURATION_MS and WALK_FRAME_COUNT from PlayerInput.
    Line that was `ctx.drawImage(pImg, 0, row * 64, …)` now reads `ctx.drawImage(pImg, col * 64, row * 64, …)`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Player walk animation wired in OfficePage.tsx. animTime drives col selection at the drawImage call site.</what-built>
  <how-to-verify>
    1. Start the dev server: cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit && npm run dev
    2. Open http://localhost:5173 and navigate to the Office page.
    3. Hold W, A, S, D (one at a time) — the player character sprite should visibly cycle through 4 walk frames. The sprite should NOT be a frozen static image.
    4. Release all keys — the player sprite must snap back to frame 0 (the neutral standing pose) immediately on key release. No mid-stride freeze.
    5. Try diagonal movement (W+D, S+A, etc.) — walking animation should still play.
    6. Verify NPCs and the tilemap are rendering correctly (no regressions).
  </how-to-verify>
  <resume-signal>Type "approved" if animation plays correctly, or describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
Full Vitest suite must pass:
  cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui && npx vitest run

TypeScript must compile cleanly:
  cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui && npx tsc --noEmit
</verification>

<success_criteria>
- OfficePage.tsx imports WALK_FRAME_DURATION_MS and WALK_FRAME_COUNT from PlayerInput.ts
- ctx.drawImage for player uses col * 64 as source-x (not hardcoded 0)
- col computed as Math.floor(gameState.player.animTime / WALK_FRAME_DURATION_MS) % WALK_FRAME_COUNT
- Human verifies: player animates on WASD, snaps to frame 0 on release
- All automated tests pass
</success_criteria>

<output>
After completion, create .planning/phases/16.2-walking-animation/16.2-02-SUMMARY.md
</output>
