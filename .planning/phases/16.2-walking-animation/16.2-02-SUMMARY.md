---
phase: 16.2-walking-animation
plan: "02"
subsystem: ui
tags: [canvas, sprite, animation, game-engine]

requires:
  - phase: 16.2-01
    provides: animTime accumulated in movePlayer() and stored on player state; WALK_FRAME_DURATION_MS and WALK_FRAME_COUNT exported from PlayerInput.ts

provides:
  - Player sprite column driven by animTime at the drawImage call site in OfficePage.tsx
  - Walking animation visible when WASD held; frame 0 on key release

affects:
  - Any future phase that modifies OfficePage render() player draw block
  - Phase 16.2-03 if further animation work is planned

tech-stack:
  added: []
  patterns:
    - "Render-site col computation: col = Math.floor(animTime / FRAME_MS) % FRAME_COUNT keeps animation logic at draw site, not in game loop"

key-files:
  created: []
  modified:
    - packages/ui/src/pages/OfficePage.tsx

key-decisions:
  - "col computed inline inside the player draw if-block; not a module-level variable — keeps scope minimal"

patterns-established:
  - "Animation frame selection is deterministic and time-based: col = Math.floor(animTime/WALK_FRAME_DURATION_MS) % WALK_FRAME_COUNT"

requirements-completed: []

duration: 5min
completed: 2026-04-12
---

# Phase 16.2 Plan 02: Walking Animation — Render Wire-up Summary

**Player walk animation wired in OfficePage.tsx: animTime drives col * 64 source-x in drawImage, producing 4-frame cycling walk cycle on WASD input**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-12T15:20:00Z
- **Completed:** 2026-04-12T15:25:00Z
- **Tasks:** 1 auto (+ 1 checkpoint:human-verify pending)
- **Files modified:** 1

## Accomplishments
- Added WALK_FRAME_DURATION_MS and WALK_FRAME_COUNT to the PlayerInput import in OfficePage.tsx
- Replaced hardcoded `0` source-x with `col * 64` where col is derived from animTime
- Player sprite now cycles through all 4 walk frames at 150ms per frame while WASD is held, snaps to frame 0 on release

## Task Commits

1. **Task 1: Import walk constants and replace col=0 in OfficePage player render** - `0e94d15` (feat)

**Plan metadata:** (pending final docs commit)

## Files Created/Modified
- `packages/ui/src/pages/OfficePage.tsx` - Added import of WALK_FRAME_DURATION_MS/WALK_FRAME_COUNT; replaced hardcoded col=0 with computed col in player drawImage call

## Decisions Made
None - followed plan as specified.

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
3 pre-existing test failures (ApprovalInbox badge, approvalsSlice `toBe` vs `toStrictEqual`, eventsSlice dedup) — confirmed pre-existing by running test suite on stashed (pre-change) code. Same 2 pre-existing TypeScript errors in ApprovalInboxPopup.test.tsx and setupTests.ts canvas stub. All out of scope for this plan.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Human verification checkpoint pending: user should open http://localhost:5173, navigate to Office, hold WASD, observe 4-frame walk cycle, release to confirm snap to frame 0
- Once approved, phase 16.2 walking animation is functionally complete

---
*Phase: 16.2-walking-animation*
*Completed: 2026-04-12*
