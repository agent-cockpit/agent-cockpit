---
phase: 16.2-walking-animation
plan: "02"
subsystem: ui
tags: [canvas, sprite, animation, game-engine]

requires:
  - phase: 16.2-01
    provides: animTime accumulated in movePlayer() and stored on player state; WALK_FRAME_DURATION_MS and WALK_FRAME_COUNT exported from PlayerInput.ts

provides:
  - Player sprite column driven by animTime at the drawImage call site in OfficePage.tsx
  - Walk row uses STATE_ROW_OFFSET.blocked (+8) to reach actual walking frames in the sprite sheet
  - Walking animation visible when WASD held; frame 0 on key release

affects:
  - Any future phase that modifies OfficePage render() player draw block
  - Phase 16.2-03 if further animation work is planned

tech-stack:
  added: []
  patterns:
    - "Render-site col computation: col = Math.floor(animTime / FRAME_MS) % FRAME_COUNT keeps animation logic at draw site, not in game loop"
    - "Walk row offset: DIRECTION_ROWS[dir] + STATE_ROW_OFFSET.blocked (+8) to reach actual walk-state rows in the sprite sheet"

key-files:
  created: []
  modified:
    - packages/ui/src/pages/OfficePage.tsx

key-decisions:
  - "col computed inline inside the player draw if-block; not a module-level variable — keeps scope minimal"
  - "Walk rows in the sprite sheet start at row +8 (STATE_ROW_OFFSET.blocked); rows 0-7 are idle/standing frames only"
  - "WALK_FRAME_COUNT raised 4→8 to match actual walking animation frames available in the sprite sheet"

patterns-established:
  - "Animation frame selection is deterministic and time-based: col = Math.floor(animTime/WALK_FRAME_DURATION_MS) % WALK_FRAME_COUNT"
  - "Sprite row for walking = DIRECTION_ROWS[direction] + STATE_ROW_OFFSET.blocked — base direction row + state offset"

requirements-completed: []

duration: ~40min
completed: 2026-04-12
---

# Phase 16.2 Plan 02: Walking Animation — Render Wire-up Summary

**Player walk animation wired in OfficePage.tsx using animTime at draw site, with row offset +8 (STATE_ROW_OFFSET.blocked) to reach actual walk frames and 8-frame cycle — visually verified**

## Performance

- **Duration:** ~40 min
- **Started:** 2026-04-12T15:20:00Z
- **Completed:** 2026-04-12
- **Tasks:** 1 auto + 1 human-verify checkpoint (approved)
- **Files modified:** 1

## Accomplishments
- Added WALK_FRAME_DURATION_MS and WALK_FRAME_COUNT to the PlayerInput import in OfficePage.tsx
- Replaced hardcoded `0` source-x with `col * 64` where col is derived from animTime
- Discovered and fixed sprite sheet row layout: idle rows 0-7 have no leg movement; actual walk frames begin at row 8 (STATE_ROW_OFFSET.blocked)
- Raised WALK_FRAME_COUNT 4→8 to match all available walk frames in the sprite sheet
- Human verified: player animates with visible leg movement on WASD, snaps to frame 0 on key release, no NPC or tilemap regressions

## Task Commits

1. **Task 1: Import walk constants and replace col=0 in OfficePage player render** - `0e94d15` (feat)
2. **Fix: Use walk row (STATE_ROW_OFFSET.blocked+8) for player movement** - `bd7187f` (fix)

## Files Created/Modified
- `packages/ui/src/pages/OfficePage.tsx` - Added import of WALK_FRAME_DURATION_MS/WALK_FRAME_COUNT; replaced hardcoded col=0 with computed col; added STATE_ROW_OFFSET.blocked to row computation for actual walk frames

## Decisions Made
- Walk rows in the sprite sheet start at row +8 (STATE_ROW_OFFSET.blocked); rows 0-7 are idle/standing-bob frames with no leg animation
- WALK_FRAME_COUNT raised from 4 to 8 to cover all walk frames present in the sprite sheet at rows 8-15

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Sprite sheet walk rows are at +8 offset (STATE_ROW_OFFSET.blocked), not row 0**

- **Found during:** human-verify checkpoint
- **Issue:** Walking animation was running but showed no leg movement — player cycled through idle/standing-bob frames (rows 0-7). Actual walk frames are in rows 8-15 (walk state).
- **Fix:** Added `STATE_ROW_OFFSET.blocked` offset to the row computation in OfficePage player render block. Raised WALK_FRAME_COUNT 4→8 to match actual walk frame count.
- **Files modified:** `packages/ui/src/pages/OfficePage.tsx`
- **Verification:** Human confirmed walking animation shows visible leg movement after fix; snap-to-frame-0 on key release also confirmed
- **Committed in:** `bd7187f`

---

**Total deviations:** 1 auto-fixed (Rule 1 — incorrect sprite row offset)
**Impact on plan:** Fix was essential for visual correctness. Without the row offset the animation ran but displayed no actual walking motion. No scope creep.

## Issues Encountered
3 pre-existing test failures (ApprovalInbox badge, approvalsSlice `toBe` vs `toStrictEqual`, eventsSlice dedup) — confirmed pre-existing, out of scope for this plan.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Player walk animation fully functional: col cycles through 8 real walk frames on WASD, snaps to col 0 on key release
- STATE_ROW_OFFSET.blocked pattern established for walking-state rows — Phase 16.2-03 NPC animation can follow same convention
- Phase 16.2 walking animation complete

---
*Phase: 16.2-walking-animation*
*Completed: 2026-04-12*
