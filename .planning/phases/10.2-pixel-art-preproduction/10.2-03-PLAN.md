---
phase: 10.2-pixel-art-preproduction
plan: 03
type: research
wave: 3
depends_on: [10.2-00, 10.2-01, 10.2-02]
autonomous: true
output: .planning/phases/10.2-pixel-art-preproduction/10.2-03-BUDGET-PLAN.md

---

<objective>
Using the backend feature catalog (Plan 00), animation API costs (Plan 01), and style references (Plan 02), allocate the 2000-image budget across all the assets we need to generate. Output a prioritized generation manifest that ensures the most critical assets are generated first.

Purpose: 2000 images sounds like a lot but can disappear quickly. We need a plan that guarantees core assets (agent sprites for all states) before spending on decorative tiles or backgrounds.
Output: 10.2-03-BUDGET-PLAN.md — a concrete generation plan with image counts per asset.
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10.2-pixel-art-preproduction/10.2-00-BACKEND-FEATURES.md
@.planning/phases/10.2-pixel-art-preproduction/10.2-01-ANIMATION-RESEARCH.md
@.planning/phases/10.2-pixel-art-preproduction/10.2-02-REFERENCES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Determine cost per asset type</name>
  <action>
    From 10.2-01-ANIMATION-RESEARCH.md, extract the exact image cost for:
    - 1x create_character call (with n_directions: 4)
    - 1x animate_character call per animation template
    - 1x create_map_object call
    - 1x create_isometric_tile call
    - 1x create_topdown_tileset call

    Calculate:
    - Cost to generate 1 fully-animated character (base + all 8 states)
    - Cost to generate 1 simple character (base + idle only)
    - Cost to generate a set of background tiles (e.g. 5 tile types)
  </action>
  <done>Cost per asset type calculated. Totals verified against 2000-image budget.</done>
</task>

<task type="auto">
  <name>Task 2: Define asset tiers by priority</name>
  <action>
    From 10.2-00-BACKEND-FEATURES.md (Pixel Art Needs Summary), organize assets into tiers:

    **Tier 1 — Critical (must have)**
    Assets that make the app non-functional without them:
    - Agent sprite sheet for the Office Mode (all 8 states)
    - This is the /sprites/agent-sheet.png the code already references

    **Tier 2 — High value (should have)**
    Assets that meaningfully improve the UX:
    - State icons for session list (active, blocked, completed)
    - Approval risk level badges (low/medium/high/critical)
    - UI accent elements (panel borders, header icons)

    **Tier 3 — Nice to have (if budget allows)**
    Assets that enhance the space theme but aren't functional:
    - Office Mode background tiles (space station floor/walls)
    - Decorative elements (stars, planets, nebula accents)
    - Loading/connecting animations

    For each tier, list every asset with its estimated image cost.
  </action>
  <done>Asset tiers defined with cost estimates per asset.</done>
</task>

<task type="auto">
  <name>Task 3: Build the generation manifest</name>
  <action>
    Create the generation manifest — an ordered list of PixelLab API calls:

    For each asset:
    - Asset name and description
    - PixelLab tool to call (create_character / animate_character / create_map_object / etc.)
    - Exact parameters to use (size, directions, style)
    - Estimated image cost
    - Running total after this asset
    - Tier (1/2/3)
    - Output file path (where the result will be saved)

    Ensure:
    - Tier 1 assets consume no more than 50% of the budget (≤1000 images)
    - Tier 2 assets consume no more than 30% (≤600 images)
    - Tier 3 assets use remaining budget (≤400 images)
    - Total never exceeds 1800 images (keep 200 as reserve for retries)

    Mark a "stop here if over budget" line at 1800 images.
  </action>
  <done>Generation manifest created. Total cost is within 2000-image limit.</done>
</task>

<task type="auto">
  <name>Task 4: Produce the budget plan document</name>
  <files>.planning/phases/10.2-pixel-art-preproduction/10.2-03-BUDGET-PLAN.md</files>
  <action>
    Write 10.2-03-BUDGET-PLAN.md with sections:

    ## Budget Summary
    Total: 2000 images available
    Tier 1 (Critical): X images
    Tier 2 (High value): Y images
    Tier 3 (Nice to have): Z images
    Reserve: 200 images
    Total planned: X+Y+Z images

    ## Cost Reference
    Table: asset type | tool | estimated images | notes

    ## Generation Manifest
    Ordered table: # | asset name | tier | tool | params summary | cost | running total | output path

    ## Retry Strategy
    How to handle failed generations (PixelLab returns bad quality or wrong style):
    - Max retries per asset before moving on
    - How to adjust prompts on retry
    - What to do if Tier 1 exhausts more budget than planned

    ## Integration Notes
    For each Tier 1 asset, note:
    - What file path the UI code expects
    - What dimensions are required
    - Whether assembly (sprite sheet stitching) is needed after generation
  </action>
  <done>10.2-03-BUDGET-PLAN.md exists with all sections complete.</done>
</task>

</tasks>

<verification>
- 10.2-03-BUDGET-PLAN.md exists and is non-empty
- Total planned images ≤ 1800 (with 200 reserve)
- Tier 1 assets include the agent sprite sheet for all 8 states
- Generation manifest is an ordered, numbered list
- Integration Notes section maps assets to UI file paths
</verification>

<success_criteria>
- The generation phase can execute this manifest sequentially without further planning
- Tier 1 assets are guaranteed to complete before budget is at risk
- Any executor can pick up this document and know exactly what to call and in what order
</success_criteria>

<output>
After completion, create `.planning/phases/10.2-pixel-art-preproduction/10.2-03-SUMMARY.md`
</output>
