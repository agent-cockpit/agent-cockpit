---
phase: 10.2-pixel-art-preproduction
plan: 00
type: research
wave: 1
depends_on: []
autonomous: true
output: .planning/phases/10.2-pixel-art-preproduction/10.2-00-BACKEND-FEATURES.md

---

<objective>
Read the entire daemon, shared, and UI codebase and produce a structured catalog of every feature, event type, session state, tool category, and UI panel that will need visual representation in the pixel art redesign.

Purpose: The pixel art designer cannot make decisions without knowing what the app actually does. This document becomes the source of truth for what must be visually communicated.
Output: 10.2-00-BACKEND-FEATURES.md — a structured catalog organized by "what the UI needs to show".
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog all event types from shared package</name>
  <files>packages/shared/src/</files>
  <action>
    Read packages/shared/src/ — specifically the events/types files.
    List every event type with:
    - Event name (e.g. tool_call, approval_request)
    - Fields relevant to UI display
    - When it fires during a session lifecycle
    - What a user would see/care about

    Group by category:
    - Session lifecycle events (session_start, session_end, etc.)
    - Tool events (tool_call, file_change, etc.)
    - Approval events (approval_request, approval_resolved)
    - Memory events (memory_read, memory_write)
    - Error/failure events
  </action>
  <done>All event types cataloged with fields and UI relevance noted.</done>
</task>

<task type="auto">
  <name>Task 2: Catalog all daemon features and capabilities</name>
  <files>packages/daemon/src/</files>
  <action>
    Read packages/daemon/src/ — routes, ws handlers, hookParser, db schema.
    Extract:
    - What agents the daemon supports (Claude Code, Codex, others?)
    - What the daemon tracks (sessions, tools, approvals, memory, timeline)
    - What approval types exist (shell_command, file_change, network_access, etc.)
    - What risk levels exist (low, medium, high, critical)
    - What session statuses exist (active, ended, failed, etc.)
    - What the WebSocket protocol sends/receives
  </action>
  <done>Daemon capabilities documented — agents supported, approval types, session states.</done>
</task>

<task type="auto">
  <name>Task 3: Catalog all UI pages, panels, and current states</name>
  <files>packages/ui/src/</files>
  <action>
    Read all UI components and pages:
    - OfficePage + AgentSprite + spriteStates.ts → list all AgentAnimState values and how they're derived
    - SessionListPanel, SessionCard → what session metadata is shown
    - ApprovalInbox → what approval data is displayed
    - TimelinePanel → what timeline events look like
    - DiffPanel, ArtifactsPanel → what code review features exist
    - MemoryPanel → what memory editing looks like
    - HistoryPage, SearchBar → search and history features
    - OpsLayout → overall navigation structure

    For each: describe what the user sees and what visual states it has.
  </action>
  <done>All UI screens cataloged with their visual states and data needs.</done>
</task>

<task type="auto">
  <name>Task 4: Produce the features document</name>
  <files>.planning/phases/10.2-pixel-art-preproduction/10.2-00-BACKEND-FEATURES.md</files>
  <action>
    Write 10.2-00-BACKEND-FEATURES.md with sections:

    ## Agent States (what the sprite must animate)
    List all AgentAnimState values, the events that trigger them, and what visual emotion/action they represent.

    ## Event Types (what happens in a session)
    Full table: event name | triggered by | visual meaning | UI panel that shows it

    ## Session Metadata (what labels appear)
    Session ID, workspace path, start time, status, pending approval count, etc.

    ## Approval System (what needs attention UI)
    All actionTypes, riskLevels, and what the user must decide.

    ## UI Screens (what exists in the app)
    List of all pages/panels with their purpose and visual complexity.

    ## Pixel Art Needs Summary
    A prioritized list of visual assets the pixel art redesign must produce, in order of importance.
  </action>
  <done>10.2-00-BACKEND-FEATURES.md exists and covers all sections above.</done>
</task>

</tasks>

<verification>
- 10.2-00-BACKEND-FEATURES.md exists and is non-empty
- "Pixel Art Needs Summary" section exists with a prioritized asset list
- All 8 AgentAnimState values are documented with their trigger conditions
- All approval actionTypes and riskLevels are listed
</verification>

<success_criteria>
- Any designer reading this document can understand exactly what the app does and what needs visual representation
- The prioritized asset list can be used directly to build the PixelLab generation budget in Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/10.2-pixel-art-preproduction/10.2-00-SUMMARY.md`
</output>
