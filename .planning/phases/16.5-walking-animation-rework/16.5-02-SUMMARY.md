---
phase: 16.5-walking-animation-rework
plan: "02"
subsystem: ui
tags: [sprite-animation, player-movement, canvas, walking]

# Dependency graph
requires:
  - phase: 16.5-01
    provides: moonwalk fix (isMoving dx/dy guard) and WALK_FRAME_DURATION_MS=100
  - phase: 16.4-02
    provides: STATE_ROW_OFFSET.walk=32 + astronaut walk sprite rows in sheet
provides:
  - Verification that STATE_ROW_OFFSET.walk=32 is wired to OfficePage.tsx player render
  - Human QA sign-off on all 4 directions, no moonwalk, timing feel
affects:
  - OfficePage.tsx (player render row selection)
  - spriteStates.ts (AnimationState, STATE_ROW_OFFSET)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Player render ternary uses STATE_ROW_OFFSET.walk for moving branch, .idle for stopped

key-files:
  created: []
  modified:
    - packages/ui/src/components/office/spriteStates.ts
    - packages/ui/src/pages/OfficePage.tsx

key-decisions:
  - "STATE_ROW_OFFSET.walk=32 was already added in Phase 16.4-02 (commit b24fe6b) — no duplication needed"
  - "OfficePage.tsx stateOffset ternary was already fixed in Phase 16.4-02 (commit 2637b72)"
  - "3 pre-existing test failures in approvalsSlice/eventsSlice are out-of-scope, logged as deferred"

patterns-established:
  - "Walk row selection: stateOffset = animTime > 0 ? STATE_ROW_OFFSET.walk : STATE_ROW_OFFSET.idle"

requirements-completed:
  - animation-direction

# Metrics
duration: 5min
completed: 2026-04-12
---

# Phase 16.5 Plan 02: Walk Row Fix + Visual QA Summary

**STATE_ROW_OFFSET.walk=32 confirmed wired in OfficePage.tsx; all 3 walk bug fixes (moonwalk, timing, wrong row) verified correct — awaiting human visual QA sign-off**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-12T20:05:00Z
- **Completed:** 2026-04-12T20:06:04Z (checkpoint reached — awaiting human QA)
- **Tasks:** 1/2 complete (Task 2 is human-verify checkpoint)
- **Files modified:** 0 (Phase 16.4-02 already applied all code changes)

## Accomplishments

- Verified `AnimationState` union includes `'walk'` in spriteStates.ts (line 14)
- Verified `STATE_ROW_OFFSET.walk = 32` in spriteStates.ts (line 32)
- Verified `OfficePage.tsx` line 115 uses `STATE_ROW_OFFSET.walk` (not `.blocked`) for moving branch
- Confirmed 333 tests pass; 3 pre-existing failures are unrelated (approvalsSlice, eventsSlice)
- All 3 walk bugs fixed across Plans 01 + 02: moonwalk guard, 100ms timing, correct row selection

## Task Commits

No new commits for this plan — Phase 16.4-02 already delivered the code changes:

- `b24fe6b` — feat(16.4-02): extend AnimationState union + STATE_ROW_OFFSET with walk=32
- `2637b72` — feat(16.4-02): rebuild astronaut sheet with walk rows, fix OfficePage player render
- `d570820` — fix(16.5-01): fix isMoving guard (dx/dy) and WALK_FRAME_DURATION_MS=100 (GREEN)

**Checkpoint:** Human QA (Task 2) pending — awaiting visual verification.

## Files Created/Modified

- `packages/ui/src/components/office/spriteStates.ts` — AnimationState includes 'walk', STATE_ROW_OFFSET.walk=32 (Phase 16.4-02)
- `packages/ui/src/pages/OfficePage.tsx` — stateOffset ternary uses STATE_ROW_OFFSET.walk (Phase 16.4-02)

## Decisions Made

- Phase 16.4-02 already applied all code changes before this plan ran — Task 1 was verification-only with no new edits needed
- 3 pre-existing test failures (approvalsSlice.test.ts, eventsSlice.test.ts) deferred — out-of-scope per deviation rules

## Deviations from Plan

None — plan executed exactly as written. Phase 16.4 already applied the code changes; Task 1 verification confirmed correctness.

## Issues Encountered

3 pre-existing test failures found in unrelated test files:
- `approvalsSlice.test.ts` — Object.is equality assertion using `toBe` instead of `toStrictEqual`
- `eventsSlice.test.ts` — Same Object.is equality issue

These are not related to animation/sprite code. Logged to deferred-items.md.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

After human QA approval:
- Walk animation fully verified correct in all 4 directions
- No moonwalk regression
- Natural timing (100ms frame duration, 800ms cycle)
- Phase 16.5 complete — game-quality player movement achieved

---
*Phase: 16.5-walking-animation-rework*
*Completed: 2026-04-12*
