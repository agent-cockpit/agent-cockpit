---
phase: 16.5-walking-animation-rework
verified: 2026-04-12T20:30:00Z
status: human_needed
score: 7/7 must-haves verified
human_verification:
  - test: "Walk all 4 cardinal directions"
    expected: "Correct sprite rows cycle; idle frame 0 shown on release"
    why_human: "Visual sprite rendering cannot be verified programmatically"
  - test: "Opposing-key moonwalk regression (hold W+S simultaneously)"
    expected: "Player stays completely still, no walk animation plays"
    why_human: "Automated test confirms animTime=0, but visual confirmation of idle frame is needed"
  - test: "Frame timing feel (walk for ~2 seconds)"
    expected: "Natural humanoid gait, no sliding, feet appear to plant coherently"
    why_human: "Subjective animation quality requires human judgement"
---

# Phase 16.5: Walking Animation Rework Verification Report

**Phase Goal:** Fix the three walking animation bugs — moonwalk (opposing keys), frame timing (150ms too slow), and wrong sprite row (blocked=8 instead of walk=32).
**Verified:** 2026-04-12T20:30:00Z
**Status:** human_needed (all automated checks pass; human QA already approved per SUMMARY)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                             | Status     | Evidence                                                                                           |
|----|-----------------------------------------------------------------------------------|------------|----------------------------------------------------------------------------------------------------|
| 1  | animTime does NOT advance when W and S are held simultaneously (opposing keys cancel) | VERIFIED | `isMoving = dx !== 0 \|\| dy !== 0` at PlayerInput.ts line 98; test "animTime stays 0 when opposing keys W+S cancel out" passes |
| 2  | animTime advances by deltaMs when a single directional key is held                | VERIFIED   | Same isMoving guard: when W alone held, dy=-1, isMoving=true; all 40 PlayerInput tests pass        |
| 3  | WALK_FRAME_DURATION_MS equals 100 (not 150)                                       | VERIFIED   | PlayerInput.ts line 5: `WALK_FRAME_DURATION_MS = 100`; test "WALK_FRAME_DURATION_MS is 100ms" passes |
| 4  | col formula wraps correctly at animTime = 800ms (100ms × 8 frames)                | VERIFIED   | Test "full 8-frame walk cycle completes in 800ms" passes; OfficePage.tsx line 117 uses correct formula |
| 5  | Walk animation plays from the dedicated walk row group (offset 32), not blocked row (offset 8) | VERIFIED | OfficePage.tsx line 115: `STATE_ROW_OFFSET.walk` (not `.blocked`) in stateOffset ternary |
| 6  | STATE_ROW_OFFSET.walk equals 32 in spriteStates.ts                                | VERIFIED   | spriteStates.ts line 32: `walk: 32`                                                                |
| 7  | Player sprite shows correct direction row when walking in each of 4 cardinal directions | human_needed | Code wiring correct; visual confirmation per SUMMARY 02 "approved" from human QA                  |

**Score:** 6/7 truths verified programmatically; 7/7 with human QA approval recorded in SUMMARY.

---

## Required Artifacts

| Artifact                                                         | Expected                                            | Status     | Details                                                          |
|------------------------------------------------------------------|-----------------------------------------------------|------------|------------------------------------------------------------------|
| `packages/ui/src/game/PlayerInput.ts`                           | Fixed isMoving guard + WALK_FRAME_DURATION_MS=100   | VERIFIED   | Line 5: `= 100`; line 98: `dx !== 0 \|\| dy !== 0`              |
| `packages/ui/src/game/__tests__/PlayerInput.test.ts`            | 4 new regression tests, all passing                 | VERIFIED   | Tests at lines 321, 327, 333, 337 all present; 40/40 tests pass |
| `packages/ui/src/components/office/spriteStates.ts`             | AnimationState includes 'walk', STATE_ROW_OFFSET.walk=32 | VERIFIED | Line 14 union includes 'walk'; line 32 `walk: 32`               |
| `packages/ui/src/pages/OfficePage.tsx`                          | Player render uses STATE_ROW_OFFSET.walk for moving frames | VERIFIED | Line 115: `STATE_ROW_OFFSET.walk` in animTime>0 ternary branch  |

---

## Key Link Verification

| From                                         | To                                              | Via                              | Status   | Details                                                                          |
|----------------------------------------------|-------------------------------------------------|----------------------------------|----------|----------------------------------------------------------------------------------|
| `PlayerInput.test.ts`                        | `PlayerInput.ts`                                | `import WALK_FRAME_DURATION_MS, movePlayer` | WIRED | Line 2: `import { PLAYER_SPEED, WALK_FRAME_DURATION_MS, WALK_FRAME_COUNT, ..., movePlayer } from '../PlayerInput.js'` |
| `OfficePage.tsx`                             | `spriteStates.ts`                               | `import STATE_ROW_OFFSET`        | WIRED    | Line 8: `import { DIRECTION_ROWS, STATE_ROW_OFFSET } from '../components/office/spriteStates.js'`; used at line 115 as `STATE_ROW_OFFSET.walk` |

---

## Requirements Coverage

| Requirement       | Source Plan | Description                                              | Status    | Evidence                                                         |
|-------------------|-------------|----------------------------------------------------------|-----------|------------------------------------------------------------------|
| animation-timing  | 16.5-01     | Frame timing corrected from 150ms to 100ms              | SATISFIED | `WALK_FRAME_DURATION_MS = 100` in PlayerInput.ts; test confirms |
| animation-direction | 16.5-02   | Walk row uses STATE_ROW_OFFSET.walk (=32), not .blocked | SATISFIED | OfficePage.tsx line 115 uses `.walk`; spriteStates.ts has `walk: 32` |

---

## Anti-Patterns Found

None detected. No TODO/FIXME/placeholder/console.log in any of the four modified files.

---

## Human Verification Required

### 1. Walking in all 4 cardinal directions

**Test:** Start dev server, navigate to Office page. Hold S, W, D, A in turn.
**Expected:** Correct sprite rows cycle for each direction; idle frame 0 shown on key release.
**Why human:** Canvas drawImage rendering cannot be verified programmatically.

### 2. Moonwalk regression

**Test:** Hold W and S simultaneously.
**Expected:** Player stands completely still, no walk animation cycles.
**Why human:** Automated test confirms `animTime` stays at 0; visual confirmation of idle sprite display requires human eyes.

### 3. Timing feel

**Test:** Walk in any direction for ~2 seconds.
**Expected:** Natural humanoid gait — not sliding (old 150ms bug), not twitchy. Feet appear to plant and lift coherently.
**Why human:** Subjective animation quality requires human judgement.

**Note:** Per 16.5-02-SUMMARY.md, human QA was already completed and "approved" — all 7 visual tests (4 cardinal directions, moonwalk regression, timing feel, diagonal) passed. The human_needed items above are noted for completeness but have already been signed off.

---

## Gaps Summary

No gaps. All three bugs are fixed:

1. **Moonwalk bug** — `isMoving = dx !== 0 || dy !== 0` replaces `up || down || left || right`. Opposing key pairs (W+S, A+D) now produce zero displacement and zero animTime advancement. Guarded by two regression tests.

2. **Frame timing bug** — `WALK_FRAME_DURATION_MS` reduced from 150 to 100. Walk cycle is now 800ms (10fps). Guarded by two constant/cycle-duration tests.

3. **Wrong sprite row bug** — `STATE_ROW_OFFSET.walk = 32` is present in spriteStates.ts and wired in OfficePage.tsx line 115. Player no longer shows fight-stance (row 8) during movement.

Commits: `3032f32` (RED tests), `d570820` (GREEN fix), `b24fe6b` + `2637b72` (Phase 16.4-02 walk row wiring, already in place).

---

_Verified: 2026-04-12T20:30:00Z_
_Verifier: Claude (gsd-verifier)_
