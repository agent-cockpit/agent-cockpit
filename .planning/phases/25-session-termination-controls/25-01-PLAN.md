---
phase: 25
plan: 01
type: execute
wave: 1
depends_on:
  - 22-01
  - 23-01
files_modified:
  - packages/daemon/src/ws/server.ts
  - packages/daemon/src/ws/handlers.ts
  - packages/daemon/src/adapters/codex/codexAdapter.ts
  - packages/daemon/src/adapters/claude/claudeLauncher.ts
  - packages/daemon/src/db/queries.ts
  - packages/daemon/src/__tests__/session-terminate.test.ts
  - packages/ui/src/components/layout/SessionListPanel.tsx
  - packages/ui/src/components/office/InstancePopupHub.tsx
  - packages/ui/src/components/sessions/SessionCard.tsx
  - packages/ui/src/hooks/useSessionEvents.ts
  - packages/ui/src/__tests__/SessionTerminateControls.test.tsx
requirements:
  - SESS-KILL-01
  - SESS-KILL-02
  - SESS-KILL-03
autonomous: true
must_haves:
  truths:
    - "Managed sessions expose canTerminateSession=true and can be terminated from UI"
    - "External sessions expose canTerminateSession=false and UI blocks kill with explicit reason"
    - "Terminate path emits deterministic lifecycle outcome (session_end or structured error)"
  artifacts:
    - path: "packages/daemon/src/ws/handlers.ts"
      provides: "session_terminate dispatch with capability guard"
      contains: "session_terminate"
    - path: "packages/ui/src/components/sessions/SessionCard.tsx"
      provides: "Capability-aware terminate action rendering"
      contains: "terminate"
  key_links:
    - from: "packages/ui/src/components/layout/SessionListPanel.tsx"
      to: "packages/ui/src/hooks/useSessionEvents.ts"
      via: "terminate action dispatch"
      pattern: "sendWsMessage"
    - from: "packages/daemon/src/ws/handlers.ts"
      to: "packages/daemon/src/adapters/codex/codexAdapter.ts"
      via: "runtime stop call"
      pattern: "stop"
---

<objective>
Implement explicit session termination controls with capability gating, enabling kill for daemon-managed sessions and blocking unsupported external sessions with clear UX.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-session-termination-controls/25-RESEARCH.md
@.planning/phases/25-session-termination-controls/25-VALIDATION.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add managed runtime termination contract in daemon</name>
  <files>packages/daemon/src/ws/server.ts, packages/daemon/src/ws/handlers.ts, packages/daemon/src/db/queries.ts, packages/daemon/src/__tests__/session-terminate.test.ts</files>
  <action>Create session runtime capability mapping and implement `session_terminate` handling with strict capability checks and structured outcomes.</action>
  <verify><automated>pnpm --filter @cockpit/daemon test --run ws && pnpm --filter @cockpit/daemon test --run launch-session</automated></verify>
  <done>Daemon can terminate managed sessions and rejects unsupported sessions predictably.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire provider-specific stop behavior safely</name>
  <files>packages/daemon/src/adapters/codex/codexAdapter.ts, packages/daemon/src/adapters/claude/claudeLauncher.ts</files>
  <action>Ensure provider runtime stop paths are idempotent and safe when process already exited.</action>
  <verify><automated>pnpm --filter @cockpit/daemon test --run codexAdapter</automated></verify>
  <done>Provider stop behavior is stable for repeated or late terminate requests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add capability-aware kill controls in UI surfaces</name>
  <files>packages/ui/src/components/layout/SessionListPanel.tsx, packages/ui/src/components/office/InstancePopupHub.tsx, packages/ui/src/components/sessions/SessionCard.tsx, packages/ui/src/hooks/useSessionEvents.ts, packages/ui/src/__tests__/SessionTerminateControls.test.tsx</files>
  <action>Add terminate actions with confirmation/in-flight/error states; render only when capability permits and show unsupported guidance otherwise.</action>
  <verify><automated>pnpm --filter @cockpit/ui test --run SessionTerminateControls && pnpm --filter @cockpit/ui test --run SessionListPanel && pnpm --filter @cockpit/ui test --run InstancePopupHub</automated></verify>
  <done>UI termination controls behave correctly for managed and external sessions.</done>
</task>
</tasks>

<verification>
pnpm --filter @cockpit/daemon test --run ws && pnpm --filter @cockpit/daemon test --run launch-session && pnpm --filter @cockpit/daemon test --run codexAdapter && pnpm --filter @cockpit/ui test --run SessionTerminateControls && pnpm --filter @cockpit/ui test --run SessionListPanel && pnpm --filter @cockpit/ui test --run InstancePopupHub
</verification>

<success_criteria>
Session termination is capability-driven, reliable for managed sessions, and explicitly blocked for external sessions without false-success behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/25-session-termination-controls/25-01-SUMMARY.md`.
</output>
