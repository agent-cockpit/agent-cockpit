---
phase: 16.10
slug: npc-spawn-inside-map-fix
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-13
---

# Phase 16.10 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (via `packages/ui/vite.config.ts`) |
| **Config file** | `packages/ui/vite.config.ts` |
| **Quick run command** | `cd packages/ui && npx vitest run --reporter=verbose 2>&1 | tail -20` |
| **Full suite command** | `cd packages/ui && npx vitest run 2>&1 | tail -30` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd packages/ui && npx vitest run --reporter=verbose 2>&1 | tail -20`
- **After every plan wave:** Run `cd packages/ui && npx vitest run 2>&1 | tail -30`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 16.10-01-01 | 01 | 0 | npc-spawn-position | unit stub | `cd packages/ui && npx vitest run src/__tests__/npcSpawnSlots.test.ts 2>&1 \| tail -15` | ❌ W0 | ⬜ pending |
| 16.10-01-02 | 01 | 1 | npc-spawn-position | unit | `cd packages/ui && npx vitest run src/__tests__/npcSpawnSlots.test.ts 2>&1 \| tail -15` | ❌ W0 | ⬜ pending |
| 16.10-01-03 | 01 | 1 | npc-spawn-position | manual | Visual QA — open OfficePage, spawn new sessions, confirm agents appear on walkable floor | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `packages/ui/src/__tests__/npcSpawnSlots.test.ts` — stubs for: `all SPAWN_SLOTS are within world bounds`, `SPAWN_SLOTS index cycles correctly beyond slot count`, `jitter does not exceed tile size`

*Existing infrastructure covers framework; only new test file needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| NPCs appear on walkable floor tiles | npc-spawn-position | Canvas rendering requires visual inspection | Open OfficePage, start 1–5 sessions from another terminal, confirm each agent sprite appears on open floor (not in void/space area) |
| Agent jitter prevents overlap stacking | npc-spawn-position | Visual overlap requires rendering | Start >12 sessions, confirm agents at same slot don't stack exactly on top of each other |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
