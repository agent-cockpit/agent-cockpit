---
phase: 18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/audio/audioSystem.ts
  - packages/ui/src/audio/useAudioBootstrap.ts
  - packages/ui/src/main.tsx
  - packages/ui/src/components/layout/MapSidebar.tsx
requirements:
  - ambient-music
  - volume-control
autonomous: true
---

<objective>
Implement the core audio runtime for Office mode: one reusable AudioContext, an ambient loop that starts after first user interaction, and persisted audio controls (mute + separate music/SFX volumes).
</objective>

<tasks>
1. Create a singleton audio runtime in `packages/ui/src/audio/audioSystem.ts`.
2. Gate audio unlock/start behind first user interaction to satisfy autoplay policy.
3. Persist audio settings in `localStorage` and expose reactive settings for UI controls.
4. Add sidebar controls in `MapSidebar` for mute, music volume, and SFX volume.
5. Bootstrap audio lifecycle from `main.tsx`.
</tasks>

<verification>
- `pnpm vitest run src/audio/__tests__/audioSystem.test.ts`
- `pnpm vitest run src/components/layout/__tests__/MapSidebar.test.tsx`
</verification>
