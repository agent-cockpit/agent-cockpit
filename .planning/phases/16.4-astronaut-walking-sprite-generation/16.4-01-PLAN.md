---
phase: 16.4-astronaut-walking-sprite-generation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/raw/astronaut/walk/south/
  - assets/raw/astronaut/walk/north/
  - assets/raw/astronaut/walk/east/
  - assets/raw/astronaut/walk/west/
  - assets/raw/astronaut/walk/south-east/
  - assets/raw/astronaut/walk/south-west/
  - assets/raw/astronaut/walk/north-east/
  - assets/raw/astronaut/walk/north-west/
autonomous: false
requirements:
  - sprite-generation

must_haves:
  truths:
    - "assets/raw/astronaut/walk/ exists with 8 direction subdirectories"
    - "Each direction directory contains frame-N.png files (N = 0 through count-1)"
    - "Frames are pixel art matching the existing astronaut art style (same character_id)"
    - "Actual frame count per direction is known and documented"
    - "Walk cycle is visually correct — feet alternate, arms swing, character moves laterally"
  artifacts:
    - path: "assets/raw/astronaut/walk/south/frame-0.png"
      provides: "First frame of south-facing walk animation"
    - path: "assets/raw/astronaut/walk/north/frame-0.png"
      provides: "First frame of north-facing walk animation"
    - path: "assets/raw/astronaut/walk/east/frame-0.png"
      provides: "First frame of east-facing walk animation"
    - path: "assets/raw/astronaut/walk/west/frame-0.png"
      provides: "First frame of west-facing walk animation"
  key_links:
    - from: "PixelLab animate_character"
      to: "assets/raw/astronaut/walk/"
      via: "ZIP download + unzip to correct directory structure"
      pattern: "assets/raw/astronaut/walk/{direction}/frame-N.png"
---

<objective>
Generate proper walking animation frames for the astronaut character using the PixelLab MCP API. The astronaut character_id `c76f4e82-7eec-4443-96fa-501def9d1656` is reused — no base character regeneration needed.

Purpose: The current player walk animation in Phase 16.2 reuses the `blocked` state rows (rows 8-15), which contain a "fight-stance-idle-8-frames" animation. This looks visually wrong — the player shuffles in a fighting stance instead of walking. This plan generates real walk cycle frames.

Output: `assets/raw/astronaut/walk/{direction}/frame-N.png` for all 8 directions — ready for sprite sheet assembly in Plan 02.
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.4-astronaut-walking-sprite-generation/16.4-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate walk cycle via PixelLab animate_character</name>
  <files>assets/raw/astronaut/walk/ (created directory tree)</files>
  <action>
Use the PixelLab MCP tool to generate walking animation for the astronaut.

Step 1 — Verify character is ready (no pending jobs):
Call `get_character` with `character_id: "c76f4e82-7eec-4443-96fa-501def9d1656"`. Confirm the character exists and has no animations in processing state. If any animation is still processing, wait 60 seconds and check again before proceeding.

Step 2 — Call animate_character:
```
tool: animate_character
character_id: "c76f4e82-7eec-4443-96fa-501def9d1656"
template_animation_id: "walking-8-frames"
directions: ["south", "north", "east", "west", "south-east", "south-west", "north-east", "north-west"]
```

If the call returns HTTP 422 (invalid template), use the fallback chain in this order:
1. `walking-8-frames` (preferred — matches WALK_FRAME_COUNT=8)
2. `walk`
3. `walking`
4. Custom: omit `template_animation_id`, add `action_description: "natural walking cycle, feet alternating, arms swinging"`

Document which template succeeded in the plan SUMMARY.

Step 3 — Poll for completion:
Call `get_character` every 60 seconds until the walk animation job status is NOT "processing". ETAs from the API are unreliable — "170s remaining" often means 7+ more minutes. Do not proceed to download until status is complete.

Step 4 — Download frames:
The API returns download URLs per direction. For each direction download the ZIP/frames. Organize to this exact structure:
```
assets/raw/astronaut/walk/south/frame-0.png ... frame-N.png
assets/raw/astronaut/walk/north/frame-0.png ... frame-N.png
assets/raw/astronaut/walk/east/frame-0.png ... frame-N.png
assets/raw/astronaut/walk/west/frame-0.png ... frame-N.png
assets/raw/astronaut/walk/south-east/frame-0.png ... frame-N.png
assets/raw/astronaut/walk/south-west/frame-0.png ... frame-N.png
assets/raw/astronaut/walk/north-east/frame-0.png ... frame-N.png
assets/raw/astronaut/walk/north-west/frame-0.png ... frame-N.png
```

If the API returns frames as a ZIP archive (Phase 13 pattern), use:
```bash
curl --fail -o /tmp/astronaut-walk.zip {download_url}
unzip /tmp/astronaut-walk.zip -d /tmp/astronaut-walk-tmp/
# Then move files to correct structure
```

Step 5 — Count frames per direction:
```bash
ls /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/assets/raw/astronaut/walk/south/ | grep "frame-" | wc -l
```

Record the actual frame count. If count ≠ 8, note it — Plan 02 must update WALK_FRAME_COUNT to match.

Critical anti-pattern: Do NOT attempt to queue another animation until this one is fully complete. PixelLab has an 8-slot limit per character.
  </action>
  <verify>
    <automated>ls /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/assets/raw/astronaut/walk/south/ && ls /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/assets/raw/astronaut/walk/north/ && ls /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/assets/raw/astronaut/walk/east/ && ls /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/assets/raw/astronaut/walk/west/</automated>
  </verify>
  <done>8 directories exist under assets/raw/astronaut/walk/ — one per direction. Each contains at least 1 frame-N.png file. Frame count per direction is documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Walk cycle frames for the astronaut character, generated via PixelLab animate_character. 8 directions × N frames stored at assets/raw/astronaut/walk/{direction}/frame-N.png</what-built>
  <how-to-verify>
Open the generated frame files in an image viewer and confirm:

1. Open assets/raw/astronaut/walk/south/frame-0.png through frame-N.png (all south frames in sequence)
2. Visually scan — frames should show an astronaut character walking: feet alternating, arms swinging, lateral movement implied
3. Confirm the art style matches the existing astronaut sprite (same character — same helmet, suit design)
4. Check assets/raw/astronaut/walk/east/ and west/ — character should face left/right respectively
5. The animation must look like WALKING, not a fight stance or idle shuffle

If frames look wrong (e.g., fight pose, wrong character, static frames):
- Note which template was used
- Claude should retry generation with the next fallback template before you approve

To view frames quickly on macOS:
```
open /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/assets/raw/astronaut/walk/south/
```
  </how-to-verify>
  <resume-signal>Type "approved" if walk frames look correct, or describe what looks wrong so Claude can retry with a different template</resume-signal>
</task>

</tasks>

<verification>
Walk frames exist at assets/raw/astronaut/walk/{direction}/frame-N.png for all 8 directions. Visually confirmed to show a walking cycle (not fight stance). Frame count per direction documented for Plan 02.
</verification>

<success_criteria>
- assets/raw/astronaut/walk/ has 8 subdirectories (one per direction)
- Each directory has at least 1 PNG frame file
- Art style matches existing astronaut character (same character_id used)
- Walk cycle is visually correct — approved by human checkpoint
- Actual frame count documented for use in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/16.4-astronaut-walking-sprite-generation/16.4-01-SUMMARY.md`

Include:
- Which template_animation_id worked (walking-8-frames / walk / walking / custom)
- Actual frame count per direction
- Any visual issues encountered and how resolved
- Confirmation that frames match art style
</output>
