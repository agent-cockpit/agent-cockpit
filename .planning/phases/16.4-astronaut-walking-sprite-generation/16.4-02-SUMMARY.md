---
phase: 16.4-astronaut-walking-sprite-generation
plan: "02"
subsystem: ui
tags: [sprite, animation, astronaut, pixel-art, canvas, vitest]

requires:
  - phase: 16.4-01
    provides: 64 raw walk frame PNGs at assets/raw/astronaut/walk/{direction}/frame-N.png

provides:
  - astronaut-sheet.png expanded to 576×2560px with walk rows 32-39
  - astronaut-manifest.json updated with walk frame count
  - spriteStates.ts AnimationState union includes 'walk', STATE_ROW_OFFSET.walk=32
  - OfficePage.tsx player uses STATE_ROW_OFFSET.walk during movement

affects:
  - 16.5-walking-animation-rework

tech-stack:
  added: []
  patterns:
    - "spriteStates.ts is the single source of truth for row offsets — walk=32 appended after failed=24"
    - "TDD RED/GREEN cycle: test written first, spriteStates.ts updated to pass"
    - "ANIM_STATES array order determines row layout in assembled sheet — walk appended at end, never reordered"

key-files:
  created:
    - packages/ui/src/components/office/__tests__/spriteStates.test.ts
  modified:
    - packages/ui/src/components/office/spriteStates.ts
    - scripts/build-spritesheet.ts
    - packages/ui/public/sprites/astronaut-sheet.png
    - packages/ui/public/sprites/astronaut-manifest.json
    - packages/ui/src/pages/OfficePage.tsx

key-decisions:
  - "STATE_ROW_OFFSET.walk=32 — walk rows occupy rows 32-39 (8 rows × 8 directions) after failed=24"
  - "Walk is player-only state — 'walk' NOT added to COLOR_STATE_TO_ANIMATION or AgentAnimState (NPC states untouched)"
  - "Walk frame count is 8 — matches WALK_FRAME_COUNT constant, no PlayerInput.ts change needed"
  - "OfficePage line 115 changed from STATE_ROW_OFFSET.blocked to STATE_ROW_OFFSET.walk"

patterns-established:
  - "TDD for sprite constants: write failing test first, then update constant to match"
  - "Sprite sheet row layout frozen by ANIM_STATES order — new states always appended at end"

requirements-completed: [sprite-integration]

duration: 15min
completed: 2026-04-12
---

# Phase 16.4 Plan 02: Sprite Sheet Assembly — Summary

**AnimationState union extended with walk=32, astronaut-sheet.png rebuilt to 576×2560px (40 rows), and OfficePage.tsx fixed to use STATE_ROW_OFFSET.walk so the player shows real walking motion instead of fight stance**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-12T19:40:00Z
- **Completed:** 2026-04-12T20:00:00Z
- **Tasks:** 3 of 3 (Task 3 was a human-verify checkpoint — approved)
- **Files modified:** 5 files + 1 test file created

## Accomplishments

- Extended `AnimationState` type union with `'walk'` and added `STATE_ROW_OFFSET.walk = 32` with full TDD Red→Green cycle
- Rebuilt `astronaut-sheet.png` from 576×2048px (32 rows) to 576×2560px (40 rows) with walk rows at rows 32–39
- Fixed `OfficePage.tsx` line 115 to use `STATE_ROW_OFFSET.walk` (was incorrectly using `.blocked`) so player movement shows real walk animation
- Human visual verification confirmed: walk cycle looks natural, NPCs show correct state animations with no regression

## Task Commits

1. **Task 1: Extend spriteStates.ts + write Wave 0 test** - `b24fe6b` (feat/test — TDD)
2. **Task 2: Extend build-spritesheet.ts, rebuild astronaut sheet, fix OfficePage.tsx** - `2637b72` (feat)
3. **Task 3: Human-verify checkpoint** - approved (no code commit — checkpoint only)

## Files Created/Modified

- `packages/ui/src/components/office/__tests__/spriteStates.test.ts` — 4 unit tests verifying walk offset and regression on existing offsets
- `packages/ui/src/components/office/spriteStates.ts` — `AnimationState` union + `STATE_ROW_OFFSET` extended with `walk: 32`
- `scripts/build-spritesheet.ts` — `ANIM_STATES` and `frameCounts` extended with `'walk'`
- `packages/ui/public/sprites/astronaut-sheet.png` — rebuilt at 576×2560px (40 rows)
- `packages/ui/public/sprites/astronaut-manifest.json` — updated with `"walk": 8`
- `packages/ui/src/pages/OfficePage.tsx` — line 115: `STATE_ROW_OFFSET.blocked` → `STATE_ROW_OFFSET.walk`

## Sprite Sheet Details

| Property | Value |
|----------|-------|
| Sheet dimensions | 576×2560px |
| Rows | 40 (rows 0–31 = idle/blocked/completed/failed; rows 32–39 = walk) |
| Columns | 9 (8 directions + 1 padding) |
| Walk frame count | 8 frames per direction |
| Frame size | 64×64px |

## Decisions Made

- `STATE_ROW_OFFSET.walk = 32` — walk rows begin at row 32, immediately after `failed` (rows 24–31). This is the natural append position and requires no reordering.
- `'walk'` was NOT added to `COLOR_STATE_TO_ANIMATION` or `AgentAnimState` because walking is a player-only state. NPC agents do not walk in this phase.
- `WALK_FRAME_COUNT = 8` confirmed from Plan 01 output — no change to `PlayerInput.ts` needed.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. The TDD cycle ran cleanly: tests failed on RED, passed on GREEN. Build script produced the correct 576×2560px sheet on the first run. TypeScript compile errors did not occur.

## Human Checkpoint Result

Task 3 `checkpoint:human-verify` was approved by user. Visual confirmation: player walks with correct walk cycle (feet alternating, arms swinging) when WASD is held. NPC agents continue to display blocked/completed/failed state animations correctly — no regression.

## Next Phase Readiness

- Walk animation is fully integrated and visually confirmed
- Phase 16.5 can proceed: it reworks `isMoving` detection, timing (100ms per frame), and ensures the correct walk row is selected dynamically
- No blockers — all success criteria met

---
*Phase: 16.4-astronaut-walking-sprite-generation*
*Completed: 2026-04-12*
