---
phase: 29-sidebar-menu-design-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md
autonomous: true
requirements: [game-ui-design]
must_haves:
  truths:
    - "A written design BRIEF exists documenting pixel-art UI patterns from 4 reference games (FTL, Dead Cells, Stardew Valley, Into the Breach)"
    - "BRIEF validates the cyan accent choice (oklch(0.75 0.18 195)) against reference game palettes"
    - "BRIEF validates provider badge hue separation (Claude hue 255 vs Codex hue 295) for distinctness on --color-panel-surface"
    - "BRIEF validates amber approval pill readability (oklch(0.75 0.16 75 / 0.2) bg + oklch(0.75 0.16 75 / 0.5) border) against both selected cyan-tinted and unselected dark row backgrounds"
    - "BRIEF renders/describes 5 required component patterns: session row (3 states), sidebar header, menu button, MenuPopup dialog, CharacterPicker"
  artifacts:
    - path: ".planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md"
      provides: "Design research findings from /frontend-design skill, reference game study, validation of color decisions"
      contains: "FTL, Dead Cells, Stardew Valley, Into the Breach, cyan, amber, Claude, Codex"
  key_links:
    - from: "29-01-BRIEF.md"
      to: "29-UI-SPEC.md"
      via: "Validates/refines every locked color and component pattern"
      pattern: "29-UI-SPEC"
---

<objective>
Run the `/frontend-design:frontend-design` skill to study 4 reference pixel-art games and produce a BRIEF.md that validates (or flags concerns about) every color, typography, and component decision locked in 29-UI-SPEC.md before implementation begins in Plan 02.

Purpose: The UI-SPEC is approved, but locked color values (cyan `oklch(0.75 0.18 195)`, provider badges hue 255/295, amber approval pill `oklch(0.75 0.16 75 / 0.2)`) need validation against established game-UI palettes. This plan produces the research artifact that de-risks Plan 02's token implementation.

Output: `.planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md` — single markdown design brief.
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/29-sidebar-menu-design-overhaul/29-UI-SPEC.md
</context>

<threat_model>
- Skill invocation may pull external image assets — no execution risk; output is markdown only.
- BRIEF.md contains no secrets, no user data. Low severity.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Invoke /frontend-design skill and produce BRIEF.md with reference game analysis</name>
  <files>.planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md</files>
  <read_first>
    - .planning/phases/29-sidebar-menu-design-overhaul/29-UI-SPEC.md (full file — primary source of truth)
    - packages/ui/src/index.css (existing @theme block and cockpit-* classes)
    - packages/ui/src/components/layout/MapSidebar.tsx (current sidebar reference)
    - packages/ui/src/components/office/MenuPopup.tsx (current menu chrome reference)
  </read_first>
  <action>
    Invoke the `/frontend-design:frontend-design` skill with the following exact prompt:

    ```
    Study pixel-art game UI patterns from these 4 reference games and produce a design brief:
    1. FTL: Faster Than Light — sidebar session panel, status indicators, icon+name rows, urgent event badges. Focus: information density, status dot placement, color economy.
    2. Dead Cells — HUD chrome and menu design. Focus: panels over game world without obscuring it.
    3. Stardew Valley — sidebar/toolbar layout. Focus: compact icon+label patterns, selection highlight, inventory grid.
    4. Into the Breach — mission select and unit status panel. Focus: monochrome-base + single accent, bracket/frame decoration, minimal typography.

    Validate these locked color decisions from 29-UI-SPEC.md:
    - Primary accent: --color-cockpit-cyan = oklch(0.75 0.18 195) (≈ #00d4e8). Is cyan the correct primary phosphor hue for a space game HUD vs green/amber alternatives?
    - Provider badge Claude: bg oklch(0.28 0.08 255 / 0.7), text oklch(0.75 0.14 240). Provider badge Codex: bg oklch(0.28 0.08 295 / 0.7), text oklch(0.75 0.14 295). Are hue 255 (blue) and hue 295 (purple) sufficiently distinct side-by-side on bg oklch(0.185 0.025 250)?
    - Amber approval pill: bg oklch(0.75 0.16 75 / 0.2), border oklch(0.75 0.16 75 / 0.5), text oklch(0.92 0.04 75). Is amber readable against both (a) selected row bg-cyan-500/10 and (b) default row bg-background/30?

    Render or describe 5 component patterns:
    1. Session row — default, hover, selected states (face avatar + project name + provider badge + status dot + approval pill)
    2. Sidebar header — MISSION CONTROL / Agent Cockpit title + ACTIVE:02 counter + HISTORY button in bracket frame
    3. Menu button (top-right overlay) — bg oklch(0.34 0.09 245 / 0.92), border oklch(0.70 0.14 210 / 0.72), box-shadow glow
    4. MenuPopup dialog — overlay bg-black/45, panel bg-background with cockpit-frame-full header + sections at bg var(--color-panel-surface)
    5. CharacterPicker — face preview + [<] / [>] nav buttons + [ CONFIRM ] button

    Deliver output as markdown saved to `.planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md` with sections:
    ## Reference Game Findings (1 section per game)
    ## Color Validation (cyan, provider hues, amber)
    ## Component Pattern Recommendations (5 patterns)
    ## Concerns / Deviations From UI-SPEC (if any — otherwise note "No deviations")
    ## Implementation Notes for Plan 02/03
    ```

    If the skill is not available or returns without a file, write the BRIEF.md manually by:
    1. Summarizing known patterns from FTL (status rows w/ pulsing dots, amber event warnings), Dead Cells (dark translucent panels over parallax), Stardew Valley (compact boxed rows, single accent highlight), Into the Breach (hairline frames + single cyan accent on near-black).
    2. Validating cyan `oklch(0.75 0.18 195)` — confirm it reads as phosphor cyan and is distinct from green (cockpit-green `oklch(0.68 0.20 155)`).
    3. Validating provider badges at 40° hue separation (255→295) on `--color-panel-surface oklch(0.185 0.025 250)` — document that blue and purple should be visually distinguishable.
    4. Validating amber `oklch(0.75 0.16 75 / 0.2)` pill contrast on both backgrounds.
    5. Writing the 5 component pattern descriptions with concrete Tailwind/CSS values pulled from 29-UI-SPEC.md.

    The BRIEF must explicitly reference all 4 game names (FTL, Dead Cells, Stardew Valley, Into the Breach) and all 3 validated color concerns (cyan, Claude/Codex, amber).
  </action>
  <verify>
    <automated>test -f .planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md && grep -q "FTL" .planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md && grep -q "Dead Cells" .planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md && grep -q "Stardew" .planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md && grep -q "Into the Breach" .planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md && grep -q "cyan" .planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md && grep -q "amber" .planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md && grep -qi "claude" .planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md && grep -qi "codex" .planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/29-sidebar-menu-design-overhaul/29-01-BRIEF.md` exists
    - BRIEF contains the literal strings: `FTL`, `Dead Cells`, `Stardew`, `Into the Breach`
    - BRIEF contains the literal strings: `cyan`, `amber`, `Claude`, `Codex`
    - BRIEF contains the string `oklch(0.75 0.18 195)` (the locked cyan value being validated)
    - BRIEF contains the strings `hue 255` AND `hue 295` (or equivalent references to provider-badge hue separation)
    - BRIEF has at least 5 markdown headers (##) covering reference findings, color validation, component patterns
    - BRIEF explicitly states whether cyan accent choice is validated or has concerns (search string: `validated` OR `concern`)
  </acceptance_criteria>
  <done>Design BRIEF.md file exists with all reference games, color validations, and 5 component pattern descriptions — Plan 02 executor can read it to confirm token values before writing @theme block.</done>
</task>

</tasks>

<verification>
- BRIEF.md file exists at the required path
- All 4 reference games named
- All 3 validated color concerns addressed (cyan, provider hues, amber)
- Explicit validation verdict present (validated / concern / deviation)
</verification>

<success_criteria>
Design research complete: executor in Plan 02 has a written artifact that confirms or refines every color/component decision from 29-UI-SPEC.md.
</success_criteria>

<output>
After completion, create `.planning/phases/29-sidebar-menu-design-overhaul/29-01-SUMMARY.md` summarizing:
- Reference game findings (1 line each)
- Validation verdicts for cyan, provider badges, amber approval pill
- Any deviations from UI-SPEC flagged for Plan 02 attention
</output>
