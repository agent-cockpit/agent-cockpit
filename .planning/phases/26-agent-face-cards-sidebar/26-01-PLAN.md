---
phase: 26-agent-face-cards-sidebar
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/copy-faces.ts
  - packages/ui/src/components/office/characterMapping.ts
  - packages/ui/src/components/office/__tests__/characterMapping.test.ts
autonomous: true
requirements:
  - sidebar-face-card

must_haves:
  truths:
    - "All 10 face PNGs are available at packages/ui/public/sprites/faces/{character}-face.png"
    - "characterFaceUrl('astronaut') returns '/sprites/faces/astronaut-face.png'"
    - "characterFaceUrl works for all 10 CHARACTER_TYPES"
  artifacts:
    - path: "scripts/copy-faces.ts"
      provides: "Build script that copies and renames raw face PNGs to public/sprites/faces/"
    - path: "packages/ui/src/components/office/characterMapping.ts"
      provides: "characterFaceUrl() helper function exported"
      exports: ["characterFaceUrl", "CHARACTER_TYPES", "CharacterType", "sessionToCharacter"]
    - path: "packages/ui/public/sprites/faces/"
      provides: "10 face PNGs at canonical filenames"
  key_links:
    - from: "scripts/copy-faces.ts"
      to: "packages/ui/public/sprites/faces/"
      via: "fs.copyFile + sharp.resize"
      pattern: "sprites/faces"
    - from: "packages/ui/src/components/layout/MapSidebar.tsx"
      to: "packages/ui/src/components/office/characterMapping.ts"
      via: "import { characterFaceUrl }"
      pattern: "characterFaceUrl"
---

<objective>
Publish face PNGs to the public asset path and add the `characterFaceUrl()` helper function that maps a character type to its browser-accessible URL.

Purpose: The sidebar avatar rendering in Plan 02 needs both the physical PNG files at a known URL and a typed helper to produce those URLs. This plan creates both.
Output: `scripts/copy-faces.ts` (asset pipeline), `packages/ui/public/sprites/faces/` (10 PNGs), extended `characterMapping.ts` with `characterFaceUrl()`.
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/ui/src/components/office/characterMapping.ts:
```typescript
export const CHARACTER_TYPES = [
  'astronaut', 'robot', 'alien', 'hologram', 'monkey',
  'caveman', 'ghost', 'ninja', 'pirate', 'medicine-woman',
] as const

export type CharacterType = typeof CHARACTER_TYPES[number]

export function sessionToCharacter(sessionId: string): CharacterType {
  const index = parseInt(sessionId.slice(-4), 16) % CHARACTER_TYPES.length
  return CHARACTER_TYPES[index]
}
// TO ADD:
export function characterFaceUrl(character: CharacterType): string
```

Raw face assets (confirmed present, all 10):
- assets/raw/astronaut/face/Screenshot Apr 14 2026 from remove.bg (9).png
- assets/raw/alien/face/Screenshot Apr 14 2026 from remove.bg (7).png
- assets/raw/caveman/face/Screenshot Apr 14 2026 from remove.bg (1).png
- assets/raw/ghost/face/Screenshot Apr 14 2026 from remove.bg (5).png
- assets/raw/hologram/face/Screenshot Apr 14 2026 from remove.bg (6).png
- assets/raw/medicine-woman/face/Screenshot Apr 14 2026 from Remove.bg.png
- assets/raw/monkey/face/Screenshot Apr 14 2026 from Remove.bg (4).png
- assets/raw/ninja/face/Screenshot Apr 14 2026 from remove.bg (3).png
- assets/raw/pirate/face/Screenshot Apr 14 2026 from Remove.bg (2).png
- assets/raw/robot/face/Screenshot Apr 14 2026 from Remove.bg (8).png

Destination: packages/ui/public/sprites/faces/{character}-face.png
Browser URL pattern: /sprites/faces/{character}-face.png
(Matches existing sprite URL pattern: /sprites/{character}-sheet.png)

Existing build script pattern: scripts/build-spritesheet.ts uses sharp + Node fs/promises
sharp is in devDependencies of packages/ui.
tsx is in devDependencies of root package.json.
Invocation pattern: npx tsx scripts/copy-faces.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add characterFaceUrl() to characterMapping.ts with tests</name>
  <files>packages/ui/src/components/office/characterMapping.ts, packages/ui/src/components/office/__tests__/characterMapping.test.ts</files>
  <behavior>
    - characterFaceUrl('astronaut') === '/sprites/faces/astronaut-face.png'
    - characterFaceUrl('medicine-woman') === '/sprites/faces/medicine-woman-face.png'
    - characterFaceUrl returns a string for all 10 CHARACTER_TYPES (parameterized test)
    - The URL always starts with '/sprites/faces/' and ends with '-face.png'
  </behavior>
  <action>
    RED: Create (or extend) `packages/ui/src/components/office/__tests__/characterMapping.test.ts`. Write tests for `characterFaceUrl` covering: all 10 CHARACTER_TYPES return correct URL, URL format is `/sprites/faces/{character}-face.png`. Run tests — they MUST fail (function not yet exported).

    GREEN: Add to `packages/ui/src/components/office/characterMapping.ts`:
    ```typescript
    export function characterFaceUrl(character: CharacterType): string {
      return `/sprites/faces/${character}-face.png`
    }
    ```
    Run tests — all must pass.

    Do NOT change any existing exports (CHARACTER_TYPES, CharacterType, sessionToCharacter) — only add the new function.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test -- characterMapping</automated>
  </verify>
  <done>characterMapping tests pass. characterFaceUrl is exported and returns correct URL for all 10 characters.</done>
</task>

<task type="auto">
  <name>Task 2: Write scripts/copy-faces.ts and run it</name>
  <files>scripts/copy-faces.ts</files>
  <action>
    Create `scripts/copy-faces.ts` at the repo root level (same location as `scripts/build-spritesheet.ts`).

    The script must:
    1. Import CHARACTER_TYPES from `../packages/ui/src/components/office/characterMapping.js` (use .js extension for ESM compatibility)
    2. Define ROOT as the directory two levels up from the script file (repo root), using `import.meta.url` + `path.resolve`
    3. Define OUT as `path.join(ROOT, 'packages/ui/public/sprites/faces')`
    4. Create OUT directory with `fs.mkdir(OUT, { recursive: true })`
    5. For each character in CHARACTER_TYPES:
       - Read `assets/raw/{character}/face/` directory
       - Filter entries to `.png` files only
       - If no PNG found, throw `Error: No face PNG found for {character}`
       - Use `sharp` to resize the first PNG found: `.resize(64, 64, { fit: 'cover' }).png()` and write to `path.join(OUT, '{character}-face.png')`
       - Log: `Copied {character}-face.png`

    Use sharp (already in packages/ui devDeps; import from 'sharp'). Use `node:fs/promises` and `node:path`.

    After writing the file, run it:
    ```bash
    npx tsx scripts/copy-faces.ts
    ```

    Then verify output:
    ```bash
    ls packages/ui/public/sprites/faces/
    ```
    All 10 files must appear: astronaut-face.png, robot-face.png, alien-face.png, hologram-face.png, monkey-face.png, caveman-face.png, ghost-face.png, ninja-face.png, pirate-face.png, medicine-woman-face.png.

    Also add a root-level npm script in `package.json`:
    ```json
    "copy-faces": "tsx scripts/copy-faces.ts"
    ```
    Add it alongside any existing `build-*` or `copy-*` scripts in the root `package.json`.
  </action>
  <verify>
    <automated>ls /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui/public/sprites/faces/ | wc -l</automated>
    Output must be 10.
  </verify>
  <done>10 face PNGs exist at packages/ui/public/sprites/faces/, all 64×64 squares with canonical filenames. copy-faces npm script added to root package.json.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @cockpit/ui test -- characterMapping` passes (characterFaceUrl tests green)
2. `ls packages/ui/public/sprites/faces/ | wc -l` outputs 10
3. No existing characterMapping exports changed (sessionToCharacter, CHARACTER_TYPES still work)
4. `pnpm test` (full suite) passes
</verification>

<success_criteria>
- characterFaceUrl() is exported from characterMapping.ts and returns correct URL for all 10 CHARACTER_TYPES
- 10 face PNGs exist at packages/ui/public/sprites/faces/{character}-face.png, each 64×64 pixels
- characterMapping tests pass
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/26-agent-face-cards-sidebar/26-01-SUMMARY.md`
</output>
