---
phase: 14-office-map-view-full-navigation-paradigm-shift
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/package.json
  - packages/ui/src/components/office/__tests__/InstancePopupHub.test.tsx
  - packages/ui/src/components/office/__tests__/HistoryPopup.test.tsx
autonomous: true
requirements:
  - popup-hub
  - history-popup
must_haves:
  truths:
    - "@radix-ui/react-dialog and @radix-ui/react-tabs are installed in packages/ui"
    - "Wave 0 test stubs exist for InstancePopupHub and HistoryPopup so subsequent plans have RED-state baselines"
  artifacts:
    - path: "packages/ui/package.json"
      provides: "Radix Dialog and Tabs dependencies"
      contains: "@radix-ui/react-dialog"
    - path: "packages/ui/src/components/office/__tests__/InstancePopupHub.test.tsx"
      provides: "Failing test stub for popup hub"
    - path: "packages/ui/src/components/office/__tests__/HistoryPopup.test.tsx"
      provides: "Failing test stub for history popup modal"
  key_links:
    - from: "packages/ui/package.json"
      to: "@radix-ui/react-dialog"
      via: "pnpm add"
      pattern: "react-dialog"
---

<objective>
Install Radix Dialog and Tabs (prerequisites for popup hub) and create Wave 0 failing test stubs for the two new components that later plans will implement.

Purpose: Front-load dependency installation so all later plans can import from @radix-ui/react-dialog and @radix-ui/react-tabs without blockers. Wave 0 stubs give Plans 03 and 04 a RED baseline to turn GREEN.
Output: Updated package.json with new Radix deps, two test stub files with it.todo() entries.
</objective>

<execution_context>
@/Users/leostuart/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leostuart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-office-map-view-full-navigation-paradigm-shift/14-CONTEXT.md
@.planning/phases/14-office-map-view-full-navigation-paradigm-shift/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @radix-ui/react-dialog and @radix-ui/react-tabs</name>
  <files>packages/ui/package.json</files>
  <action>
Run the following command from the repo root:

```
cd /Users/leostuart/agent-cockpit/packages/ui && pnpm add @radix-ui/react-dialog @radix-ui/react-tabs
```

This installs the two missing Radix packages needed for the SessionPopupHub and HistoryPopupModal components in Plans 03 and 04.

Verify package.json now lists both:
- `"@radix-ui/react-dialog": "^1.x"`
- `"@radix-ui/react-tabs": "^1.x"`

Do NOT install any other packages. The project already has @radix-ui/react-hover-card — same pattern.
  </action>
  <verify>
    <automated>cd /Users/leostuart/agent-cockpit/packages/ui && node -e "const pkg = require('./package.json'); const ok = '@radix-ui/react-dialog' in (pkg.dependencies||{}) && '@radix-ui/react-tabs' in (pkg.dependencies||{}); process.exit(ok ? 0 : 1)"</automated>
  </verify>
  <done>Both @radix-ui/react-dialog and @radix-ui/react-tabs appear in packages/ui/package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 failing test stubs for InstancePopupHub and HistoryPopup</name>
  <files>
    packages/ui/src/components/office/__tests__/InstancePopupHub.test.tsx
    packages/ui/src/components/office/__tests__/HistoryPopup.test.tsx
  </files>
  <action>
Create two test stub files using it.todo() (same Wave 0 pattern used in Phase 04 and 07).

**File 1: `packages/ui/src/components/office/__tests__/InstancePopupHub.test.tsx`**

```typescript
import { describe, it } from 'vitest'

// Wave 0 stubs — RED baseline for Plan 03 implementation
// Component: packages/ui/src/components/office/InstancePopupHub.tsx (does not exist yet)

describe('InstancePopupHub', () => {
  it.todo('renders Dialog overlay when open=true')
  it.todo('does not render when open=false')
  it.todo('renders all 5 tab triggers: Approvals, Timeline, Diff, Memory, Artifacts')
  it.todo('reads selectedSessionId from Zustand store')
  it.todo('shows session name in popup header')
  it.todo('calls onClose when Dialog Close button clicked')
  it.todo('renders ApprovalInbox inside Approvals tab content')
})
```

**File 2: `packages/ui/src/components/office/__tests__/HistoryPopup.test.tsx`**

```typescript
import { describe, it } from 'vitest'

// Wave 0 stubs — RED baseline for Plan 04 implementation
// Component: packages/ui/src/components/office/HistoryPopup.tsx (does not exist yet)

describe('HistoryPopup', () => {
  it.todo('renders nothing when open=false')
  it.todo('renders Dialog overlay when open=true')
  it.todo('renders HistoryPage content inside modal')
  it.todo('calls onClose when dismissed')
})
```

Create the `__tests__` directory if it does not already exist under `packages/ui/src/components/office/`. Check: `ls packages/ui/src/components/office/` — if `__tests__` is absent, create it.
  </action>
  <verify>
    <automated>cd /Users/leostuart/agent-cockpit/packages/ui && pnpm test --run InstancePopupHub HistoryPopup 2>&1 | grep -E "todo|skip|pass|0 failed"</automated>
  </verify>
  <done>
Both test files exist. Running `pnpm test --run` shows the todo entries — no import errors, no failures (todo tests are skipped by vitest).
  </done>
</task>

</tasks>

<verification>
cd /Users/leostuart/agent-cockpit/packages/ui && pnpm test --run 2>&1 | tail -10
</verification>

<success_criteria>
1. @radix-ui/react-dialog and @radix-ui/react-tabs present in packages/ui/package.json
2. Two stub test files exist with it.todo() entries (no failures in test suite)
3. Full test suite still green (pnpm test --run passes)
</success_criteria>

<output>
After completion, create `.planning/phases/14-office-map-view-full-navigation-paradigm-shift/14-01-SUMMARY.md`
</output>
