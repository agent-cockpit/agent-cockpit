---
phase: 16.7-wall-and-object-collision-physics
plan: "02"
subsystem: ui
tags: [collision, physics, game-engine, canvas, typescript]

requires:
  - phase: 16.7-01
    provides: CollisionMap class with loadTerrain/loadObjects/overlaps + PLAYER_HITBOX constant

provides:
  - movePlayer with optional 4th collisionMap param and axis-separated slide logic
  - OfficePage fetches terrain-map.json + objects/manifest.json and passes CollisionMap to movePlayer

affects:
  - 16.7-wall-and-object-collision-physics
  - 16.8-sidebar-design-overhaul

tech-stack:
  added: []
  patterns:
    - Declare collisionMap before engine class definition so update() closure captures it
    - Axis-separated slide: test X and Y axes independently to enable wall-slide movement
    - Async fetch after engine.start() — collision data loads in background, movePlayer gracefully degrades on first frames

key-files:
  created: []
  modified:
    - packages/ui/src/game/PlayerInput.ts
    - packages/ui/src/game/__tests__/PlayerInput.test.ts
    - packages/ui/src/pages/OfficePage.tsx

key-decisions:
  - "collisionMap declared before engine anonymous class so closure in update() can reference it"
  - "Axis-separated slide: X and Y blocked checks use current player position (not newX/newY) for the cross-axis — prevents diagonal stop at corners"
  - "Test geometry uses loadObjects() with synthetic bounding boxes to avoid tile coord transform math in test setup"

patterns-established:
  - "movePlayer optional 4th param: backward compat preserved — undefined collisionMap falls back to unconstrained movement"

requirements-completed:
  - collision-physics

duration: 8min
completed: 2026-04-12
---

# Phase 16.7 Plan 02: Wire Collision Physics Summary

**movePlayer updated with axis-separated slide logic and OfficePage wired to fetch + load CollisionMap from terrain-map.json and objects/manifest.json**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-12T09:10:00Z
- **Completed:** 2026-04-12T09:18:00Z
- **Tasks:** 2 (+ 1 checkpoint awaiting human verify)
- **Files modified:** 3

## Accomplishments
- Updated movePlayer() with optional 4th `collisionMap?: CollisionMap` param — fully backward compatible
- Axis-separated slide block: X and Y movement tested independently against CollisionMap.overlaps()
- Added 6 new TDD tests for collision: X-blocked, Y-blocked, corner-blocked, no-collision, diagonal-slide, no-map-arg
- OfficePage instantiates CollisionMap, async-fetches terrain-map.json + objects/manifest.json, passes to movePlayer

## Task Commits

1. **Task 1: Update movePlayer with optional CollisionMap param + slide logic + new tests** - `201b12f` (feat)
2. **Task 2: Wire CollisionMap initialization in OfficePage** - `dbc100e` (feat)

**Plan metadata:** (pending — created after checkpoint approval)

## Files Created/Modified
- `packages/ui/src/game/PlayerInput.ts` - Added CollisionMap import, PLAYER_HITBOX import, optional 4th param, axis-separated slide block
- `packages/ui/src/game/__tests__/PlayerInput.test.ts` - Added CollisionMap import + 6 new collision tests with correct AABB geometry
- `packages/ui/src/pages/OfficePage.tsx` - CollisionMap import, instantiation before engine, async fetch, movePlayer call updated

## Decisions Made
- `collisionMap` declared before the engine anonymous class so the closure in `update()` captures it correctly
- Test object placement required careful AABB geometry calculation (plan's example coords were incorrect — fixed inline as Rule 1 auto-fix)
- `loadObjects()` type cast via `Parameters<CollisionMap['loadObjects']>[0]` avoids exposing internal `ObjectEntry` type

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed incorrect test object coordinates in collision tests**
- **Found during:** Task 1 (TDD RED phase + GREEN verification)
- **Issue:** Plan's suggested solid object positions (e.g., x=640 for player starting at x=600 moving right) did not actually overlap the player hitbox after movement. The hitbox at newX=720 starts at 736, so an object at x=640 (ending at x=672) is to the left of it.
- **Fix:** Recalculated correct AABB geometry for each test case using: hitbox position = player position + PLAYER_HITBOX.offset, then placed objects to actually intersect that computed hitbox.
- **Files modified:** packages/ui/src/game/__tests__/PlayerInput.test.ts
- **Verification:** All 6 new collision tests pass GREEN after fix
- **Committed in:** 201b12f (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - Bug)
**Impact on plan:** Required to make tests actually verify collision blocking. No scope creep.

## Issues Encountered
- Pre-existing TypeScript errors in setupTests.ts, AgentSprite.tsx, ApprovalInboxPopup.test.tsx — not introduced by this plan, out of scope
- Pre-existing failing tests in ApprovalInbox.test.tsx, approvalsSlice.test.ts, eventsSlice.test.ts — pre-existing, not caused by this work

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- Collision physics wired: player will be blocked by solid terrain + furniture objects once dev server loads the JSON files
- Awaiting human visual verification: walls block, furniture blocks, character sprites passable, wall-slide works
- After approval: plan 16.7-02 complete, ready for Phase 16.8 (Sidebar Design Overhaul)

---
*Phase: 16.7-wall-and-object-collision-physics*
*Completed: 2026-04-12*
