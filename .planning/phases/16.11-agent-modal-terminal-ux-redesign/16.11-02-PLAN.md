---
phase: 16.11-agent-modal-terminal-ux-redesign
plan: 02
type: execute
wave: 1
depends_on:
  - 16.11-01
files_modified:
  - packages/ui/src/index.css
  - packages/ui/src/components/panels/TimelinePanel.tsx
  - packages/ui/src/components/panels/EventCard.tsx
autonomous: true
requirements:
  - modal-terminal-ux
must_haves:
  truths:
    - "All text inside the modal is legible — no black text on dark background"
    - "Timeline tab shows most-recent event at top; scrolling down goes further into the past"
    - "Each event type renders a formatted terminal card, not a raw JSON blob"
    - "rowRefs are keyed by sequenceNumber so jump-to-next/prev still works after reversal"
  artifacts:
    - path: "packages/ui/src/index.css"
      provides: "body { color: var(--color-foreground); background: var(--color-background) } rule"
    - path: "packages/ui/src/components/panels/EventCard.tsx"
      provides: "Per-type terminal chrome EventCard component for all 11 event types"
    - path: "packages/ui/src/components/panels/TimelinePanel.tsx"
      provides: "displayEvents = [...filteredEvents].reverse(); rowRefs keyed by sequenceNumber; delegates to EventCard"
  key_links:
    - from: "TimelinePanel.tsx displayEvents"
      to: "EventCard component"
      via: "isSelected && <EventCard event={event} />"
      pattern: "EventCard"
    - from: "TimelinePanel.tsx rowRefs"
      to: "event.sequenceNumber"
      via: "rowRefs.current.set(seq, el) where seq = event.sequenceNumber ?? idx-N"
      pattern: "sequenceNumber"
---

<objective>
Fix the three UX problems in the Timeline tab: (1) black text on dark background, (2) oldest-first sort, (3) raw JSON event display. Replace InlineDetail with a proper per-type EventCard component using the cockpit terminal design system.

Purpose: Make the agent modal readable and professional — the core UX failures of Phase 16.11.
Output: EventCard.tsx (new), updated TimelinePanel.tsx, index.css body rule.
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16.11-agent-modal-terminal-ux-redesign/16.11-RESEARCH.md
@packages/ui/src/index.css
@packages/ui/src/components/panels/TimelinePanel.tsx
@packages/ui/src/components/office/InstancePopupHub.tsx

<interfaces>
<!-- Key types from @cockpit/shared events.ts that EventCard must handle -->
<!-- Use these directly — no exploration needed -->

NormalizedEvent union type (all handled by EventCard):
  tool_call:          { toolName: string; input: unknown }
  file_change:        { filePath: string; changeType: 'created'|'modified'|'deleted'; diff?: string }
  approval_request:   { proposedAction: string; actionType: string; riskLevel: 'low'|'medium'|'high'|'critical'; whyRisky?: string }
  approval_resolved:  { approvalId: string; decision: 'approve'|'deny'|'always_allow'|'timeout' }
  session_start:      { provider: string; workspacePath: string }
  session_end:        { exitCode: number }
  subagent_spawn:     { subagentSessionId: string }
  subagent_complete:  { success: boolean }
  memory_read:        { memoryKey: string }
  memory_write:       { memoryKey: string; value: string; suggested?: boolean }
  provider_parse_error: { provider: string; errorMessage: string; rawPayload?: unknown }

From TimelinePanel.tsx (current):
  type EventWithSeq = NormalizedEvent & { sequenceNumber?: number }
  rowRefs: useRef<Map<number, HTMLElement>>(new Map())  ← currently keyed by idx
  filteredEvents: NormalizedEvent[]  ← used in render loop
  jumpNext/jumpPrev: use rowRefs.current.get(key)?.scrollIntoView(...)

CSS token map (from index.css @theme):
  --color-foreground:      oklch(0.96 0.01 255)   /* near-white body text */
  --color-muted-foreground: oklch(0.80 0.02 250)  /* secondary text */
  --color-panel-surface:   oklch(0.185 0.025 250) /* card background */
  --color-cockpit-cyan:    oklch(0.75 0.18 195)   /* accent/tool name */
  --color-cockpit-green:   oklch(0.68 0.20 155)   /* success/low risk */
  --color-cockpit-amber:   oklch(0.75 0.16 75)    /* warning/medium risk */
  --color-cockpit-red:     oklch(0.60 0.22 25)    /* danger/high risk */
  --color-cockpit-dim:     oklch(0.35 0.04 220)   /* decorative only - NOT for body text */
  --font-mono-data:        "IBM Plex Mono", monospace
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix global contrast — body color rule in index.css</name>
  <files>packages/ui/src/index.css</files>
  <action>
    Add the following CSS rule immediately after the `:root { }` block in `packages/ui/src/index.css`:

    ```css
    body {
      color: var(--color-foreground);
      background-color: var(--color-background);
    }
    ```

    This ensures all text inside Radix Dialog Portal inherits a near-white color (`oklch(0.96 0.01 255)`) from `<body>` instead of the browser default black. This is the root cause of the black-text-on-dark-background bug.

    Also audit the `InlineDetail` fallback branch in TimelinePanel.tsx (the last `return` in InlineDetail that renders raw `JSON.stringify`). Add `text-[var(--color-muted-foreground)]` class to the `<pre>` element in that fallback — it currently has no text color class and will show black without the body rule as a belt-and-suspenders fix. Make this change while touching TimelinePanel.tsx in Task 2.
  </action>
  <verify>
    <automated>cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui && npx vitest run src/__tests__/TimelinePanel.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>`body { color: var(--color-foreground); background-color: var(--color-background); }` exists in index.css after the :root block. No test regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build EventCard.tsx + update TimelinePanel (reverse + rowRef fix)</name>
  <files>
    packages/ui/src/components/panels/EventCard.tsx
    packages/ui/src/components/panels/TimelinePanel.tsx
  </files>
  <behavior>
    - EventCard for tool_call: shows toolName as cyan mono heading, input as formatted key-value pairs (not full JSON dump), has data-testid="event-card-tool-call"
    - EventCard for file_change: shows filePath prominent, changeType badge (green=created, amber=modified, red=deleted), diff preview if present
    - EventCard for approval_request: shows proposedAction, actionType badge, riskLevel badge color-coded (low=green, medium=amber, high=red, critical=red+pulse), whyRisky if present
    - EventCard for approval_resolved: shows decision badge (approve=green, deny=red, always_allow=amber, timeout=dim)
    - EventCard for session_start: shows provider badge, workspacePath
    - EventCard for session_end: shows exitCode (0=green, nonzero=red)
    - EventCard for subagent_spawn/complete, memory_read/write, provider_parse_error: minimal readable cards with correct accent colors (see RESEARCH.md Pattern 3 table)
    - All EventCard text uses explicit color tokens (never inheriting from parent)
    - Timeline renders newest event first: displayEvents = [...filteredEvents].reverse()
    - rowRefs keyed by sequenceNumber (not array index) so jump scroll still works
  </behavior>
  <action>
    **Step A — Create EventCard.tsx:**

    Create `packages/ui/src/components/panels/EventCard.tsx`. Export a single `EventCard` component receiving `{ event: NormalizedEvent }`.

    Card chrome pattern (terminal aesthetic — apply to all event types):
    ```tsx
    <div className="mx-3 my-1.5 border border-[var(--color-cockpit-dim)]/40 bg-[var(--color-panel-surface)] rounded-none font-[var(--font-mono-data)]">
      {/* Header bar */}
      <div className="flex items-center gap-2 px-3 py-1.5 border-b border-[var(--color-cockpit-dim)]/30">
        <span className="text-[var(--color-cockpit-cyan)] text-[10px] uppercase tracking-wider">
          {eventTypeLabel}
        </span>
        {/* per-type badges go here */}
      </div>
      {/* Body */}
      <div className="px-3 py-2 text-[var(--color-muted-foreground)] text-[11px]">
        {/* per-type content */}
      </div>
    </div>
    ```

    Per-type implementations (from RESEARCH.md Pattern 3):

    `tool_call`: header label "TOOL CALL" cyan, body shows `<div className="text-[var(--color-cockpit-cyan)] font-medium">{event.toolName}</div>` then for each key in `event.input as Record`, render `<div><span className="text-[var(--color-muted-foreground)]">{key}:</span> <span className="text-[var(--color-foreground)]">{String(value)}</span></div>`. Add `data-testid="event-card-tool-call"` on the outer div.

    `file_change`: header shows changeType badge. changeType color: created=cockpit-green, modified=cockpit-amber, deleted=cockpit-red. Body shows filePath in foreground, diff in `<pre>` with muted-foreground.

    `approval_request`: riskLevel badge colors (low=green, medium=amber, high=red, critical=red + `animate-pulse`). Body shows proposedAction, whyRisky.

    `approval_resolved`: decision badge (approve=green, deny=red, always_allow=amber, timeout=dim/muted-foreground).

    `session_start`: provider badge (reuse `.badge-provider-claude` / `.badge-provider-codex` from index.css), workspacePath in muted-foreground.

    `session_end`: exitCode — `0` renders in cockpit-green ("EXIT 0"), non-zero renders in cockpit-red (`EXIT ${code}`).

    `subagent_spawn`: shows truncated `subagentSessionId` (first 8 chars) in muted-foreground.

    `subagent_complete`: success badge (green "DONE" or red "FAILED").

    `memory_read`: shows memoryKey in muted-foreground.

    `memory_write`: shows memoryKey + value preview (max 100 chars). If `suggested`, shows amber "SUGGESTED" badge.

    `provider_parse_error`: header in cockpit-red. Body shows provider + errorMessage in red.

    Fallback (unknown type): `<pre className="text-[var(--color-muted-foreground)] whitespace-pre-wrap">{JSON.stringify(event, null, 2)}</pre>`.

    **Step B — Update TimelinePanel.tsx:**

    1. Import `EventCard` from `../panels/EventCard.js` (or relative path).
    2. Add `const displayEvents = [...filteredEvents].reverse()` after `filteredEvents` derivation (line ~97).
    3. Replace `{filteredEvents.map((event, idx) => {` with `{displayEvents.map((event, idx) => {` in the render loop.
    4. Change rowRef key from `idx` to sequenceNumber:
       - Change `rowRefs` type to `useRef<Map<number | string, HTMLElement>>(new Map())`
       - In the ref callback: `const seqKey = (event as EventWithSeq).sequenceNumber ?? \`idx-${idx}\``
       - `ref={(el) => { if (el) rowRefs.current.set(seqKey, el) }}`
    5. Update `jumpNext` and `jumpPrev` to look up by sequenceNumber:
       - In both functions, when calling `rowRefs.current.get(...)`, use `rowRefs.current.get(next.e.sequenceNumber !== undefined ? next.e.sequenceNumber : next.i)`
       - (jumpTargets still uses `next.i` for index tracking — that stays, but the ref lookup switches to seqNumber when available)
    6. Replace `{isSelected && <InlineDetail event={event} />}` with `{isSelected && <EventCard event={event} />}`.
    7. Add `text-[var(--color-muted-foreground)]` to the fallback `<pre>` in InlineDetail (keep InlineDetail as dead code for now or remove it — removing is cleaner).
    8. The `InlineDetail` function can be removed entirely once EventCard handles all cases.
  </action>
  <verify>
    <automated>cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui && npx vitest run src/__tests__/TimelinePanel.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `EventCard.tsx` exists with all 11 event type branches + fallback
    - `TimelinePanel.tsx` uses `displayEvents` (reversed), rowRefs keyed by sequenceNumber, delegates to `<EventCard>`
    - All 5 previously-RED tests from Plan 01 that target TimelinePanel are now GREEN
    - All pre-existing TimelinePanel tests remain passing
    - `npx vitest run src/__tests__/TimelinePanel.test.tsx` shows 0 failures
  </done>
</task>

</tasks>

<verification>
Full test run after wave completion:
`cd packages/ui && npx vitest run 2>&1 | tail -30`

Expected: all tests green, including the 2 new TimelinePanel RED tests from Plan 01 now passing.
</verification>

<success_criteria>
- index.css has `body { color: var(--color-foreground); }` rule
- EventCard.tsx exists with per-type terminal cards for all 11 event types
- TimelinePanel.tsx renders newest-first with sequenceNumber-keyed rowRefs
- 0 test failures in TimelinePanel.test.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/16.11-agent-modal-terminal-ux-redesign/16.11-02-SUMMARY.md`
</output>
