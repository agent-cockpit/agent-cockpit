---
phase: 16.11-agent-modal-terminal-ux-redesign
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - packages/ui/src/__tests__/TimelinePanel.test.tsx
  - packages/ui/src/components/office/__tests__/InstancePopupHub.test.tsx
autonomous: true
requirements:
  - modal-terminal-ux
must_haves:
  truths:
    - "Failing test exists asserting events render newest-first"
    - "Failing test exists asserting tool_call event shows toolName, not raw JSON blob"
    - "Failing test exists asserting terminal input bar renders in InstancePopupHub"
    - "Failing test exists asserting pressing Enter dispatches sendWsMessage with correct shape"
    - "Failing test exists asserting input is disabled when wsStatus is disconnected"
  artifacts:
    - path: "packages/ui/src/__tests__/TimelinePanel.test.tsx"
      provides: "Extended with 2 RED failing tests for sort order and EventCard rendering"
    - path: "packages/ui/src/components/office/__tests__/InstancePopupHub.test.tsx"
      provides: "Extended with 3 RED failing tests for terminal input bar"
  key_links:
    - from: "TimelinePanel.test.tsx"
      to: "TimelinePanel.tsx displayEvents"
      via: "expect newest event at position 0 in rendered list"
      pattern: "newest-first"
    - from: "InstancePopupHub.test.tsx"
      to: "TerminalInputBar component"
      via: "expect input + SEND button render, onKeyDown Enter fires sendWsMessage"
      pattern: "terminal_input"
---

<objective>
Write RED (failing) test stubs for all behaviors introduced in Phase 16.11. Tests must fail before implementation — they define the contract that Wave 1 plans must satisfy.

Purpose: Prevent scope drift and provide fast feedback during Wave 1 execution.
Output: 5 new failing test cases across 2 existing test files.
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16.11-agent-modal-terminal-ux-redesign/16.11-RESEARCH.md
@packages/ui/src/__tests__/TimelinePanel.test.tsx
@packages/ui/src/components/office/__tests__/InstancePopupHub.test.tsx
@packages/ui/src/components/panels/TimelinePanel.tsx
@packages/ui/src/components/office/InstancePopupHub.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED failing tests to TimelinePanel.test.tsx</name>
  <files>packages/ui/src/__tests__/TimelinePanel.test.tsx</files>
  <behavior>
    - Test: "renders events newest-first" — given events with sequenceNumbers [1, 2, 3], the FIRST rendered row in data-testid="timeline-list" must contain the timestamp of sequenceNumber=3 (newest), not sequenceNumber=1 (oldest). Query all timeline rows and assert row[0] shows the most recent event.
    - Test: "tool_call event card shows toolName not raw JSON" — render a session with a single tool_call event (toolName="Bash", input={command:"ls"}). Click the row to expand it. Assert the expanded detail shows "Bash" as a visible text node and does NOT contain the text "\"toolName\"" (raw JSON key). The card must display the tool name as a human-readable heading, not a JSON blob.
  </behavior>
  <action>
    Add two new `it()` blocks to the existing describe block. Both tests must be RED (failing) on current code — do not implement the feature, only write the expectations.

    For "renders events newest-first": create 3 NormalizedEvent mocks with distinct timestamps (oldest to newest). Render TimelinePanel with those events in the store. Query all rows inside [data-testid="timeline-list"]. Assert the first row contains the timestamp string of the NEWEST event. This will fail because current code renders oldest-first.

    For "tool_call event card shows toolName not raw JSON": create a single tool_call event mock with toolName="Bash" and input={command:"ls -la"}. Render TimelinePanel. Click the row to expand InlineDetail. Assert `screen.getByText('Bash')` is present AND `screen.queryByText(/"toolName"/)` (the raw JSON key) is absent. This test will pass with current code for the "Bash" assertion but will need the EventCard to remove JSON display — mark it as a placeholder for the EventCard's formatted rendering. Alternatively, assert that the rendered detail contains a `data-testid="event-card-tool-call"` element — which does not exist yet, so it fails immediately.

    Use `data-testid="event-card-tool-call"` as the failing assertion for the EventCard test — simpler and guaranteed RED.
  </action>
  <verify>
    <automated>cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui && npx vitest run src/__tests__/TimelinePanel.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Both new tests are present and failing (RED). All previously passing tests in TimelinePanel.test.tsx remain passing. `vitest run` shows 2 new failed test cases with expected assertion errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED failing tests to InstancePopupHub.test.tsx</name>
  <files>packages/ui/src/components/office/__tests__/InstancePopupHub.test.tsx</files>
  <behavior>
    - Test: "renders terminal input bar" — open the modal, assert a `<input>` or element with role="textbox" and placeholder containing "command" is present in the document.
    - Test: "pressing Enter on terminal input calls sendWsMessage with correct shape" — type a command into the input, fire keyDown Enter, assert sendWsMessage mock was called with `{ type: 'terminal_input', sessionId: expect.any(String), text: 'test command' }`.
    - Test: "terminal input is disabled when wsStatus is disconnected" — set wsStatus to 'disconnected' in the store mock, assert the input has the `disabled` attribute.
  </behavior>
  <action>
    Add three new `it()` blocks to InstancePopupHub.test.tsx. All must be RED on current code since TerminalInputBar does not exist yet.

    Mock `sendWsMessage` from `'../../hooks/useSessionEvents.js'` using `vi.mock()` at the top of the file — add this mock if not already present.

    For "renders terminal input bar": open the modal (open=true). Assert `screen.getByPlaceholderText(/command/i)` exists. This will fail because no input is in the current modal.

    For "pressing Enter calls sendWsMessage": find the input, `userEvent.type(input, 'test command')`, fire `{ key: 'Enter' }`, assert `sendWsMessage` was called with `{ type: 'terminal_input', sessionId: expect.any(String), text: 'test command' }`.

    For "disabled when disconnected": set wsStatus='disconnected' in store mock before render, assert input is disabled.

    Store mock pattern: look at existing tests in the file to see how the store is mocked — follow the same `vi.mock('../../store/index.js', ...)` pattern already used.
  </action>
  <verify>
    <automated>cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui && npx vitest run src/components/office/__tests__/InstancePopupHub.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Three new tests are present and failing (RED). All previously passing tests in InstancePopupHub.test.tsx remain passing. `vitest run` shows 3 new failed test cases.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `cd packages/ui && npx vitest run src/__tests__/TimelinePanel.test.tsx src/components/office/__tests__/InstancePopupHub.test.tsx 2>&1 | tail -20`
- Result: 5 new FAILING tests, 0 regressions in previously passing tests
</verification>

<success_criteria>
5 RED (failing) test cases exist across the two test files. Pre-existing tests all still pass. Wave 1 plans have clear contracts to satisfy.
</success_criteria>

<output>
After completion, create `.planning/phases/16.11-agent-modal-terminal-ux-redesign/16.11-01-SUMMARY.md`
</output>
