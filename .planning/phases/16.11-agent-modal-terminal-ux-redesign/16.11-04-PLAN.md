---
phase: 16.11-agent-modal-terminal-ux-redesign
plan: 04
type: execute
wave: 2
depends_on:
  - 16.11-02
  - 16.11-03
files_modified: []
autonomous: false
requirements:
  - modal-terminal-ux
must_haves:
  truths:
    - "All text in the modal is legible at WCAG AA contrast"
    - "Timeline tab shows newest events at top"
    - "Event cards are visually distinct and readable (not raw JSON)"
    - "Terminal input bar has authentic terminal chrome"
    - "Designer (frontend-design skill) has signed off on the visual design"
  artifacts: []
  key_links: []
---

<objective>
Visual QA checkpoint — verify the redesigned modal looks correct in the browser and get designer sign-off via the frontend-design skill before the phase is considered complete.

Purpose: Aesthetic and contrast quality cannot be verified by automated tests. Human + designer review is required.
Output: Sign-off (or issues to fix) on contrast, spacing, event card design, and terminal chrome.
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.11-agent-modal-terminal-ux-redesign/16.11-02-SUMMARY.md
@.planning/phases/16.11-agent-modal-terminal-ux-redesign/16.11-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm green baseline</name>
  <files></files>
  <action>
    Before invoking the visual QA checkpoint, confirm the automated baseline is fully green:

    ```bash
    cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui && npx vitest run 2>&1 | tail -30
    ```

    If any tests fail, diagnose and fix before proceeding to the checkpoint. Do not proceed to the visual QA if automated tests are red — fix the regression first.
  </action>
  <verify>
    <automated>cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui && npx vitest run 2>&1 | tail -30</automated>
  </verify>
  <done>All tests pass. 0 failures in the full UI test suite.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual QA — frontend-design skill sign-off</name>
  <what-built>
    The agent modal (InstancePopupHub) has been fully redesigned:
    1. Global body color fix — no more black text on dark background
    2. Timeline tab reversed — newest events appear at top
    3. Per-type EventCard components — beautiful terminal-chrome cards for all 11 event types
    4. Terminal input bar — ">_" prompt, command input, SEND button, WS connection indicator
  </what-built>
  <how-to-verify>
    **Step 1 — Run the design QA skill:**
    Call `Skill(skill="frontend-design:frontend-design")` to invoke the designer review of the modal. The skill will evaluate:
    - Contrast: All text passes WCAG AA (4.5:1 ratio minimum)
    - Typography: Mono font usage, label hierarchy, spacing
    - Event cards: Visual distinction between event types, readability of tool names / file paths / risk levels
    - Terminal chrome: Input bar aesthetic, prompt glyph, connection indicator
    - Overall: Does the modal feel like a professional space-theme terminal interface?

    **Step 2 — Manual browser verification:**
    1. Start the dev server: `npm run dev` in packages/ui
    2. Open http://localhost:5173 in the browser
    3. Click any agent on the map to open the InstancePopupHub modal
    4. Navigate to the Timeline tab
    5. Verify: Events appear newest-first (check timestamps — latest event is at top)
    6. Click any event row to expand — verify the card shows formatted content (NOT raw JSON blob)
    7. Verify all text is clearly legible (no black text on dark background)
    8. Scroll to the bottom of the modal — verify terminal input bar is visible with ">_" prompt
    9. Check input bar: connection status dot visible, placeholder "enter command..." shown
    10. Type a command and press Enter — verify input clears (command sent to daemon WS)
    11. Open browser DevTools → Console — confirm "[cockpit-daemon] terminal_input for session..." log appears

    **Step 3 — Accessibility quick check:**
    Open browser DevTools → Accessibility tree. Confirm the input has a proper accessible role.
    Use DevTools color picker to spot-check at least 2 text elements — confirm contrast ratios are > 4.5:1.
  </how-to-verify>
  <resume-signal>
    Type "approved" if the visual QA passes.
    Type "issues: [description]" to list any visual problems that need fixing — executor will address them and return.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 16.11 is complete when:
1. Full test suite is green
2. Visual QA checkpoint approved (either by user or after fixing reported issues)
3. Designer sign-off recorded in SUMMARY.md
</verification>

<success_criteria>
- All 5 automated tests from Plan 01 are GREEN
- full `npx vitest run` passes with 0 failures
- Visual QA checkpoint approved
- Modal text is legible in browser at WCAG AA contrast
- Timeline newest-first verified visually
- Terminal input bar functional
</success_criteria>

<output>
After completion, create `.planning/phases/16.11-agent-modal-terminal-ux-redesign/16.11-04-SUMMARY.md`
</output>
