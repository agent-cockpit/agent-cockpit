---
phase: 23
plan: 01
type: execute
wave: 1
depends_on:
  - 22-01
files_modified:
  - packages/daemon/src/adapters/claude/hookParser.ts
  - packages/daemon/src/adapters/claude/hookServer.ts
  - packages/daemon/src/adapters/codex/codexAdapter.ts
  - packages/daemon/src/approvals/approvalQueue.ts
  - packages/daemon/src/ws/handlers.ts
  - packages/daemon/src/__tests__/hookParser.test.ts
  - packages/daemon/src/__tests__/hook-server.test.ts
  - packages/daemon/src/__tests__/approval-queue.test.ts
  - packages/daemon/src/adapters/codex/__tests__/codexAdapter.test.ts
  - packages/ui/src/store/approvalsSlice.ts
  - packages/ui/src/__tests__/approvalsSlice.test.ts
requirements:
  - APPR-HOOK-01
  - APPR-HOOK-02
  - APPR-HOOK-03
autonomous: true
must_haves:
  truths:
    - "Each approval_request resolves exactly once into approved/denied/always_allow/timeout"
    - "Claude and Codex both emit persistent approval_resolved events for terminal decisions"
    - "Subagent lifecycle events remain intact under approval-heavy flows"
  artifacts:
    - path: "packages/daemon/src/approvals/approvalQueue.ts"
      provides: "Queue-level idempotent approval resolution"
      contains: "approval_resolved"
    - path: "packages/daemon/src/adapters/codex/codexAdapter.ts"
      provides: "Codex approval cleanup and resolver safety"
      contains: "pendingCodexApprovals"
  key_links:
    - from: "packages/daemon/src/ws/handlers.ts"
      to: "packages/daemon/src/approvals/approvalQueue.ts"
      via: "approval_decision"
      pattern: "approvalQueue.decide"
    - from: "packages/ui/src/store/approvalsSlice.ts"
      to: "packages/shared/src/events.ts"
      via: "approval_request + approval_resolved replay"
      pattern: "approval_resolved"
---

<objective>
Fix approval reliability regressions by enforcing deterministic approval handling across Claude and Codex, including timeout, duplicate-decision, and subagent-adjacent event flows.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-approval-hook-reliability-across-providers-and-subagents/23-RESEARCH.md
@.planning/phases/23-approval-hook-reliability-across-providers-and-subagents/23-VALIDATION.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build approval reliability matrix and lock failing cases</name>
  <files>packages/daemon/src/__tests__/hookParser.test.ts, packages/daemon/src/__tests__/hook-server.test.ts, packages/daemon/src/__tests__/approval-queue.test.ts, packages/daemon/src/adapters/codex/__tests__/codexAdapter.test.ts</files>
  <action>Codify red/green coverage for PreToolUse, PermissionRequest, Codex requestApproval, timeout race, and duplicate-decision race.</action>
  <verify><automated>pnpm --filter @cockpit/daemon test --run hookParser && pnpm --filter @cockpit/daemon test --run hook-server</automated></verify>
  <done>Matrix covers all critical provider approval paths and initially reproduces failing behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden provider and queue resolution semantics</name>
  <files>packages/daemon/src/adapters/claude/hookServer.ts, packages/daemon/src/adapters/codex/codexAdapter.ts, packages/daemon/src/approvals/approvalQueue.ts, packages/daemon/src/ws/handlers.ts</files>
  <action>Enforce exactly-once resolution claims, safe timeout/manual ordering, and deterministic provider dispatch no-op behavior after terminal resolution.</action>
  <verify><automated>pnpm --filter @cockpit/daemon test --run approval-queue && pnpm --filter @cockpit/daemon test --run codexAdapter</automated></verify>
  <done>All terminal decisions are persisted once and provider dispatch cannot double-apply.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ensure UI replay convergence for approval state</name>
  <files>packages/ui/src/store/approvalsSlice.ts, packages/ui/src/__tests__/approvalsSlice.test.ts</files>
  <action>Guarantee replay/catch-up and late approval_resolved events converge to correct pending queue without stale cards.</action>
  <verify><automated>pnpm --filter @cockpit/ui test --run approvalsSlice</automated></verify>
  <done>UI approval state is stable under reconnect/replay ordering variations.</done>
</task>
</tasks>

<verification>
pnpm --filter @cockpit/daemon test --run hookParser && pnpm --filter @cockpit/daemon test --run hook-server && pnpm --filter @cockpit/daemon test --run approval-queue && pnpm --filter @cockpit/daemon test --run codexAdapter && pnpm --filter @cockpit/ui test --run approvalsSlice
</verification>

<success_criteria>
All provider approval flows resolve deterministically once, emit replay-safe events, and preserve subagent-related event integrity.
</success_criteria>

<output>
After completion, create `.planning/phases/23-approval-hook-reliability-across-providers-and-subagents/23-01-SUMMARY.md`.
</output>
