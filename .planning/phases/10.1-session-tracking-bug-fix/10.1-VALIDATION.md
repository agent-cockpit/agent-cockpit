---
phase: 10.1
slug: session-tracking-bug-fix
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-08
---

# Phase 10.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest ^3.0.0 |
| **Config file** | packages/daemon/vitest.config.ts |
| **Quick run command** | `cd packages/daemon && pnpm test -- --run hookParser.test.ts` |
| **Full suite command** | `cd packages/daemon && pnpm test -- --run` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd packages/daemon && pnpm test -- --run hookParser.test.ts`
- **After every plan wave:** Run `cd packages/daemon && pnpm test -- --run`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 10.1-01-01 | 01 | 1 | SESS-01 | unit | `pnpm test -- --run hookParser.test.ts` | ❌ W0 | ⬜ pending |
| 10.1-01-02 | 01 | 1 | SESS-01 | integration | `pnpm test -- --run database.test.ts` | ❌ W0 | ⬜ pending |
| 10.1-01-03 | 01 | 1 | SESS-01 | integration | `pnpm test -- --run hookParser.test.ts` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `packages/daemon/src/__tests__/hookParser.test.ts` — tests for session mapping persistence (extend existing file)
- [ ] `packages/daemon/src/__tests__/database.test.ts` — tests for `claude_sessions` table creation and cache population (extend existing file)
- [ ] No framework setup needed — vitest ^3.0.0 already configured and used in existing tests

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Sessions appear in UI after daemon restart | SESS-01 | Requires full daemon lifecycle + UI + Claude Code session | 1. Start Claude Code session with hooks configured. 2. Verify session appears in UI. 3. Stop daemon. 4. Start daemon. 5. Verify same session still appears in UI with same UUID. |
| Same Claude session_id maps to same UUID across restarts | SESS-01 | Requires daemon restart | 1. Note session UUID for a running Claude session. 2. Stop daemon. 3. Start daemon. 4. Send a hook event from same Claude session. 5. Verify UUID matches original. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
