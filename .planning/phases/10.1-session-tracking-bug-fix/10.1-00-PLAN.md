---
phase: 10.1
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/daemon/src/__tests__/hookParser.test.ts
  - packages/daemon/src/__tests__/database.test.ts
autonomous: true
requirements:
  - SESS-01
user_setup: []

must_haves:
  truths:
    - "Test files exist with failing stubs for session mapping persistence"
    - "Test framework (vitest) can run the test files"
    - "All verify commands in subsequent plans have test files to execute"
  artifacts:
    - path: "packages/daemon/src/__tests__/hookParser.test.ts"
      provides: "Failing test stubs for session mapping persistence"
      contains: "describe('parseHookPayload session mapping')"
    - path: "packages/daemon/src/__tests__/database.test.ts"
      provides: "Failing test stubs for claude_sessions table and cache"
      contains: "describe('claude_sessions')"
  key_links:
    - from: "10.1-00-PLAN.md"
      to: "10.1-01-PLAN.md"
      via: "Wave 0 must complete before implementation can begin"
      pattern: "pnpm test.*hookParser.test.ts|database.test.ts"
---

<objective>
Create failing test stubs for session mapping persistence before any implementation work begins.

Purpose: Follows TDD methodology — tests must exist first to drive implementation. This Wave 0 plan ensures all verify commands in subsequent plans have test files to execute, preventing the "MISSING — Wave 0 must create" errors in validation.

Output: Two test files (hookParser.test.ts, database.test.ts) with failing test stubs that describe the expected behavior of session mapping persistence.
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-session-tracking-bug-fix/10.1-RESEARCH.md
@.planning/phases/10.1-session-tracking-bug-fix/10.1-VALIDATION.md

<!-- Test patterns from existing database.test.ts -->

Existing test pattern (from database.test.ts):
```typescript
import { describe, it, expect } from 'vitest';
import { openDatabase } from '../db/database.js';

function makeEvent(overrides: Partial<NormalizedEvent> = {}): NormalizedEvent {
  return {
    schemaVersion: 1,
    sessionId: '123e4567-e89b-12d3-a456-426614174000',
    timestamp: new Date().toISOString(),
    type: 'session_start',
    provider: 'claude',
    workspacePath: '/home/user/project',
    ...overrides,
  } as NormalizedEvent;
}

describe('openDatabase', () => {
  it('creates the events table', () => {
    const db = openDatabase(':memory:');
    const row = db.prepare(
      "SELECT name FROM sqlite_master WHERE type='table' AND name='events'"
    ).get();
    expect(row).toBeDefined();
    db.close();
  });
});
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hookParser.test.ts with failing test stubs for session mapping</name>
  <files>packages/daemon/src/__tests__/hookParser.test.ts</files>
  <action>
    Create packages/daemon/src/__tests__/hookParser.test.ts with the following failing test stubs:

    1. Import vitest functions: describe, it, expect
    2. Import parseHookPayload from '../adapters/claude/hookParser.js' (will fail initially as signature changes)
    3. Import openDatabase from '../db/database.js' (will fail until db parameter is added)
    4. Create test suite: describe('parseHookPayload session mapping persistence')

    Test stubs (all will fail initially):
    - Test: Same claude_id maps to same UUID across multiple parseHookPayload calls (cache hit)
      * Create a SessionStart payload with claude_id='test-session-1'
      * Call parseHookPayload twice with same payload
      * Expect sessionId to be identical

    - Test: After clearing cache, claude_id still maps to same UUID (database hit)
      * Create SessionStart payload, get sessionId
      * Simulate cache clear (will need to add cache clearing helper later)
      * Call parseHookPayload again with same claude_id
      * Expect sessionId to match original (from database)

    - Test: New claude_id generates new UUID and persists to database
      * Create SessionStart payload with new claude_id='test-session-new'
      * Call parseHookPayload, capture sessionId
      * Verify UUID format (v4)
      * Query database to verify row exists in claude_sessions table

    - Test: Subagent sessions use workspace parameter correctly
      * Create SubagentStart payload with workspace='/test/workspace'
      * Call parseHookPayload, capture sessionId
      * Verify claude_sessions row has correct workspace

    5. Use expect().toBe() and expect().toMatch() for assertions
    6. Tests should FAIL because:
       - hookParser signature doesn't accept db/cache parameters yet
       - claude_sessions table doesn't exist yet
       - setClaudeSessionCache() function doesn't exist yet
  </action>
  <verify>
    <automated>cd packages/daemon && pnpm test -- --run hookParser.test.ts</automated>
  </verify>
  <done>
    - hookParser.test.ts file exists
    - 4 test stubs defined (cache hit, database hit, new session, subagent)
    - Running tests produces expected failures (tests exist but implementation doesn't)
    - Test file can be executed by vitest (no syntax errors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend database.test.ts with failing test stubs for claude_sessions</name>
  <files>packages/daemon/src/__tests__/database.test.ts</files>
  <action>
    Extend existing packages/daemon/src/__tests__/database.test.ts with new test suite:

    1. Add new describe block at end of file: describe('claude_sessions table and cache')

    Test stubs (all will fail initially):
    - Test: claude_sessions table is created with correct schema
      * Call openDatabase(':memory:')
      * Query sqlite_master for table 'claude_sessions'
      * Expect row to exist
      * Query table schema to verify columns: session_id (PK), claude_id, workspace, created_at

    - Test: idx_claude_sessions_claude_id index exists
      * Call openDatabase(':memory:')
      * Query sqlite_master for index 'idx_claude_sessions_claude_id'
      * Expect index row to exist

    - Test: initializeClaudeSessionCache() returns Map populated from database rows
      * Call openDatabase(':memory:')
      * Manually insert test row into claude_sessions (will fail until table exists)
      * Call initializeClaudeSessionCache(db) (will fail until function exists)
      * Expect returned Map to have correct key-value mapping

    - Test: initializeClaudeSessionCache() returns empty Map when table is empty
      * Call openDatabase(':memory:')
      * Call initializeClaudeSessionCache(db)
      * Expect returned Map to be empty (size 0)

    - Test: getClaudeSessionId() returns existing mapping or null
      * Call openDatabase(':memory:')
      * Insert test mapping via setClaudeSessionId() (will fail until function exists)
      * Call getClaudeSessionId(db, claudeId)
      * Expect correct session_id returned

    - Test: setClaudeSessionId() inserts new mapping and INSERT OR IGNORE prevents duplicates
      * Call openDatabase(':memory:')
      * Call setClaudeSessionId() with test data
      * Verify row exists in claude_sessions table
      * Call setClaudeSessionId() again with same claude_id
      * Verify only 1 row exists (INSERT OR IGNORE worked)

    2. Use existing makeEvent() pattern if needed for test data
    3. Tests should FAIL because:
       - initializeClaudeSessionCache() function doesn't exist yet
       - getClaudeSessionId() function doesn't exist yet
       - setClaudeSessionId() function doesn't exist yet
       - claude_sessions table doesn't exist yet
  </action>
  <verify>
    <automated>cd packages/daemon && pnpm test -- --run database.test.ts</automated>
  </verify>
  <done>
    - database.test.ts extended with new test suite
    - 6 test stubs defined (schema, index, cache pop, empty cache, getClaudeSessionId, setClaudeSessionId)
    - Running tests produces expected failures (tests exist but implementation doesn't)
    - Test file can be executed by vitest (no syntax errors)
  </done>
</task>

</tasks>

<verification>
1. Run hookParser tests: `cd packages/daemon && pnpm test -- --run hookParser.test.ts`
   - Expect 4 tests to fail (implementation doesn't exist yet)
   - Confirm no syntax errors or import failures unrelated to implementation
2. Run database tests: `cd packages/daemon && pnpm test -- --run database.test.ts`
   - Expect 6 new tests to fail (functions don't exist yet)
   - Existing tests should still pass (no regressions)
3. Verify test files are on disk: `ls packages/daemon/src/__tests__/hookParser.test.ts packages/daemon/src/__tests__/database.test.ts`
4. Confirm Wave 0 requirements met: All verify commands in 10.1-01-PLAN.md have test files to execute
</verification>

<success_criteria>
- hookParser.test.ts created with 4 failing test stubs
- database.test.ts extended with 6 failing test stubs
- All tests can be executed by vitest (no syntax errors)
- Tests fail for the right reasons (missing implementation, not import/syntax errors)
- Wave 0 validation requirements satisfied: no "MISSING — Wave 0 must create" errors
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-session-tracking-bug-fix/10.1-00-SUMMARY.md`
</output>
