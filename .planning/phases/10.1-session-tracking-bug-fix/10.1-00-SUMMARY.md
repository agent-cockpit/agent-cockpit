---
phase: 10.1
plan: 00
subsystem: session-tracking
tags: [wave-0, tdd, test-stubs]
dependency_graph:
  requires: []
  provides: [test-infrastructure]
  affects: []
tech_stack:
  added: []
  patterns: [tdd-red-phase]
key_files:
  created:
    - packages/daemon/src/__tests__/hookParser.test.ts
  modified:
    - packages/daemon/src/__tests__/database.test.ts
decisions: []
metrics:
  duration: 2min
  completed_date: 2026-04-08
---

# Phase 10.1 Plan 00: Test Stubs for Session Mapping Persistence Summary

Created failing test stubs for session mapping persistence following TDD methodology. This Wave 0 plan establishes the test infrastructure that drives implementation in subsequent plans.

## One-liner
Test stubs created for session mapping persistence with 10 failing tests to drive TDD implementation.

## Deviations from Plan

None - plan executed exactly as written.

## Test Stubs Created

### hookParser.test.ts (4 tests)
- Test: Same claude_id maps to same UUID across multiple parseHookPayload calls (cache hit)
- Test: After clearing cache, claude_id still maps to same UUID (database hit)
- Test: New claude_id generates new UUID and persists to database
- Test: Subagent sessions use workspace parameter correctly

### database.test.ts (6 tests)
- Test: claude_sessions table is created with correct schema
- Test: idx_claude_sessions_claude_id index exists
- Test: initializeClaudeSessionCache() returns Map populated from database rows
- Test: initializeClaudeSessionCache() returns empty Map when table is empty
- Test: getClaudeSessionId() returns existing mapping or null
- Test: setClaudeSessionId() inserts new mapping and INSERT OR IGNORE prevents duplicates

## Test Results

**Wave 0 Status:** RED (as expected)
- 4 hookParser tests: 2 passing, 2 failing (missing claude_sessions table)
- 6 database tests: 6 failing (missing table and functions)
- All existing tests: passing (no regressions)

**Failure Reasons:**
- `no such table: claude_sessions` - Table will be created in Plan 01
- `Cannot find module '../db/database.js'` - Function exports will be added in Plan 01
- `Cannot find module '../db/queries.js'` - Query functions will be added in Plan 01

## Files Modified

- **packages/daemon/src/__tests__/hookParser.test.ts** (new, 103 lines)
- **packages/daemon/src/__tests__/database.test.ts** (extended, +121 lines)

## Next Steps

Plan 10.1-01 will implement the database layer:
- Create claude_sessions table and index
- Implement initializeClaudeSessionCache() function
- Implement getClaudeSessionId() and setClaudeSessionId() query functions

## Self-Check: PASSED

- [x] hookParser.test.ts created with 4 failing test stubs
- [x] database.test.ts extended with 6 failing test stubs
- [x] All tests can be executed by vitest (no syntax errors)
- [x] Tests fail for the right reasons (missing implementation, not import/syntax errors)
- [x] Wave 0 validation requirements satisfied: no "MISSING — Wave 0 must create" errors
