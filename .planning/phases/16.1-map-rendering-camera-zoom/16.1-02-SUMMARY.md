---
phase: 16.1-map-rendering-camera-zoom
plan: "02"
subsystem: ui
tags: [canvas, tilemap, offscreen-canvas, wang-tiles, game-engine, vitest]

# Dependency graph
requires:
  - phase: 16.1-01
    provides: Wave 0 TilemapRenderer test stubs (RED), map assets at /map/* URLs, CameraState.zoom field
  - phase: 15-game-engine-foundation
    provides: Camera.ts, GameState.ts, OffscreenCanvas stub in setupTests.ts
provides:
  - TilemapRenderer class with load(), blit(), ready property
  - tileToPixel(tx,ty) exported pure function
  - wangIndexFromEdges(edges) exported pure function
  - OffscreenCanvas pre-renderer for 3232×3232 world
affects:
  - 16.1-03: OfficePage zoom integration (will call tilemapRenderer.blit() in render loop)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "OffscreenCanvas pre-render: draw world once at load, blit single drawImage per frame"
    - "Wang tile 4-bit bitmask: N=bit0, E=bit1, S=bit2, W=bit3 → 16 tileset grid positions"
    - "Graceful degradation: missing PNGs → #9ca3af color fill (no crash)"
    - "Tile-to-pixel offset: (tx - TILE_ORIGIN_X) * TILE_SIZE where TILE_ORIGIN_X=-46"

key-files:
  created:
    - packages/ui/src/game/TilemapRenderer.ts
  modified: []

key-decisions:
  - "Objects use boundingBox.x/y directly in pixel world-space — NO tile offset applied (boundingBox is already in world pixels)"
  - "Overlay uses worldCoordinates.x/y directly from map.json — NO tile offset"
  - "tilesetId for transition cells: pick first present edge (north ?? east ?? south ?? west)"
  - "blit() calls ctx.drawImage(offscreen, -camX, -camY) inside caller's scale(zoom,zoom) context"
  - "terrainId=0 cells skipped via continue — these are empty/no-terrain tiles"

patterns-established:
  - "TDD Wave 0 pattern: tests written in RED (Plan 01) → implementation in GREEN (Plan 02)"
  - "OffscreenCanvas rendering: create once in load(), expose read-only via blit()"

requirements-completed: [MAP-RENDER]

# Metrics
duration: 4min
completed: "2026-04-11"
---

# Phase 16.1 Plan 02: TilemapRenderer OffscreenCanvas Pre-renderer Summary

**OffscreenCanvas pre-renderer that draws 3232×3232 world once (terrain Wang tiles + overlay + layer-sorted objects) and exposes blit(ctx, camX, camY) for zero-overhead game loop rendering**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-04-11T23:51:22Z
- **Completed:** 2026-04-11T23:55:00Z
- **Tasks:** 1 (TDD: RED already existed from Plan 01; GREEN + verify)
- **Files modified:** 1 (created TilemapRenderer.ts)

## Accomplishments
- TilemapRenderer.ts created (211 lines) with all required exports
- All 10 TilemapRenderer unit tests pass GREEN (3 tileToPixel, 6 wangIndexFromEdges, 1 ready state)
- Full test suite (67 tests across all game modules) passes with zero regressions
- Pre-existing TypeScript errors in unrelated files confirmed pre-existing (not caused by this plan)

## Task Commits

Each task was committed atomically:

1. **Task 1: Implement TilemapRenderer.ts** - `5f52737` (feat)

## Files Created/Modified
- `packages/ui/src/game/TilemapRenderer.ts` — OffscreenCanvas pre-renderer with tileToPixel, wangIndexFromEdges exports and TilemapRenderer class

## Decisions Made
- Objects and overlays use their pixel world-space coordinates directly from JSON (no tile offset) — boundingBox and worldCoordinates are already in pixel space
- First present edge determines the tilesetId for transition cells (north ?? east ?? south ?? west)
- blit() uses `ctx.drawImage(offscreen as unknown as HTMLCanvasElement, -camX, -camY)` to bridge OffscreenCanvas type with CanvasRenderingContext2D.drawImage signature in test environment

## Deviations from Plan

None - plan executed exactly as written. Implementation is a direct transcription of the plan's `<action>` code block.

## Issues Encountered
- Pre-existing TypeScript errors in ApprovalInboxPopup.test.tsx (TS2304 missing beforeEach) and setupTests.ts (TS2322 OffscreenCanvas stub type) were present before this plan and are out of scope. Confirmed via git stash verification.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 03 (OfficePage zoom integration) can now integrate TilemapRenderer: instantiate at startup, call load() once, call blit() each frame inside scale(2,2) block
- All exported API surface is tested: TilemapRenderer class, tileToPixel(), wangIndexFromEdges()
- blit() no-op before ready=true means safe to wire up before load() completes

---
*Phase: 16.1-map-rendering-camera-zoom*
*Completed: 2026-04-11*
