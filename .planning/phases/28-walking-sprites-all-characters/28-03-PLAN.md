---
phase: 28-walking-sprites-all-characters
plan: "03"
type: execute
wave: 2
depends_on:
  - "28-01"
  - "28-02"
files_modified:
  - packages/ui/public/sprites/robot-sheet.png
  - packages/ui/public/sprites/robot-manifest.json
  - packages/ui/public/sprites/alien-sheet.png
  - packages/ui/public/sprites/alien-manifest.json
  - packages/ui/public/sprites/hologram-sheet.png
  - packages/ui/public/sprites/hologram-manifest.json
  - packages/ui/public/sprites/monkey-sheet.png
  - packages/ui/public/sprites/monkey-manifest.json
  - packages/ui/public/sprites/caveman-sheet.png
  - packages/ui/public/sprites/caveman-manifest.json
  - packages/ui/public/sprites/ghost-sheet.png
  - packages/ui/public/sprites/ghost-manifest.json
  - packages/ui/public/sprites/ninja-sheet.png
  - packages/ui/public/sprites/ninja-manifest.json
  - packages/ui/public/sprites/pirate-sheet.png
  - packages/ui/public/sprites/pirate-manifest.json
  - packages/ui/public/sprites/medicine-woman-sheet.png
  - packages/ui/public/sprites/medicine-woman-manifest.json
autonomous: false
requirements:
  - all-character-walk-sprites

must_haves:
  truths:
    - "Each of 9 non-astronaut character sprite sheets is 576×2560 pixels (40 rows)"
    - "Rows 32-39 of each character sheet contain that character's walk animation (8 directions × 8 frames)"
    - "Manifest JSON for each character has `states.walk = 8`"
    - "build-spritesheet.ts runs identically for all 10 characters with no per-character branching"
    - "In-game: selecting any character and moving shows a proper walk animation, not fight-stance"
  artifacts:
    - path: "packages/ui/public/sprites/robot-sheet.png"
      provides: "Robot sprite sheet with walk rows appended"
      contains: "576x2560"
    - path: "packages/ui/public/sprites/alien-sheet.png"
      provides: "Alien sprite sheet with walk rows appended"
      contains: "576x2560"
    - path: "packages/ui/public/sprites/hologram-sheet.png"
      provides: "Hologram sprite sheet with walk rows appended"
    - path: "packages/ui/public/sprites/monkey-sheet.png"
      provides: "Monkey sprite sheet with walk rows appended"
    - path: "packages/ui/public/sprites/caveman-sheet.png"
      provides: "Caveman sprite sheet with walk rows appended"
    - path: "packages/ui/public/sprites/ghost-sheet.png"
      provides: "Ghost sprite sheet with walk rows appended"
    - path: "packages/ui/public/sprites/ninja-sheet.png"
      provides: "Ninja sprite sheet with walk rows appended"
    - path: "packages/ui/public/sprites/pirate-sheet.png"
      provides: "Pirate sprite sheet with walk rows appended"
    - path: "packages/ui/public/sprites/medicine-woman-sheet.png"
      provides: "Medicine-woman sprite sheet with walk rows appended"
  key_links:
    - from: "assets/raw/{character}/walk/{direction}/frame-N.png"
      to: "packages/ui/public/sprites/{character}-sheet.png rows 32-39"
      via: "scripts/build-spritesheet.ts"
      pattern: "buildSheet\\(character\\)"
    - from: "OfficePage.tsx player sprite render"
      to: "STATE_ROW_OFFSET.walk (= 32) in spriteStates.ts"
      via: "existing wiring from Phase 16.4-02"
      pattern: "STATE_ROW_OFFSET\\.walk"
---

<objective>
Rebuild all 9 non-astronaut sprite sheets via the existing `scripts/build-spritesheet.ts` pipeline so each one gains walk rows at offset 32-39. Verify each sheet's new dimensions (576×2560), confirm the manifest reports `walk: 8`, and run an in-game human-verify checkpoint where the tester selects each of the 10 characters and confirms the player walks smoothly on-screen.

Purpose: This is the final wiring step. The raw PNGs generated in 28-01 and 28-02 are inert until assembled into sprite sheets that `OfficePage.tsx` loads. After this plan, character selection (Phase 27) fully works — choosing any character produces correct walk animation, not the fight-stance fallback.

Output: 9 rebuilt sprite sheets, 9 updated manifest files, human approval.
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.4-astronaut-walking-sprite-generation/16.4-02-SUMMARY.md
@.planning/phases/28-walking-sprites-all-characters/28-01-PLAN.md
@.planning/phases/28-walking-sprites-all-characters/28-02-PLAN.md
@scripts/build-spritesheet.ts
@packages/ui/src/components/office/spriteStates.ts

<interfaces>
<!-- build-spritesheet.ts — the pipeline is ALREADY wired for walk state -->
<!-- Already present in scripts/build-spritesheet.ts: -->
<!--   const ANIM_STATES = ['idle', 'blocked', 'completed', 'failed', 'walk'] as const -->
<!-- Already present in packages/ui/src/components/office/spriteStates.ts: -->
<!--   STATE_ROW_OFFSET.walk = 32 -->
<!--   AnimationState union includes 'walk' -->
<!-- OfficePage.tsx ALREADY references STATE_ROW_OFFSET.walk (fixed in Phase 16.4-02) -->

CLI:
  npx tsx scripts/build-spritesheet.ts <character>
  → Reads assets/raw/<character>/{idle,blocked,completed,failed,walk}/<direction>/frame-*.png
  → Writes packages/ui/public/sprites/<character>-sheet.png (576×2560 when all 5 states present)
  → Writes packages/ui/public/sprites/<character>-manifest.json

Expected manifest shape (from Phase 16.4-02 astronaut reference):
{
  "states": { "idle": 4, "blocked": 8, "completed": 9, "failed": 7, "walk": 8 },
  "frameSize": 64,
  "directions": 8
}
</interfaces>
</context>

<threat_model>
- **Accidental overwrite of astronaut sheet:** Do NOT rebuild astronaut — Phase 16.4-02 already produced the correct astronaut-sheet.png. Running `build-spritesheet.ts astronaut` would be idempotent but wastes time. Skip astronaut in this plan.
- **Path traversal:** Character list is hard-coded to the 9 non-astronaut character names. Do not accept arbitrary input.
- **Broken sheet dimensions breaking the live game:** If any character's walk frames are missing (gap between 28-01/28-02 completion and this run), the sheet will build with 0 walk frames and `buildRow` will pad — producing an undersized 32-row sheet or broken row 32-39. Verify input frames exist BEFORE running build; fail loudly if any direction is missing.
- **Manifest/sheet desync:** If build-spritesheet.ts fails mid-run, a stale sheet could coexist with a new manifest. Run each character in sequence and verify both outputs per character.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rebuild all 9 non-astronaut sprite sheets via build-spritesheet.ts</name>
  <files>
    packages/ui/public/sprites/robot-sheet.png, packages/ui/public/sprites/robot-manifest.json,
    packages/ui/public/sprites/alien-sheet.png, packages/ui/public/sprites/alien-manifest.json,
    packages/ui/public/sprites/hologram-sheet.png, packages/ui/public/sprites/hologram-manifest.json,
    packages/ui/public/sprites/monkey-sheet.png, packages/ui/public/sprites/monkey-manifest.json,
    packages/ui/public/sprites/caveman-sheet.png, packages/ui/public/sprites/caveman-manifest.json,
    packages/ui/public/sprites/ghost-sheet.png, packages/ui/public/sprites/ghost-manifest.json,
    packages/ui/public/sprites/ninja-sheet.png, packages/ui/public/sprites/ninja-manifest.json,
    packages/ui/public/sprites/pirate-sheet.png, packages/ui/public/sprites/pirate-manifest.json,
    packages/ui/public/sprites/medicine-woman-sheet.png, packages/ui/public/sprites/medicine-woman-manifest.json
  </files>

  <read_first>
    - scripts/build-spritesheet.ts (confirm 'walk' is already in ANIM_STATES — it IS as of Phase 16.4-02, no code change needed)
    - packages/ui/src/components/office/spriteStates.ts (confirm STATE_ROW_OFFSET.walk = 32 — already present)
    - .planning/phases/16.4-astronaut-walking-sprite-generation/16.4-02-SUMMARY.md (reference output: 576×2560px, 40 rows, manifest shape with walk:8)
    - packages/ui/public/sprites/astronaut-manifest.json (reference shape for walk manifest entry)
    - assets/raw/robot/walk/south/frame-0.png (sanity: confirm Plan 28-01 output exists — prerequisite)
    - assets/raw/ghost/walk/south/frame-0.png (sanity: confirm Plan 28-02 output exists — prerequisite)
  </read_first>

  <action>
**Precondition check** (abort if any input missing): Before running build, verify all 72 direction-dirs exist on disk:

```bash
for c in robot alien hologram monkey caveman ghost ninja pirate medicine-woman; do
  for d in south north east west south-east south-west north-east north-west; do
    count=$(ls assets/raw/$c/walk/$d/frame-*.png 2>/dev/null | wc -l | tr -d ' ')
    if [ "$count" != "8" ]; then
      echo "ABORT: missing walk frames at assets/raw/$c/walk/$d/ (found $count, expected 8)"
      exit 1
    fi
  done
done
```

If any character is missing, abort and report — Plans 28-01 and 28-02 must complete first.

**Build step** — rebuild sheets sequentially for all 9 non-astronaut characters:

```bash
for c in robot alien hologram monkey caveman ghost ninja pirate medicine-woman; do
  echo "=== Building $c ==="
  npx tsx scripts/build-spritesheet.ts "$c"
done
```

DO NOT run for `astronaut` — it was already built correctly in Phase 16.4-02 and is out of scope here.

**Post-build verification** — for each of the 9 characters:

1. Check output PNG dimensions equal 576×2560px using `sharp` or ImageMagick `identify`:
   ```bash
   npx tsx -e "import sharp from 'sharp'; sharp('packages/ui/public/sprites/$c-sheet.png').metadata().then(m => { if (m.width !== 576 || m.height !== 2560) { console.error('FAIL $c: ' + m.width + 'x' + m.height); process.exit(1) } else console.log('OK $c 576x2560') })"
   ```

2. Check manifest has `states.walk === 8`:
   ```bash
   node -e "const m = require('./packages/ui/public/sprites/$c-manifest.json'); if (m.states.walk !== 8) { console.error('FAIL $c manifest walk: ' + m.states.walk); process.exit(1) } else console.log('OK $c manifest walk:8')"
   ```

If any check fails, report which character and abort — do NOT partially commit.

**No code changes to build-spritesheet.ts, spriteStates.ts, or OfficePage.tsx** — all wiring was completed in Phase 16.4-02. This task is pure asset regeneration.

Commit all 18 changed files (9 PNGs + 9 manifests) in one commit with message like `feat(28): rebuild 9 character sprite sheets with walk rows`.
  </action>

  <verify>
    <automated>for c in robot alien hologram monkey caveman ghost ninja pirate medicine-woman; do node -e "const m = require('./packages/ui/public/sprites/$c-manifest.json'); if (m.states.walk !== 8) { console.error('FAIL $c walk:' + m.states.walk); process.exit(1) }" || exit 1; done && npx tsx -e "import sharp from 'sharp'; const chars = ['robot','alien','hologram','monkey','caveman','ghost','ninja','pirate','medicine-woman']; Promise.all(chars.map(c => sharp('packages/ui/public/sprites/' + c + '-sheet.png').metadata().then(m => { if (m.width !== 576 || m.height !== 2560) throw new Error(c + ': ' + m.width + 'x' + m.height) }))).then(() => console.log('OK: all 9 sheets 576x2560')).catch(e => { console.error('FAIL', e.message); process.exit(1) })"</automated>
  </verify>

  <acceptance_criteria>
    - All 9 files `packages/ui/public/sprites/{robot,alien,hologram,monkey,caveman,ghost,ninja,pirate,medicine-woman}-sheet.png` exist
    - Each of those 9 PNGs has dimensions exactly `576×2560` (verifiable via `sharp().metadata()` or `identify` — width=576, height=2560)
    - All 9 manifests `packages/ui/public/sprites/{character}-manifest.json` contain a `states.walk` field with integer value `8`
    - Each manifest's `frameSize` is `64` and `directions` is `8`
    - `scripts/build-spritesheet.ts` has NOT been modified (diff is empty): `git diff scripts/build-spritesheet.ts` outputs nothing
    - `packages/ui/src/components/office/spriteStates.ts` has NOT been modified: `git diff packages/ui/src/components/office/spriteStates.ts` outputs nothing
    - `packages/ui/src/pages/OfficePage.tsx` has NOT been modified: `git diff packages/ui/src/pages/OfficePage.tsx` outputs nothing
    - Astronaut sheet NOT modified: `git diff packages/ui/public/sprites/astronaut-sheet.png` outputs nothing
    - All 18 target files (9 PNGs + 9 manifests) appear in `git status --porcelain packages/ui/public/sprites/` output
  </acceptance_criteria>

  <done>All 9 non-astronaut sprite sheets are rebuilt at 576×2560 with walk rows at offset 32-39. All manifests report walk:8. No code files modified. Astronaut untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-game visual QA — confirm every character walks correctly</name>
  <what-built>
    9 rebuilt sprite sheets (robot, alien, hologram, monkey, caveman, ghost, ninja, pirate, medicine-woman), each 576×2560 pixels with walk-cycle rows at row offset 32-39. Combined with the existing astronaut sheet, all 10 character sheets now have the same structure and share identical `STATE_ROW_OFFSET.walk = 32` wiring.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `pnpm dev` (or your usual local command) and open the Office page in the browser.
    2. Open the character picker (from Phase 27 — accessible via menu/settings area).
    3. For EACH of the 10 characters — astronaut, robot, alien, hologram, monkey, caveman, ghost, ninja, pirate, medicine-woman:
       a. Select the character via the picker and confirm.
       b. Close the picker so the Office map is visible.
       c. Hold W / A / S / D (and diagonals) for ~2 seconds each to walk in all 4 cardinal + 4 diagonal directions.
       d. Observe: the player sprite plays a believable walk cycle (feet/legs alternating, arms swinging) — NOT the static fight-stance pose that used to appear.
       e. Release keys: the sprite should snap back to an idle pose with no glitch frames.
    4. Confirm there are NO broken/blank/garbled frames during any of the 10 character walk cycles.
    5. Confirm NPC agents on the map still render their existing state animations correctly (idle, blocked, completed, failed) — no regression.

    Expected: All 10 characters walk smoothly. Each character's walk cycle matches its art style. No console errors about missing sprites or row-out-of-bounds.

    If any character shows the old fight-stance or broken frames, describe the character and the executor will re-run that character's build step (or retry generation if frames are bad).
  </how-to-verify>
  <resume-signal>Type `approved` if all 10 characters walk smoothly with no NPC regression, or describe the issue (character, direction, symptom) to trigger a retry.</resume-signal>
</task>

</tasks>

<verification>
- All 9 non-astronaut sprite sheets are 576×2560 pixels.
- All 9 manifests have `states.walk: 8`.
- build-spritesheet.ts, spriteStates.ts, OfficePage.tsx unmodified.
- Human in-game verification approved walking animation for all 10 characters.
- NPC state animations (idle/blocked/completed/failed) still work correctly — no regression.
</verification>

<success_criteria>
1. 9 × `{character}-sheet.png` at 576×2560 in `packages/ui/public/sprites/`.
2. 9 × `{character}-manifest.json` with `states.walk = 8`.
3. No source-code files modified in this plan.
4. In-game QA: all 10 characters walk smoothly when controlled by the player.
5. No NPC rendering regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/28-walking-sprites-all-characters/28-03-SUMMARY.md` documenting:
- Build durations per character (if notable)
- Final sheet dimensions and manifest values
- Human checkpoint approval and any issues encountered
- Confirmation that phase goal is met: all 10 characters now have proper walk animations
</output>
