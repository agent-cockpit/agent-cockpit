---
phase: 27-player-character-selection
plan: 02
type: component
wave: 1
depends_on:
  - 27-01-PLAN.md
files_modified:
  - packages/ui/src/components/sessions/CharacterPicker.tsx
  - packages/ui/src/components/sessions/__tests__/CharacterPicker.test.tsx
autonomous: true
requirements:
  - character-selection

must_haves:
  truths:
    - "CharacterPicker renders current face portrait and display name"
    - "Left/right controls wrap across all 10 CHARACTER_TYPES"
    - "Confirm callback receives the currently drafted character"
  artifacts:
    - path: "packages/ui/src/components/sessions/CharacterPicker.tsx"
      provides: "Reusable controlled character picker component"
    - path: "packages/ui/src/components/sessions/__tests__/CharacterPicker.test.tsx"
      provides: "Component tests for render, wrap-around, and confirm behavior"
  key_links:
    - from: "packages/ui/src/components/sessions/CharacterPicker.tsx"
      to: "packages/ui/src/components/office/characterMapping.ts"
      via: "CHARACTER_TYPES + characterFaceUrl"
      pattern: "wrap-around"
---

<objective>
Create the reusable character picker UI component.

Purpose: This plan isolates the game-styled picker into a testable component before it is wired into menu and rendering flows.
Output: `CharacterPicker.tsx` plus component tests covering navigation and confirm semantics.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-player-character-selection/27-RESEARCH.md
</execution_context>

<context>
<interfaces>
Component contract:
```typescript
interface CharacterPickerProps {
  value: CharacterType
  onChange: (character: CharacterType) => void
  onConfirm: () => void
}
```

UI requirements:
- show face image via `characterFaceUrl(value)`
- show character name in user-facing form
- render previous/next arrow buttons
- wrap from first -> last and last -> first
- expose a confirm button
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CharacterPicker tests for render, wrap-around, and confirm</name>
  <files>packages/ui/src/components/sessions/__tests__/CharacterPicker.test.tsx</files>
  <behavior>
    - Renders face image and formatted character name for current value
    - Left arrow from first character wraps to last
    - Right arrow from last character wraps to first
    - Confirm button calls `onConfirm`
    - `onChange` is called with the newly selected character
  </behavior>
  <action>
    RED: Create `packages/ui/src/components/sessions/__tests__/CharacterPicker.test.tsx`.
    Use a real subset of `CHARACTER_TYPES` behavior, not hardcoded index math in the test body only.
    Cover at least:
    1. `value='astronaut'`, click previous -> `onChange('medicine-woman')`
    2. `value='medicine-woman'`, click next -> `onChange('astronaut')`
    3. current face image src is `/sprites/faces/{character}-face.png`
    4. confirm button triggers `onConfirm`
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test -- CharacterPicker</automated>
  </verify>
  <done>Tests fail because the component does not exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CharacterPicker as a controlled component</name>
  <files>packages/ui/src/components/sessions/CharacterPicker.tsx</files>
  <action>
    GREEN: Create `packages/ui/src/components/sessions/CharacterPicker.tsx`.

    Implementation requirements:
    1. Import `CHARACTER_TYPES`, `CharacterType`, and `characterFaceUrl`
    2. Derive current index from `value`
    3. Compute previous and next with wrap-around modulo logic
    4. Render:
       - face portrait image
       - readable character label (e.g. `medicine-woman` -> `Medicine Woman`)
       - previous button
       - next button
       - confirm button
    5. Keep the component controlled: arrow buttons call `onChange(...)`; confirm calls `onConfirm()`

    Keep styling consistent with existing cockpit menu chrome. Do not add new dependencies.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test -- CharacterPicker</automated>
  </verify>
  <done>CharacterPicker tests pass and the component is ready for integration.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @cockpit/ui test -- CharacterPicker` passes
2. Wrap-around behavior is covered at both boundaries
3. Component remains reusable and does not read from the store directly
</verification>

<success_criteria>
- CharacterPicker shows face portrait, name, arrows, and confirm control
- Arrow navigation cycles through all 10 characters with wrap-around
- Confirm behavior is separated from draft navigation behavior
- Component is independent of `MenuPopup` so it can be reused later
</success_criteria>

<output>
After completion, create `.planning/phases/27-player-character-selection/27-02-SUMMARY.md`
</output>
