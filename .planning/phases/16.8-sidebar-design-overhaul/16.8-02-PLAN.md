---
phase: 16.8-sidebar-design-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/components/layout/MapSidebar.tsx
  - packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx
autonomous: true
requirements:
  - sidebar-design

must_haves:
  truths:
    - "MapSidebar rows are sorted by most recent activity first (lastEventAt descending)"
    - "Each row displays project name, provider label, status dot + text label, and pending approvals numeric pill when >0"
    - "Secondary metadata line is conditional (renders only when urgency/context is present)"
    - "Selected row has persistent active styling; hover/focus styles provide subtle motion and color shift"
    - "MapSidebar no longer uses stale `failed` status mapping and is aligned to SessionStatus (active|ended|error)"
  artifacts:
    - path: "packages/ui/src/components/layout/MapSidebar.tsx"
      provides: "Redesigned cockpit row UI with proper hierarchy, recency ordering, and map-focus click behavior"
      exports: ["MapSidebar"]
    - path: "packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx"
      provides: "Reliable tests for sorting, metadata rendering, active state, and click behavior"
  key_links:
    - from: "packages/ui/src/components/layout/MapSidebar.tsx"
      to: "packages/ui/src/store/selectors.ts (useActiveSessions)"
      via: "sidebar-local sorted copy by lastEventAt"
      pattern: "lastEventAt"
    - from: "packages/ui/src/components/layout/MapSidebar.tsx"
      to: "packages/ui/src/store/index.ts"
      via: "selectedSessionId + selectSession"
      pattern: "selectedSessionId|selectSession"
---

<objective>
Implement the visual and information-hierarchy redesign of session rows in the sidebar while preserving behavior (active-session navigation + map focus).

Purpose: This is the core UI overhaul for Phase 16.8. It upgrades readability, urgency signaling, and theme coherence without adding new sidebar features.
Output: Redesigned MapSidebar rows + robust tests replacing the current brittle test suite.
</objective>

<execution_context>
@/Users/leostuart/.codex/get-shit-done/workflows/execute-plan.md
@/Users/leostuart/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16.8-sidebar-design-overhaul/16.8-CONTEXT.md
@.planning/phases/16.8-sidebar-design-overhaul/16.8-RESEARCH.md

<interfaces>
From packages/ui/src/store/index.ts:
```typescript
export type SessionStatus = 'active' | 'ended' | 'error'
export interface SessionRecord {
  sessionId: string
  provider: 'claude' | 'codex'
  workspacePath: string
  status: SessionStatus
  lastEventAt: string
  pendingApprovals: number
}
```

Current MapSidebar contract:
```typescript
interface Props {
  onFocusSession: (sessionId: string) => void
}
```

Current test weakness:
- Existing empty-state test uses `vi.doMock` after import and does not validate empty rendering.
- Current suite does not validate sort order, status text semantics, or selected-row behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Rewrite MapSidebar tests against final row contract</name>
  <files>packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx</files>
  <behavior>
    - Rows render in `lastEventAt` descending order, not insertion order
    - Row includes project name + provider badge + status text + status dot
    - Pending approvals pill appears only when `pendingApprovals > 0`
    - Secondary metadata line renders conditionally (for non-default states)
    - Clicking row triggers both `selectSession(sessionId)` and `onFocusSession(sessionId)`
    - Empty-state render is correctly asserted when sessions array is empty
  </behavior>
  <action>
    Replace `packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx` with deterministic mocks:

    - Mock `useActiveSessions` via mutable array fixture (do not remock mid-test with `vi.doMock`).
    - Mock `useStore` selector to provide `selectedSessionId` and `selectSession` spy.

    Required RED tests:
    1. Sort test with three sessions whose `lastEventAt` values are intentionally out of order.
    2. Status semantics test: `active`, `ended`, `error` labels are rendered and tied to status dots.
    3. Pending badge test: only rows with approvals > 0 show a numeric pill.
    4. Click contract test: row click calls `selectSession` and `onFocusSession` with same sessionId.
    5. Empty state test: with zero sessions, no row buttons exist and empty message is visible.

    Run RED:
    ```bash
    pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx
    ```
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx</automated>
  </verify>
  <done>New MapSidebar tests exist and fail RED against the current implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement redesigned row UI, ordering, and interaction states</name>
  <files>packages/ui/src/components/layout/MapSidebar.tsx</files>
  <behavior>
    - Sidebar content component is list-focused (no nested width ownership assumptions)
    - Sessions are copied/sorted by descending `lastEventAt`
    - Row layout:
      - Primary line: project name + provider badge + status dot/text
      - Secondary line (conditional): urgency/context metadata only when needed
      - Pending approvals numeric pill with restrained accent ring
    - Active row style persists when `selectedSessionId === session.sessionId`
    - Hover/focus-visible styles apply subtle transform/contrast changes
    - Status map uses `error` (not `failed`)
  </behavior>
  <action>
    Update `packages/ui/src/components/layout/MapSidebar.tsx`:

    Step 1 — Data/view-model:
    - Read sessions from `useActiveSessions()`.
    - Create local sorted array: `const rows = [...sessions].sort((a, b) => b.lastEventAt.localeCompare(a.lastEventAt))`.
    - Pull `selectedSessionId` + `selectSession` from store.

    Step 2 — Row click behavior:
    - On row click: call `selectSession(sessionId)` then `onFocusSession(sessionId)`.

    Step 3 — Visual hierarchy classes:
    - Container spacing and list rhythm consistent with cockpit theme.
    - Status uses both text and dot (non-color-only).
    - Provider badge uses distinct claude/codex chips.
    - Pending approvals shown as numeric capsule only when >0.

    Step 4 — Conditional secondary line policy:
    - Render secondary metadata only when row needs extra context, e.g. `pendingApprovals > 0` or non-active status.
    - Keep default rows single-line for density.

    Step 5 — Accessibility:
    - Ensure buttons have explicit `type="button"`.
    - Add `aria-current="true"` on selected row.
    - Preserve keyboard focus-visible styles.

    Run GREEN:
    ```bash
    pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx
    ```
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx</automated>
  </verify>
  <done>All rewritten MapSidebar tests pass; row redesign, ordering, and active/focus behavior are implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Layout regression check (sidebar-specific)</name>
  <files>packages/ui/src/components/layout/MapSidebar.tsx, packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx</files>
  <action>
    Execute regression checks after redesign:

    ```bash
    pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx
    pnpm --filter @cockpit/ui typecheck
    ```

    If any failures are pre-existing and unrelated, record them in summary under issues/deviations.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx && pnpm --filter @cockpit/ui typecheck</automated>
  </verify>
  <done>Sidebar redesign integrates cleanly with layout shell and compiles without new regressions.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx`
- `pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx`
- `pnpm --filter @cockpit/ui typecheck`
- Manual quick scan: rows show project/provider/status/pending hierarchy and selected row remains visually persistent.
</verification>

<success_criteria>
- MapSidebar rows are recency-sorted and semantically complete (project, provider, status text+dot, approvals badge).
- Selected row highlight is persistent and keyboard focus styles are present.
- Empty state is themed/minimal and correctly tested.
- MapSidebar test suite is robust (no ineffective remock patterns).
</success_criteria>

<output>
After completion, create `.planning/phases/16.8-sidebar-design-overhaul/16.8-02-SUMMARY.md`
</output>
