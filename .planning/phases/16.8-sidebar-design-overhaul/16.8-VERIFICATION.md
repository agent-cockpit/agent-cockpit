---
phase: 16.8-sidebar-design-overhaul
verified: 2026-04-13T16:27:57Z
status: gaps_found
---

## VERIFICATION GAPS_FOUND

Phase goal implementation is largely achieved in code, but requirement-ID traceability has a blocking documentation gap.

### Must-have verification (codebase)
- `16.8-01` shell/theming truths are implemented in:
  - `packages/ui/src/index.css` (semantic tokens: background/foreground/sidebar/border/muted/accent)
  - `packages/ui/src/components/layout/OpsLayout.tsx` (unified header, desktop-only resize handle, clamped persisted width via `cockpit.sidebar.width`)
  - `packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx` (persistence/clamp/mobile guard coverage)
- `16.8-02` row redesign truths are implemented in:
  - `packages/ui/src/components/layout/MapSidebar.tsx` (recency sort, project/provider/status hierarchy, conditional metadata, approvals pill, selected-row styling)
  - `packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx` (sorting, semantics, click contract, empty state)
- `16.8-03` focus-behavior truths are implemented in:
  - `packages/ui/src/pages/OfficePage.tsx` (`scrollToSession` now centers camera + syncs player; safe no-op for missing session)
  - `packages/ui/src/pages/__tests__/OfficePage.test.tsx` (focus + no-op tests)
  - `16.8-03-SUMMARY.md` documents human checkpoint approval.

### Automated verification run
- Command: `pnpm --filter @cockpit/ui test --run src/components/layout/__tests__/OpsLayout.test.tsx src/components/layout/__tests__/MapSidebar.test.tsx src/pages/__tests__/OfficePage.test.tsx`
- Result: 3/3 test files passed, 22/22 tests passed.
- Notes: non-failing jsdom/fetch URL warnings for `/map/terrain-map.json` appeared in `OfficePage` tests (pre-existing warning pattern).

### Requirement ID cross-reference (PLAN frontmatter -> REQUIREMENTS.md)
- IDs found in all three plans: `sidebar-design`
- `sidebar-design` is present in `.planning/ROADMAP.md` (Phase 16.8 requirements line), but **not found in `.planning/REQUIREMENTS.md`**.

Because the objective requires every PLAN-frontmatter requirement ID to be accounted for in `REQUIREMENTS.md`, this verification is marked `gaps_found`.
