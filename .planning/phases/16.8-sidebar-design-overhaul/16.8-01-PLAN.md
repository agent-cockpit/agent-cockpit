---
phase: 16.8-sidebar-design-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/index.css
  - packages/ui/src/components/layout/OpsLayout.tsx
  - packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx
autonomous: true
requirements:
  - sidebar-design

must_haves:
  truths:
    - "Semantic Tailwind tokens used by layout components are defined in packages/ui/src/index.css (background/foreground/sidebar/border/muted/accent)"
    - "OpsLayout renders a single unified sidebar header block (title + contextual count + history action) instead of split bar fragments"
    - "Desktop sidebar width is user-resizable with clamp bounds and persisted in localStorage"
    - "Resize affordance is disabled on narrow/mobile breakpoints to avoid broken touch behavior"
    - "OpsLayout behavior is covered by automated tests for persistence and clamp logic"
  artifacts:
    - path: "packages/ui/src/index.css"
      provides: "Sidebar palette + typography CSS variables/tokens that make semantic classes render consistently"
      contains: "--color-sidebar"
    - path: "packages/ui/src/components/layout/OpsLayout.tsx"
      provides: "Resizable sidebar shell with unified header and desktop-only drag handle"
      exports: ["OpsLayout"]
    - path: "packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx"
      provides: "Regression tests for width persistence, resize clamp, and handle visibility behavior"
  key_links:
    - from: "packages/ui/src/components/layout/OpsLayout.tsx"
      to: "packages/ui/src/hooks/useLocalStorage.ts"
      via: "useLocalStorage('cockpit.sidebar.width', ... )"
      pattern: "cockpit\.sidebar\.width"
    - from: "packages/ui/src/components/layout/OpsLayout.tsx"
      to: "window pointer listeners"
      via: "pointerdown/pointermove/pointerup"
      pattern: "pointermove|pointerup"
---

<objective>
Establish the sidebar shell foundation for the design overhaul: define missing semantic theme tokens, implement a unified cockpit-style header block, and add desktop resizable width with persistence.

Purpose: The current sidebar cannot be polished reliably while semantic classes resolve to missing tokens and width is fixed. This plan creates the structural/theming baseline that later plans style and populate.
Output: index.css token baseline, OpsLayout resizable shell, and passing OpsLayout tests.
</objective>

<execution_context>
@/Users/leostuart/.codex/get-shit-done/workflows/execute-plan.md
@/Users/leostuart/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16.8-sidebar-design-overhaul/16.8-CONTEXT.md
@.planning/phases/16.8-sidebar-design-overhaul/16.8-RESEARCH.md

<interfaces>
From packages/ui/src/hooks/useLocalStorage.ts:
```typescript
export function useLocalStorage<T>(
  key: string,
  defaultValue: T,
): [T, (updater: T | ((prev: T) => T)) => void]
```

Current OpsLayout shell:
```tsx
<aside className="w-72 flex-none border-r overflow-hidden flex flex-col">
  <div className="px-4 py-3 border-b flex items-center justify-between"> ... </div>
  <MapSidebar onFocusSession={scrollToSession} />
</aside>
```

Current semantic class risk:
- `bg-sidebar`, `bg-background`, `border-border`, `text-muted-foreground`, `hover:bg-accent`
- These classes need explicit token definitions in `packages/ui/src/index.css` for Tailwind v4 semantic mapping.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Add OpsLayout resize/persistence behavior tests</name>
  <files>packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx</files>
  <behavior>
    - OpsLayout reads initial width from localStorage key `cockpit.sidebar.width`
    - Resize handle drag updates inline sidebar width style and clamps to bounds (min 260, max 460)
    - Width updates persist by writing the same localStorage key
    - History button remains rendered and clickable after resizing
    - Resize handle is not interactive on narrow viewports (mobile guard)
  </behavior>
  <action>
    Create `packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx`.

    Test setup guidance:
    - Mock `react-router` `Outlet` to a sentinel `<div data-testid="outlet" />`
    - Mock `MapSidebar` to a lightweight component that renders `<div data-testid="map-sidebar" />`
    - Mock `scrollToSession` import from OfficePage to avoid depending on map internals
    - Stub `window.matchMedia` for desktop/mobile scenarios

    Required assertions:
    1. With `localStorage.setItem('cockpit.sidebar.width', '340')` before render, sidebar container width is 340px.
    2. Simulate pointer drag on resizer from 340 -> 520 and assert clamped width is 460px.
    3. Simulate pointer drag from 340 -> 120 and assert clamped width is 260px.
    4. After drag, verify localStorage key stores the clamped width.
    5. History button still calls `setHistoryOpen(true)` behavior (assert popup or button state change).

    Run tests to confirm RED before implementation:
    ```bash
    pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx
    ```
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx</automated>
  </verify>
  <done>OpsLayout test file exists and fails in RED state on resize/persistence expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement semantic tokens + unified header + resizable sidebar shell</name>
  <files>packages/ui/src/index.css, packages/ui/src/components/layout/OpsLayout.tsx</files>
  <behavior>
    - index.css defines semantic theme variables for sidebar surface, borders, muted text, accent, and foreground/background
    - OpsLayout uses `useLocalStorage` for sidebar width state and clamps values to [260, 460]
    - OpsLayout exposes a keyboard-focusable resize handle (`role="separator"` with aria label)
    - Header is a single block with title, optional active-session count label, and History button
    - Mobile guard: resizer hidden/disabled when viewport is below desktop breakpoint
  </behavior>
  <action>
    Step 1 — Update `packages/ui/src/index.css`:
    - Add Tailwind v4 `@theme` token definitions used by semantic classes (`--color-background`, `--color-foreground`, `--color-sidebar`, `--color-border`, `--color-muted`, `--color-muted-foreground`, `--color-accent`, `--color-accent-foreground`).
    - Add CSS variables for sidebar typography pairing:
      - Display/label family (pixel-flavored stack)
      - Body/list family (readability-first stack)
    - Keep existing sprite glow classes intact.

    Step 2 — Refactor `packages/ui/src/components/layout/OpsLayout.tsx`:
    - Replace fixed `w-72` width with inline style driven by `useLocalStorage('cockpit.sidebar.width', 320)`.
    - Implement drag-resize using pointer events on a right-edge handle.
    - Clamp width updates to min/max constants (260/460).
    - Use desktop-only interaction guard (e.g., `window.matchMedia('(min-width: 1024px)')`).
    - Keep `MapSidebar` and `HistoryPopup` behavior unchanged.

    Step 3 — Ensure header structure is one visual block:
    - Title line: “Agent Cockpit”.
    - Context line (e.g., active list label/count) in smaller muted style.
    - History button remains in the same block.

    Run tests to move RED→GREEN:
    ```bash
    pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx
    ```
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx</automated>
  </verify>
  <done>OpsLayout tests pass; semantic tokens are defined; desktop width resize persists and is clamped.</done>
</task>

<task type="auto">
  <name>Task 3: Regression pass for layout package</name>
  <files>packages/ui/src/components/layout/OpsLayout.tsx, packages/ui/src/index.css</files>
  <action>
    Run targeted and package-level verification:

    ```bash
    pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx
    pnpm --filter @cockpit/ui typecheck
    ```

    If typecheck failures are unrelated pre-existing issues, document them in the summary and proceed only if touched files are clean.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx && pnpm --filter @cockpit/ui typecheck</automated>
  </verify>
  <done>Layout tests and typecheck pass for modified files, no new regressions introduced by shell/token changes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx`
- `pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx`
- `pnpm --filter @cockpit/ui typecheck`
- Manual code review: OpsLayout has persisted width, clamp bounds, and a desktop-only resize handle.
</verification>

<success_criteria>
- Semantic sidebar tokens exist in `packages/ui/src/index.css` and align with semantic class usage.
- OpsLayout sidebar width can be resized on desktop and is clamped + persisted.
- Header is a unified cockpit block with clear hierarchy and working History action.
- OpsLayout tests cover persistence and resize behavior; all pass.
</success_criteria>

<output>
After completion, create `.planning/phases/16.8-sidebar-design-overhaul/16.8-01-SUMMARY.md`
</output>
