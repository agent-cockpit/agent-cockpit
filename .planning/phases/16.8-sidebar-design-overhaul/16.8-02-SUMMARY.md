---
phase: 16.8-sidebar-design-overhaul
plan: "02"
subsystem: ui
tags: [sidebar, react, tailwind, vitest, zustand]

requires:
  - phase: 14-office-map-view-full-navigation-paradigm-shift
    provides: Existing map-focused sidebar interaction contract (`onFocusSession`)

provides:
  - MapSidebar row redesign with recency ordering and status/provider hierarchy
  - Deterministic MapSidebar test suite covering sorting, click contract, and empty state
  - Regression-blocker log for unrelated OpsLayout/typecheck failures

affects:
  - 16.8-sidebar-design-overhaul
  - sidebar-design
  - office-layout-regression-tests

tech-stack:
  added: []
  patterns:
    - Sidebar-local row sorting by `lastEventAt` descending to avoid selector side effects
    - Row interaction contract: `selectSession` then `onFocusSession`
    - Conditional secondary metadata line only for urgency/context rows

key-files:
  created:
    - .planning/phases/16.8-sidebar-design-overhaul/deferred-items.md
  modified:
    - packages/ui/src/components/layout/MapSidebar.tsx
    - packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx

key-decisions:
  - "MapSidebar is now list-content focused and no longer owns nested sidebar width/surface framing"
  - "Status semantics align to SessionStatus (`active | ended | error`) with text+dot cues"
  - "Secondary metadata renders only when `pendingApprovals > 0` or status is non-active"

patterns-established:
  - "Use mutable fixture + selector callback mock in Vitest instead of `vi.doMock` remock patterns for store-backed components"

requirements-completed:
  - sidebar-design

duration: 17m
completed: 2026-04-13
---

# Phase 16.8 Plan 02: Sidebar Row Redesign Summary

**MapSidebar now renders recency-sorted cockpit rows with provider/status hierarchy, pending-approval urgency cues, and deterministic interaction-focused tests**

## Performance

- **Duration:** ~17 min
- **Started:** 2026-04-13T14:54:24Z
- **Completed:** 2026-04-13T15:11:39Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- Replaced brittle MapSidebar tests with deterministic RED/GREEN coverage for order, status semantics, pending badges, click behavior, and empty state.
- Implemented redesigned row UI in `MapSidebar` with sorted local rows, provider chips, status text+dot, conditional metadata, persistent selected styling, and focus-visible affordances.
- Preserved behavior contract by wiring row click to `selectSession(sessionId)` followed by `onFocusSession(sessionId)`.
- Documented out-of-scope regression blockers found in Task 3 in a phase-local deferred log.

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — Rewrite MapSidebar tests against final row contract** - `b1d239f` (test)
2. **Task 2: GREEN — Implement redesigned row UI, ordering, and interaction states** - `43de326` (feat)
3. **Task 3: Layout regression check (sidebar-specific)** - `93a4d9c` (chore)

## Files Created/Modified
- `packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx` - New deterministic TDD suite for row contract and behavior.
- `packages/ui/src/components/layout/MapSidebar.tsx` - Redesigned row rendering, store selection handling, recency sort, and state styling.
- `.planning/phases/16.8-sidebar-design-overhaul/deferred-items.md` - Out-of-scope verification failures captured for follow-up.

## Decisions Made
- Keep sorting logic local to `MapSidebar` (`rows = [...sessions].sort(...)`) to avoid changing global selector behavior.
- Use explicit status labels (`Active`, `Ended`, `Error`) paired with status dots for non-color-only semantics.
- Render secondary metadata only when a row carries context/urgency (non-active status or pending approvals).
- Apply `aria-current="true"` only on selected rows; maintain keyboard `focus-visible` ring styling.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Corrected package-scoped Vitest test path for filtered runs**
- **Found during:** Task 1 verification command
- **Issue:** Plan command used repo-root-prefixed paths with `pnpm --filter @cockpit/ui`, causing Vitest to report "No test files found."
- **Fix:** Ran equivalent package-relative test paths (`src/components/...`) for RED/GREEN/regression verification.
- **Files modified:** None (execution-command fix only)
- **Verification:** RED failures and GREEN pass executed successfully against `MapSidebar.test.tsx`.
- **Committed in:** `b1d239f` / `43de326` task flow (no additional code changes)

---

**Total deviations:** 1 auto-fixed (Rule 3 - Blocking)
**Impact on plan:** Kept verification executable without changing scope or behavior.

## Issues Encountered
- `src/components/layout/__tests__/OpsLayout.test.tsx` (untracked, pre-existing) failed 4 assertions expecting resize-handle/sidebar-width features not present in current `OpsLayout.tsx`.
- `pnpm --filter @cockpit/ui typecheck` failed on pre-existing non-sidebar errors in:
  - `src/components/office/__tests__/ApprovalInboxPopup.test.tsx`
  - `src/components/office/AgentSprite.tsx`
  - `src/setupTests.ts`
- All out-of-scope findings were logged to `.planning/phases/16.8-sidebar-design-overhaul/deferred-items.md`.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Sidebar row redesign contract is implemented and covered by deterministic tests.
- Plan 16.8-03 can build on this row contract while tracking deferred OpsLayout/typecheck issues separately.

---
*Phase: 16.8-sidebar-design-overhaul*
*Completed: 2026-04-13*

## Self-Check: PASSED

- Verified required files exist:
  - `.planning/phases/16.8-sidebar-design-overhaul/16.8-02-SUMMARY.md`
  - `.planning/phases/16.8-sidebar-design-overhaul/deferred-items.md`
  - `packages/ui/src/components/layout/MapSidebar.tsx`
  - `packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx`
- Verified task commits exist in git history: `b1d239f`, `43de326`, `93a4d9c`
