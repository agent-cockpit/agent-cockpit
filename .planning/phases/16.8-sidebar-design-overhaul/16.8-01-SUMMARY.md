---
phase: 16.8-sidebar-design-overhaul
plan: 01
subsystem: ui
tags: [tailwind-v4, sidebar, layout, vitest, localstorage]
requires:
  - phase: 16.7-wall-and-object-collision-physics
    provides: office map + session shell baseline
provides:
  - Semantic Tailwind color tokens for sidebar/background/border/accent classes
  - Resizable desktop sidebar in OpsLayout with persisted clamped width
  - Unified sidebar header block with title, active-session context, and history action
affects: [16.8-03, sidebar-styling, layout-shell]
tech-stack:
  added: []
  patterns:
    - OpsLayout owns sidebar geometry; MapSidebar remains content-focused
    - Sidebar width persisted with useLocalStorage and clamped to desktop-safe bounds
key-files:
  created:
    - packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx
  modified:
    - packages/ui/src/index.css
    - packages/ui/src/components/layout/OpsLayout.tsx
    - packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx
key-decisions:
  - "Defined semantic Tailwind v4 tokens in index.css instead of local hardcoded utility colors."
  - "Implemented desktop-only resize handle via matchMedia('(min-width: 1024px)') with width clamp [260, 460]."
patterns-established:
  - "Sidebar shell keeps resize logic in OpsLayout; session rows remain isolated in MapSidebar."
  - "Layout behavior tests include localStorage hydration, clamp bounds, and viewport guard checks."
requirements-completed: [sidebar-design]
duration: 9 min
completed: 2026-04-13
---

# Phase 16.8 Plan 01: Sidebar Shell Foundation Summary

**Semantic sidebar theming plus a desktop-resizable, persisted OpsLayout shell with unified cockpit header and regression coverage for width behavior**

## Performance

- **Duration:** 9 min
- **Started:** 2026-04-13T15:16:00Z
- **Completed:** 2026-04-13T15:24:56Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- Added missing Tailwind semantic tokens (`background/foreground/sidebar/border/muted/accent`) and sidebar font variables in `index.css`.
- Rebuilt `OpsLayout` sidebar shell to support persisted + clamped width resizing on desktop while preserving History popup behavior.
- Added and validated `OpsLayout` behavior tests for hydration, clamp bounds, persistence, and mobile resize guard.

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — Add OpsLayout resize/persistence behavior tests** - `1ea180b` (test)
2. **Task 2: GREEN — Implement semantic tokens + unified header + resizable sidebar shell** - `a998c68` (feat)
3. **Task 3: Regression pass for layout package** - `e840838` (chore)

## Files Created/Modified
- `packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx` - new behavior coverage for persisted width, clamp logic, and mobile guard
- `packages/ui/src/components/layout/OpsLayout.tsx` - desktop-resizable sidebar shell with unified header block and persisted width
- `packages/ui/src/index.css` - semantic Tailwind tokens and sidebar typography variables

## Decisions Made
- Defined semantic theme tokens in `index.css` to make existing semantic classes (`bg-sidebar`, `border-border`, etc.) resolve consistently.
- Kept resize interaction desktop-only via media query gating to avoid broken touch behavior on narrow viewports.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Adjusted test command paths for pnpm filter execution**
- **Found during:** Task 1 verification
- **Issue:** Plan commands referenced `packages/ui/...` paths that do not resolve when `pnpm --filter @cockpit/ui` runs from package root.
- **Fix:** Used package-relative test paths (`src/components/...`) for all vitest invocations in this plan.
- **Files modified:** None
- **Verification:** Targeted and combined test commands executed successfully with corrected paths.
- **Committed in:** `e840838`

**2. [Rule 3 - Blocking] Added pointer-event compatibility shim for jsdom**
- **Found during:** Task 2 GREEN verification
- **Issue:** `fireEvent.pointerDown` in jsdom did not dispatch reliable pointer semantics without `window.PointerEvent`, preventing drag tests from triggering.
- **Fix:** Added a `beforeEach` shim in `OpsLayout.test.tsx` to map `window.PointerEvent` to `MouseEvent` when missing.
- **Files modified:** `packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx`
- **Verification:** `OpsLayout.test.tsx` transitions RED→GREEN with all five assertions passing.
- **Committed in:** `a998c68`

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both fixes were execution blockers; no scope expansion beyond intended sidebar shell and test coverage work.

## Issues Encountered
- `pnpm --filter @cockpit/ui typecheck` fails on pre-existing issues outside plan scope:
  - `src/components/office/__tests__/ApprovalInboxPopup.test.tsx` (`beforeEach` missing)
  - `src/components/office/AgentSprite.tsx` (`walk` missing in animation map)
  - `src/setupTests.ts` (`HTMLCanvasElement.getContext` typing mismatch)

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Sidebar shell/token foundation is in place and regression-tested for resize behavior.
- Ready for remaining sidebar overhaul work in `16.8-03`.

---
*Phase: 16.8-sidebar-design-overhaul*
*Completed: 2026-04-13*

## Self-Check: PASSED

- FOUND: `.planning/phases/16.8-sidebar-design-overhaul/16.8-01-SUMMARY.md`
- FOUND: `1ea180b`
- FOUND: `a998c68`
- FOUND: `e840838`
