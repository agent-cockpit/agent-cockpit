---
phase: 16.8-sidebar-design-overhaul
plan: 03
type: execute
wave: 2
depends_on:
  - 16.8-01
  - 16.8-02
files_modified:
  - packages/ui/src/pages/OfficePage.tsx
  - packages/ui/src/pages/__tests__/OfficePage.test.tsx
autonomous: false
requirements:
  - sidebar-design

must_haves:
  truths:
    - "Clicking a sidebar row focuses the corresponding session on the map (no longer no-op)"
    - "Focus behavior keeps camera and player state coherent (camera snap preserved on subsequent ticks)"
    - "Sidebar redesign remains behavior-compatible: active-session navigation + map focus"
    - "Automated tests cover exported scrollToSession focus behavior and no-op safety when session is missing"
    - "Human verification confirms desktop/mobile usability, resize behavior, and visual hierarchy quality"
  artifacts:
    - path: "packages/ui/src/pages/OfficePage.tsx"
      provides: "Functional scrollToSession callback implementation that centers map camera on sidebar-selected session"
      exports: ["OfficePage", "scrollToSession"]
    - path: "packages/ui/src/pages/__tests__/OfficePage.test.tsx"
      provides: "Tests validating sidebar-triggered map focus behavior"
  key_links:
    - from: "packages/ui/src/components/layout/OpsLayout.tsx"
      to: "packages/ui/src/pages/OfficePage.tsx"
      via: "MapSidebar onFocusSession={scrollToSession}"
      pattern: "onFocusSession=\{scrollToSession\}"
    - from: "packages/ui/src/pages/OfficePage.tsx"
      to: "gameState.camera + gameState.player"
      via: "scrollToSession callback implementation"
      pattern: "_scrollToSession"
---

<objective>
Close the remaining behavioral gap for the sidebar overhaul by restoring real map-focus behavior for sidebar clicks, then run a human QA checkpoint for final design/usability acceptance.

Purpose: Phase 16.8 is not complete if the redesigned sidebar looks polished but still fails to focus sessions on the map. This plan restores that behavior and validates the end-to-end experience.
Output: OfficePage map-focus callback implemented + tests + human verification notes.
</objective>

<execution_context>
@/Users/leostuart/.codex/get-shit-done/workflows/execute-plan.md
@/Users/leostuart/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16.8-sidebar-design-overhaul/16.8-CONTEXT.md
@.planning/phases/16.8-sidebar-design-overhaul/16.8-RESEARCH.md
@.planning/phases/16.8-sidebar-design-overhaul/16.8-01-SUMMARY.md
@.planning/phases/16.8-sidebar-design-overhaul/16.8-02-SUMMARY.md

<interfaces>
From packages/ui/src/pages/OfficePage.tsx:
```typescript
let _scrollToSession: ((id: string) => void) | null = null
export function scrollToSession(id: string) { _scrollToSession?.(id) }

useEffect(() => {
  _scrollToSession = (_id: string) => {}
  return () => { _scrollToSession = null }
}, [])
```

Expected behavior parity with canvas click handler:
```typescript
// Canvas click currently centers camera and moves player to NPC position.
// scrollToSession should apply equivalent state updates when invoked from sidebar.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Add OfficePage tests for sidebar-triggered map focus</name>
  <files>packages/ui/src/pages/__tests__/OfficePage.test.tsx</files>
  <behavior>
    - Calling exported `scrollToSession(sessionId)` after OfficePage mount centers camera on target NPC
    - Focused session move also keeps player position synchronized to target NPC position
    - Calling `scrollToSession` with unknown session id is safe no-op (no throw, no camera mutation)
  </behavior>
  <action>
    Extend `packages/ui/src/pages/__tests__/OfficePage.test.tsx` with a new describe block:

    Test setup:
    - Render `OfficePage` with at least one active session so `gameState.npcs[sessionId]` is seeded.
    - Set deterministic camera viewport values.

    Required RED tests:
    1. `scrollToSession('test-session-1')` updates `gameState.camera.targetX/targetY` and `gameState.camera.x/y` to centered values.
    2. Same call sets `gameState.player.x/y` to target NPC coordinates (camera-follow coherence).
    3. Unknown id leaves camera/player unchanged.

    Run RED:
    ```bash
    pnpm --filter @cockpit/ui test --run packages/ui/src/pages/__tests__/OfficePage.test.tsx
    ```
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run packages/ui/src/pages/__tests__/OfficePage.test.tsx</automated>
  </verify>
  <done>New OfficePage focus tests exist and fail RED against current no-op callback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement real scrollToSession callback in OfficePage</name>
  <files>packages/ui/src/pages/OfficePage.tsx</files>
  <behavior>
    - `_scrollToSession` callback resolves NPC position by session id from `gameState.npcs`
    - If position exists: camera instantly centers and clamps to world bounds (same as click-to-focus)
    - Player x/y is updated to focused NPC position so subsequent game loop frames preserve camera center
    - Missing session id path exits safely with no state mutation
  </behavior>
  <action>
    Update `packages/ui/src/pages/OfficePage.tsx` callback registration effect:

    Replace current no-op assignment with logic equivalent to canvas click focus behavior:
    - Resolve `const pos = gameState.npcs[id]`; return early if absent.
    - Compute clamped camera target using `WORLD_W`, `WORLD_H`, `camera.viewportW`, `camera.viewportH`.
    - Assign `gameState.player.x/y = pos.x/pos.y`.
    - Assign both `camera.targetX/Y` and `camera.x/y` to snapped values.

    Keep exported `scrollToSession(id)` signature unchanged for OpsLayout compatibility.

    Run GREEN:
    ```bash
    pnpm --filter @cockpit/ui test --run packages/ui/src/pages/__tests__/OfficePage.test.tsx
    ```
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run packages/ui/src/pages/__tests__/OfficePage.test.tsx</automated>
  </verify>
  <done>OfficePage tests pass and sidebar-triggered focus is functionally equivalent to click focus.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human QA checkpoint — sidebar redesign acceptance</name>
  <what-built>
Phase 16.8 complete stack:
- Semantic sidebar theming + typography baseline
- Resizable desktop sidebar shell with unified header
- Redesigned session rows (project/provider/status/approvals, recency sort, active/hover/focus states)
- Sidebar click now focuses selected session on map via scrollToSession implementation
  </what-built>
  <how-to-verify>
Start app:
```bash
pnpm --filter @cockpit/ui dev
```

Verify all items:
1. Desktop: drag sidebar edge; width clamps and persists after reload.
2. Mobile/narrow viewport: resize handle is absent/non-interactive.
3. Row hierarchy scan: project name prominent, provider/status readable, approvals badge visible when pending.
4. Click several sidebar rows: map camera snaps to corresponding NPC each time.
5. Active row highlight remains visible after selection and through hover transitions.
6. Empty state (if no active sessions) is minimal/themed and visually balanced.
7. Keyboard check: tab focus outline is visible on row buttons and History button.
  </how-to-verify>
  <resume-signal>Type "approved" if sidebar redesign is accepted, or report concrete issues (example: "focus wrong NPC", "resizer overflows at 460px", "status text unreadable").</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @cockpit/ui test --run packages/ui/src/pages/__tests__/OfficePage.test.tsx`
- `pnpm --filter @cockpit/ui test --run packages/ui/src/components/layout/__tests__/MapSidebar.test.tsx packages/ui/src/components/layout/__tests__/OpsLayout.test.tsx`
- Human checkpoint approval for visual and interaction quality.
</verification>

<success_criteria>
- `scrollToSession` is no longer a no-op and correctly centers camera by sidebar selection.
- Camera/player state remains coherent after focus operations.
- Sidebar redesign passes manual UX quality gate on desktop and mobile.
- Automated tests cover focus callback behavior and remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/16.8-sidebar-design-overhaul/16.8-03-SUMMARY.md`
</output>
