---
phase: 13-pixel-art-generation-and-integration
plan: "04"
type: execute
wave: 5
depends_on: ["13-03"]
files_modified:
  - packages/ui/public/sprites/icon-timeline.png
  - packages/ui/public/sprites/icon-memory.png
  - packages/ui/public/sprites/icon-approvals.png
  - packages/ui/public/sprites/icon-diff.png
  - packages/ui/public/sprites/icon-history.png
  - packages/ui/public/sprites/badge-low.png
  - packages/ui/public/sprites/badge-medium.png
  - packages/ui/public/sprites/badge-high.png
  - packages/ui/public/sprites/badge-critical.png
  - packages/ui/public/sprites/loading-animation.png
  - packages/ui/src/components/RiskBadge.tsx
  - packages/ui/src/components/LoadingSpinner.tsx
  - packages/ui/src/components/panels/ApprovalInbox.tsx
  - packages/ui/src/components/layout/SessionListPanel.tsx
  - scripts/generation-log.md
autonomous: false
requirements:
  - GEN-06

must_haves:
  truths:
    - "All 5 panel header icons exist at public/sprites/icon-{name}.png"
    - "All 4 risk level badges exist at public/sprites/badge-{level}.png"
    - "Loading animation asset exists at public/sprites/loading-animation.png"
    - "RiskBadge component renders the correct badge image by risk level"
    - "LoadingSpinner component renders the loading-animation.png"
    - "RiskBadge is imported and used in ApprovalInbox.tsx risk level display"
    - "LoadingSpinner is imported and shown in SessionListPanel.tsx when wsStatus === 'connecting'"
    - "validate-sprites.ts --tier2 passes (exits 0)"
  artifacts:
    - path: "packages/ui/public/sprites/icon-timeline.png"
      provides: "Timeline panel header icon"
    - path: "packages/ui/public/sprites/badge-critical.png"
      provides: "Critical risk level badge"
    - path: "packages/ui/src/components/RiskBadge.tsx"
      provides: "Risk level badge component using sprite images"
      exports: ["RiskBadge"]
    - path: "packages/ui/src/components/LoadingSpinner.tsx"
      provides: "Loading animation component using sprite image"
      exports: ["LoadingSpinner"]
    - path: "packages/ui/src/components/panels/ApprovalInbox.tsx"
      provides: "ApprovalInbox with RiskBadge wired in place of inline text badge"
    - path: "packages/ui/src/components/layout/SessionListPanel.tsx"
      provides: "SessionListPanel with LoadingSpinner shown during WebSocket connecting state"
  key_links:
    - from: "packages/ui/src/components/RiskBadge.tsx"
      to: "/sprites/badge-{level}.png"
      via: "img src prop — level prop maps to filename"
      pattern: "badge-.*\\.png"
    - from: "packages/ui/src/components/LoadingSpinner.tsx"
      to: "/sprites/loading-animation.png"
      via: "img src or CSS background-image"
      pattern: "loading-animation"
    - from: "packages/ui/src/components/panels/ApprovalInbox.tsx"
      to: "packages/ui/src/components/RiskBadge.tsx"
      via: "import RiskBadge — replaces inline riskBadgeClass text span"
      pattern: "RiskBadge"
    - from: "packages/ui/src/components/layout/SessionListPanel.tsx"
      to: "packages/ui/src/components/LoadingSpinner.tsx"
      via: "import LoadingSpinner — rendered when wsStatus === 'connecting'"
      pattern: "LoadingSpinner"
---

<objective>
Generate and integrate Tier 2/3 assets: panel header icons, risk level badges, and the loading animation. Wire RiskBadge and LoadingSpinner components into their consuming parents.

Purpose: Completes SC-6 — all Tier 2/3 assets generated and wired. After this plan, the full visual system is operational: risk badges appear in the approval panel, and a pixel art spinner shows during WebSocket reconnect.
Output: 10 icon/badge PNG files, RiskBadge.tsx, LoadingSpinner.tsx, both wired into consuming components, validate-sprites --tier2 passing.
</objective>

<execution_context>
@/Users/leostuart/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leostuart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pixel-art-generation-and-integration/13-RESEARCH.md
@.planning/phases/10.2-pixel-art-preproduction/10.2-04-DESIGN-DECISIONS.md
@.planning/phases/13-pixel-art-generation-and-integration/13-03-SUMMARY.md

<interfaces>
<!-- PixelLab create_map_object — for icons and badges -->
Tool: create_map_object
Key parameters: description, width, height, view
Typical params for panel icons:
  width: 16, height: 16, view: "top-down"
  description: "[icon description], pixel art, minimal, single color, space station UI icon"

Risk badge params:
  width: 24, height: 16, view: "top-down"
  description: "[level] risk badge, pixel art, [color]: low=green, medium=yellow, high=orange, critical=red"

Loading animation params:
  Use create_map_object or create_character (single frame or looping)
  description: "circular loading spinner, space station UI style, pixel art, 16px, technical readout aesthetic"
  width: 16, height: 16

RiskBadge.tsx contract:
```typescript
// New file: packages/ui/src/components/RiskBadge.tsx
type RiskLevel = 'low' | 'medium' | 'high' | 'critical'
interface RiskBadgeProps { level: RiskLevel; className?: string }
export function RiskBadge({ level, className }: RiskBadgeProps) {
  return <img src={`/sprites/badge-${level}.png`} alt={`${level} risk`}
    style={{ imageRendering: 'pixelated' }} className={className} />
}
```

LoadingSpinner.tsx contract:
```typescript
// New file: packages/ui/src/components/LoadingSpinner.tsx
export function LoadingSpinner({ className }: { className?: string }) {
  return <img src="/sprites/loading-animation.png" alt="loading"
    style={{ imageRendering: 'pixelated' }} className={className} />
}
```

ApprovalInbox.tsx — existing inline risk badge (to be replaced):
  // Currently renders: <span className={riskBadgeClass(approval.riskLevel)}>{approval.riskLevel}</span>
  // Replace with: <RiskBadge level={approval.riskLevel as RiskLevel} />
  // riskBadgeClass function and RISK_COLORS map can be removed after replacement

SessionListPanel.tsx — existing wsStatus location:
  // import { useStore } from '../../store/index.js' — already present
  // wsStatus is accessible via: const wsStatus = useStore((s) => s.wsStatus)
  // Add: when wsStatus === 'connecting', render <LoadingSpinner /> near the top of the panel
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate all Tier 2/3 icon and badge assets via PixelLab</name>
  <files>
    assets/raw/icons/
    packages/ui/public/sprites/icon-timeline.png
    packages/ui/public/sprites/icon-memory.png
    packages/ui/public/sprites/icon-approvals.png
    packages/ui/public/sprites/icon-diff.png
    packages/ui/public/sprites/icon-history.png
    packages/ui/public/sprites/badge-low.png
    packages/ui/public/sprites/badge-medium.png
    packages/ui/public/sprites/badge-high.png
    packages/ui/public/sprites/badge-critical.png
    packages/ui/public/sprites/loading-animation.png
    scripts/generation-log.md
  </files>
  <action>
    Generate assets using create_map_object. Process sequentially (one at a time).
    Download each result to assets/raw/icons/{name}-raw.png, then copy to packages/ui/public/sprites/{name}.png.

    PANEL HEADER ICONS (5 icons, 16×16 each):

    icon-timeline:
      description: "horizontal timeline bar with tick marks, pixel art, space station UI icon, minimal, single color white"
      width: 16, height: 16, view: "top-down"

    icon-memory:
      description: "brain or memory chip outline, pixel art, space station UI icon, minimal, single color white"
      width: 16, height: 16, view: "top-down"

    icon-approvals:
      description: "checkmark in circle, approval stamp, pixel art, space station UI icon, minimal, single color white"
      width: 16, height: 16, view: "top-down"

    icon-diff:
      description: "two overlapping document pages with diff lines, pixel art, space station UI icon, minimal, single color white"
      width: 16, height: 16, view: "top-down"

    icon-history:
      description: "clock with circular arrow, history or replay icon, pixel art, space station UI icon, minimal, single color white"
      width: 16, height: 16, view: "top-down"

    RISK LEVEL BADGES (4 badges, 24×16 each):

    badge-low:
      description: "LOW risk label badge, pixel art, green color scheme, small rectangular badge, space station UI"
      width: 24, height: 16, view: "top-down"

    badge-medium:
      description: "MEDIUM risk label badge, pixel art, yellow/amber color scheme, small rectangular badge, space station UI"
      width: 24, height: 16, view: "top-down"

    badge-high:
      description: "HIGH risk label badge, pixel art, orange color scheme, small rectangular badge, space station UI"
      width: 24, height: 16, view: "top-down"

    badge-critical:
      description: "CRITICAL risk label badge, pixel art, red color scheme, flashing alert badge, space station UI"
      width: 24, height: 16, view: "top-down"

    LOADING ANIMATION:
      description: "circular spinning loading indicator, technical readout style, pixel art, space station UI, 16px"
      width: 16, height: 16, view: "top-down"
      Save to: packages/ui/public/sprites/loading-animation.png

    Append 10 rows to scripts/generation-log.md with asset name, tool, output path, file size.

    Note: create_map_object returns a single image (not animated frames). No stitching needed.
    If any individual generation fails, retry once with a simplified description (remove adjectives, keep core object name).
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const files = [
  'icon-timeline','icon-memory','icon-approvals','icon-diff','icon-history',
  'badge-low','badge-medium','badge-high','badge-critical','loading-animation'
].map(n => 'packages/ui/public/sprites/' + n + '.png');
let ok = true;
files.forEach(f => {
  try { const s = fs.statSync(f); if(s.size===0) { console.error('EMPTY:', f); ok=false; } }
  catch { console.error('MISSING:', f); ok=false; }
});
if(ok) console.log('All 10 tier2 assets present'); else process.exit(1);
"</automated>
  </verify>
  <done>
    - All 10 PNG files exist at packages/ui/public/sprites/ with non-zero byte size
    - generation-log.md has entries for all 10 assets
    - No generation failures (or failures were retried successfully)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RiskBadge.tsx and LoadingSpinner.tsx, wire into ApprovalInbox and SessionListPanel, update validate-sprites --tier2</name>
  <files>
    packages/ui/src/components/RiskBadge.tsx
    packages/ui/src/components/LoadingSpinner.tsx
    packages/ui/src/components/panels/ApprovalInbox.tsx
    packages/ui/src/components/layout/SessionListPanel.tsx
    scripts/validate-sprites.ts
    packages/ui/src/__tests__/RiskBadge.test.tsx
  </files>
  <behavior>
    - RiskBadge with level='low' renders an img with src='/sprites/badge-low.png'
    - RiskBadge with level='critical' renders an img with src='/sprites/badge-critical.png'
    - RiskBadge img has alt text containing the level name
    - RiskBadge img has style imageRendering:'pixelated'
    - LoadingSpinner renders an img with src='/sprites/loading-animation.png'
    - LoadingSpinner img has alt='loading'
    - validate-sprites.ts --tier2 exits 0 when all 10 tier2 PNG files exist
  </behavior>
  <action>
    1. Create packages/ui/src/components/RiskBadge.tsx per interface contract above.
       type RiskLevel = 'low' | 'medium' | 'high' | 'critical'

    2. Create packages/ui/src/components/LoadingSpinner.tsx per interface contract above.

    3. Update scripts/validate-sprites.ts --tier2 handler:
       Replace the "placeholder: log tier2 check pending" stub with actual validation:
       Check all 10 files: icon-timeline, icon-memory, icon-approvals, icon-diff, icon-history,
       badge-low, badge-medium, badge-high, badge-critical, loading-animation
       at packages/ui/public/sprites/{name}.png
       Exit 1 if any missing or zero bytes.

    4. Create packages/ui/src/__tests__/RiskBadge.test.tsx with RTL tests covering behavior block above.

    5. Wire RiskBadge into packages/ui/src/components/panels/ApprovalInbox.tsx:
       - Add import: import { RiskBadge } from '../RiskBadge.js'
       - In the approval item JSX, locate the inline risk badge span:
           <span className={riskBadgeClass(approval.riskLevel)}>{approval.riskLevel}</span>
         Replace it with:
           <RiskBadge level={approval.riskLevel as RiskLevel} />
       - Add type import: import type { RiskLevel } from '../RiskBadge.js' (or inline the type if simpler)
       - Remove the now-unused riskBadgeClass function and RISK_COLORS map from ApprovalInbox.tsx.
         (If RISK_COLORS or riskBadgeClass are used elsewhere in the file, keep them — only remove
         the specific span that was replaced.)

    6. Wire LoadingSpinner into packages/ui/src/components/layout/SessionListPanel.tsx:
       - Add import: import { LoadingSpinner } from '../LoadingSpinner.js'
       - Add wsStatus to the store subscription:
           const wsStatus = useStore((s) => s.wsStatus)
       - In the JSX, add a reconnecting indicator at the top of the panel content (above SessionFilters):
           {wsStatus === 'connecting' && (
             <div className="flex items-center gap-1 px-3 py-1 text-xs text-muted-foreground">
               <LoadingSpinner className="w-4 h-4" />
               <span>Connecting...</span>
             </div>
           )}

    7. Run: pnpm --filter @cockpit/ui run typecheck
       Fix any type errors.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run RiskBadge && npx tsx scripts/validate-sprites.ts --tier2 && pnpm --filter @cockpit/ui run typecheck</automated>
  </verify>
  <done>
    - RiskBadge.test.tsx passes (4+ test cases)
    - validate-sprites.ts --tier2 exits 0
    - RiskBadge.tsx and LoadingSpinner.tsx compile without type errors
    - ApprovalInbox.tsx imports and renders RiskBadge (not inline text span)
    - SessionListPanel.tsx imports and renders LoadingSpinner when wsStatus === 'connecting'
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human Review — Tier 2/3 Icon and Badge Quality</name>
  <action>Visually review all 10 Tier 2/3 assets (icons and badges) and approve or request regeneration.</action>
  <what-built>
    All Tier 2/3 assets generated and wired:
    - 5 panel header icons (timeline, memory, approvals, diff, history)
    - 4 risk level badges (low, medium, high, critical) — visible in ApprovalInbox
    - 1 loading animation — visible in SessionListPanel during WebSocket connecting state
    - RiskBadge and LoadingSpinner components wired into consuming parents
  </what-built>
  <how-to-verify>
    1. Open the assets directory: ls packages/ui/public/sprites/*.png — confirm all files are present
    2. Open each icon PNG in an image viewer to spot-check visual quality:
       - Icons should be recognizable pixel art at 16×16
       - Badges should show correct color (green=low, yellow=medium, orange=high, red=critical)
       - Loading animation should look like a spinner/loader
    3. In the running app, navigate to an approval panel — confirm the risk badge shows the pixel art badge image (not inline text)
    4. To test LoadingSpinner: temporarily set wsStatus to 'connecting' in the store devtools or source, confirm spinner appears in SessionListPanel header
    5. If any icon looks completely wrong (solid color, blank, unrecognizable): note which ones need retries
    6. Minor quality variation between icons is acceptable at this tier (these are small UI icons)
    7. Type "approved" to complete Phase 13, or describe which assets need regeneration
  </how-to-verify>
  <resume-signal>Type "approved" or list assets that need regeneration with description of issue</resume-signal>
</task>

</tasks>

<verification>
pnpm --filter @cockpit/ui test --run
npx tsx scripts/validate-sprites.ts --all
npx tsx scripts/validate-sprites.ts --tier2
pnpm --filter @cockpit/ui run typecheck
</verification>

<success_criteria>
- All 10 Tier 2/3 PNG assets exist at public/sprites/ with non-zero bytes
- RiskBadge.test.tsx passes
- validate-sprites.ts --tier2 exits 0
- validate-sprites.ts --all exits 0 (10 character sheets + tier2 assets)
- RiskBadge imported and rendered in ApprovalInbox.tsx
- LoadingSpinner imported and rendered in SessionListPanel.tsx when wsStatus === 'connecting'
- TypeScript typecheck passes
- User approves visual quality at checkpoint
- Phase 13 complete: all 6 success criteria from ROADMAP achieved
</success_criteria>

<output>
After completion, create `.planning/phases/13-pixel-art-generation-and-integration/13-04-SUMMARY.md`
</output>
