---
phase: 30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and
plan: 01
subsystem: testing
tags: [vitest, daemon, ui, pty, claude, replay]
requires:
  - phase: 30
    provides: PTY Claude runtime, popup terminal surface
provides:
  - RED daemon parity coverage for PTY Claude launch, hook, and catch-up flows
  - RED UI parity coverage for approvals, timeline, diff, memory, and artifacts hydration
affects: [30.1-02, 30.1-03, 30.1-04]
tech-stack:
  added: []
  patterns:
    - PTY parity asserted through structured events and replay, not terminal text scraping
key-files:
  created:
    - packages/ui/src/__tests__/ArtifactsPanel.test.tsx
  modified:
    - packages/daemon/src/__tests__/launch-session.test.ts
    - packages/daemon/src/__tests__/hook-server.test.ts
    - packages/daemon/src/__tests__/ws-catchup.test.ts
    - packages/ui/src/__tests__/ApprovalInbox.test.tsx
    - packages/ui/src/__tests__/TimelinePanel.test.tsx
    - packages/ui/src/__tests__/DiffPanel.test.tsx
    - packages/ui/src/__tests__/MemoryPanel.test.tsx
key-decisions:
  - "Use PTY Claude fixtures with empty-store and replay paths so parity failures are observable without browser-only manual setup"
patterns-established:
  - "PTY session contract must include structured session_start metadata, replay-safe events, and empty-state hydration"
requirements-completed: [PTY-TABS-01, PTY-TABS-02]
duration: 1h
completed: 2026-05-04
---

# Phase 30.1 Plan 01 Summary

**Failing PTY parity tests now define the structured-event, replay, and hydration contract for Claude PTY sessions across daemon and UI layers.**

## Accomplishments

- Extended daemon tests to assert `mode: 'pty'`, deterministic `session_start`, and replay parity for daemon-launched Claude sessions.
- Extended UI tests to assert PTY approvals, timeline, diff, memory, and artifacts still hydrate correctly when live store state is empty.
- Added `ArtifactsPanel` regression coverage so PTY Claude activity is no longer an untested blind spot.

## Task Commits

Executed inline in the shared working tree during phase execution; no per-task commits were recorded.

## Decisions Made

- The parity contract is defined against normalized events and session metadata, not PTY byte output.
- Replay and reopen flows are first-class acceptance criteria, not follow-up coverage.

## Deviations from Plan

None in scope. The plan intent was preserved, but execution happened inline instead of as a separate RED-only commit sequence.

## Issues Encountered

- Initial subagent attempts did not complete reliably, so execution was moved inline to keep the phase moving.

## Next Phase Readiness

- Plan 02 can implement against explicit daemon failures instead of inferred PTY symptoms.
- Plan 03 can target only the remaining UI-layer assumptions after daemon parity is restored.
