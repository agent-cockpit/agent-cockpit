---
phase: 30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - packages/daemon/src/__tests__/launch-session.test.ts
  - packages/daemon/src/__tests__/hook-server.test.ts
  - packages/daemon/src/__tests__/ws-catchup.test.ts
  - packages/ui/src/__tests__/ApprovalInbox.test.tsx
  - packages/ui/src/__tests__/TimelinePanel.test.tsx
  - packages/ui/src/__tests__/DiffPanel.test.tsx
  - packages/ui/src/__tests__/MemoryPanel.test.tsx
  - packages/ui/src/__tests__/ArtifactsPanel.test.tsx
autonomous: true
requirements:
  - PTY-TABS-01
  - PTY-TABS-02

must_haves:
  truths:
    - "Failing daemon test exists proving PTY Claude launch/session metadata preserves workspace and PTY mode for downstream tabs"
    - "Failing daemon test exists proving PTY Claude structured events survive replay/catch-up after reconnect"
    - "Failing UI regression tests exist for ApprovalInbox, TimelinePanel, DiffPanel, MemoryPanel, and ArtifactsPanel in PTY Claude session context"
    - "At least one new PTY Claude parity test covers empty-state hydration from daemon endpoints rather than pre-seeded store state"
    - "All new tests are RED by design before implementation work begins"
  artifacts:
    - path: "packages/daemon/src/__tests__/hook-server.test.ts"
      provides: "RED coverage for PTY Claude synthetic session_start / structured event parity"
    - path: "packages/daemon/src/__tests__/ws-catchup.test.ts"
      provides: "RED coverage for PTY Claude replay/catch-up parity"
    - path: "packages/ui/src/__tests__/ArtifactsPanel.test.tsx"
      provides: "New RED PTY Claude artifacts regression coverage"
  key_links:
    - from: "packages/daemon/src/__tests__/ws-catchup.test.ts"
      to: "packages/ui/src/hooks/useSessionEvents.ts"
      via: "catchup_complete and replay batch assumptions"
      pattern: "PTY Claude replay parity"
    - from: "packages/ui/src/__tests__/MemoryPanel.test.tsx"
      to: "packages/daemon/src/ws/server.ts"
      via: "memory endpoint hydration for PTY-launched Claude sessions"
      pattern: "workspace-backed PTY memory"
---

<objective>
Define the PTY Claude parity contract in failing tests before implementation.

Purpose: This plan turns the vague report of “tabs are wrong under PTY Claude” into executable assertions across daemon and UI layers. The next plans should implement against these tests rather than guessing at the root cause.
Output: RED parity tests covering PTY Claude live/replay behavior and tab hydration.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and/30.1-CONTEXT.md
@.planning/phases/30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and/30.1-RESEARCH.md
</execution_context>

<context>
<interfaces>
PTY side-channel today:
```typescript
// useSessionEvents.ts
if (payload.type === 'pty_output') {
  ptyBus.emit(payload.sessionId, payload.data)
  return
}
```

Structured tabs today:
```typescript
// ApprovalInbox / TimelinePanel / DiffPanel / MemoryPanel / ArtifactsPanel
// consume normalized events and/or daemon REST hydration, not PTY bytes
```

Required contract for this phase:
```typescript
type ClaudePtyParity = {
  sessionStartHasWorkspace: true
  modeIsPty: true
  structuredEventsAvailable: true
  replayConvergesWithLive: true
  emptyStateHydrationWorks: true
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED daemon parity tests for PTY Claude launch, hook, and catch-up behavior</name>
  <files>packages/daemon/src/__tests__/launch-session.test.ts, packages/daemon/src/__tests__/hook-server.test.ts, packages/daemon/src/__tests__/ws-catchup.test.ts</files>
  <behavior>
    - PTY Claude launch test asserts downstream-visible session metadata remains usable for tabs: `mode: 'pty'`, workspace present, managed session semantics preserved
    - Hook server test asserts PTY-launched Claude still emits synthetic or direct `session_start` before first approval/file/memory event when needed
    - Catch-up test asserts a reconnecting client receives the same PTY Claude structured event sequence needed for approvals/timeline/diff/memory/artifacts as a live client
  </behavior>
  <action>
    Add failing tests only. Do not repair production code in this plan.

    Minimum RED cases:
    1. `launch-session.test.ts`: PTY Claude launch path produces session summary / start metadata consistent with downstream tab expectations.
    2. `hook-server.test.ts`: first PTY Claude structured event after launch causes a usable `session_start` with workspace, then forwards approval/file/memory events in order.
    3. `ws-catchup.test.ts`: replay after reconnect converges for PTY Claude approvals + file_change + memory_write sequence.

    Tests should fail because the current PTY parity contract is not yet codified end-to-end.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/daemon test --run launch-session hook-server ws-catchup</automated>
  </verify>
  <done>Daemon parity tests exist and fail for the intended PTY Claude contract gaps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED UI regression tests for PTY Claude tab hydration and replay assumptions</name>
  <files>packages/ui/src/__tests__/ApprovalInbox.test.tsx, packages/ui/src/__tests__/TimelinePanel.test.tsx, packages/ui/src/__tests__/DiffPanel.test.tsx, packages/ui/src/__tests__/MemoryPanel.test.tsx, packages/ui/src/__tests__/ArtifactsPanel.test.tsx</files>
  <behavior>
    - ApprovalInbox can hydrate pending PTY Claude approvals when local queue is empty
    - TimelinePanel renders PTY Claude structured events after replay/hydration, not just for non-PTY sessions
    - DiffPanel remains useful for PTY Claude file activity, including tool-call fallback or `file_change` parity as designed
    - MemoryPanel resolves PTY Claude workspace-backed content and suggested writes
    - ArtifactsPanel shows PTY Claude activity entries rather than staying empty
  </behavior>
  <action>
    Extend the existing test files and create `ArtifactsPanel.test.tsx` if it does not exist.

    Use PTY Claude session fixtures (`provider: 'claude'`, `mode: 'pty'`) and prefer empty-store hydration paths in at least one test per panel where realistic. The assertions should encode desired parity, not current behavior.

    Keep the tests RED. The point of this plan is to define the contract for Plans 02 and 03.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run ApprovalInbox TimelinePanel DiffPanel MemoryPanel ArtifactsPanel</automated>
  </verify>
  <done>UI parity tests exist and fail specifically on PTY Claude tab correctness gaps.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @cockpit/daemon test --run launch-session hook-server ws-catchup` shows the new RED daemon cases
2. `pnpm --filter @cockpit/ui test --run ApprovalInbox TimelinePanel DiffPanel MemoryPanel ArtifactsPanel` shows the new RED UI parity cases
3. No production implementation is changed in this plan
</verification>

<success_criteria>
- PTY Claude parity expectations are explicit in tests across daemon and UI layers
- Replay/catch-up is part of the contract, not an afterthought
- Execution plans can implement against concrete failing tests instead of broad symptoms
</success_criteria>

<output>
After completion, create `.planning/phases/30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and/30.1-01-SUMMARY.md`
</output>
