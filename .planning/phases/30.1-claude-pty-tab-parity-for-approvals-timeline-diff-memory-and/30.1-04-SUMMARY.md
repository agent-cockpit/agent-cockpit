---
phase: 30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and
plan: 04
subsystem: ui
tags: [react, xterm, pty, popup, regression]
requires:
  - phase: 30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and
    provides: restored PTY non-terminal tab parity
provides:
  - TerminalPanel regression coverage for output, input, and resize
  - Popup coexistence coverage for PTY chat mode and capability-state messaging
affects: [phase-30-regression-guard]
tech-stack:
  added: []
  patterns:
    - Terminal resize behavior is validated with explicit fake-timer and non-zero layout test harnessing
key-files:
  created:
    - packages/ui/src/__tests__/TerminalPanel.test.tsx
  modified:
    - packages/ui/src/components/office/__tests__/InstancePopupHub.test.tsx
key-decisions:
  - "Protect PTY terminal behavior at the test harness boundary instead of changing working production terminal logic for jsdom limitations"
patterns-established:
  - "Popup coexistence coverage should assert both TerminalPanel routing and PTY capability messaging"
requirements-completed: []
duration: 45min
completed: 2026-05-04
---

# Phase 30.1 Plan 04 Summary

**The PTY terminal surface is now regression-protected for output, input, resize, and popup coexistence while the restored non-terminal tabs remain reachable.**

## Accomplishments

- Added `TerminalPanel` coverage for PTY output subscription, `pty_input`, and `pty_resize`.
- Added `InstancePopupHub` coverage proving PTY sessions still route chat to `TerminalPanel` and preserve terminate capability messaging.
- Resolved the terminal resize test against the component’s actual debounce and layout guards rather than loosening assertions.
- Verified the focused PTY UI suite, including `TerminalPanel` and `InstancePopupHub`, is green.

## Task Commits

Executed inline in the shared working tree during phase execution; no per-task commits were recorded.

## Decisions Made

- jsdom layout and timer behavior are modeled in the test harness so the production resize path remains untouched.
- Popup coexistence regression protection lives in tests because the production `InstancePopupHub.tsx` already carried user-owned in-progress edits and did not need source changes for this phase.

## Deviations from Plan

The plan’s required coverage was delivered without modifying `InstancePopupHub.tsx` itself; only the regression tests changed.

## Issues Encountered

- The first terminal resize assertion failed because the component intentionally waits for a timer and non-zero container measurements; the harness was updated to reflect that behavior precisely.

## Next Phase Readiness

- Phase 30.1 is ready for manual verification and formal `/gsd-verify-work 30.1` without losing PTY terminal safety coverage.
