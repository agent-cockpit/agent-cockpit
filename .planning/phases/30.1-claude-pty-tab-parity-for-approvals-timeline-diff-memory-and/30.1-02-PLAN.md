---
phase: 30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and
plan: 02
type: auto
wave: 1
depends_on:
  - 30.1-01-PLAN.md
files_modified:
  - packages/daemon/src/adapters/claude/ptyLauncher.ts
  - packages/daemon/src/adapters/claude/hookServer.ts
  - packages/daemon/src/adapters/claude/hookParser.ts
  - packages/daemon/src/ws/server.ts
  - packages/daemon/src/ws/handlers.ts
  - packages/daemon/src/db/queries.ts
autonomous: true
requirements:
  - PTY-TABS-01
  - PTY-TABS-02
  - PTY-TABS-03

must_haves:
  truths:
    - "PTY Claude sessions preserve workspace/session metadata early enough for all downstream tabs and memory endpoints"
    - "Structured Claude events still flow under PTY launch without requiring terminal text parsing"
    - "Replay/catch-up for PTY Claude converges with live observation for approvals, file activity, and memory events"
    - "Pending approvals remain actionable and correctly hydrated for PTY Claude sessions"
  artifacts:
    - path: "packages/daemon/src/adapters/claude/ptyLauncher.ts"
      provides: "PTY Claude launch/runtime state needed for structured parity"
    - path: "packages/daemon/src/adapters/claude/hookServer.ts"
      provides: "Reliable lifecycle + event forwarding for PTY Claude sessions"
    - path: "packages/daemon/src/ws/server.ts"
      provides: "Replay-safe PTY session metadata and HTTP endpoint correctness"
  key_links:
    - from: "packages/daemon/src/adapters/claude/ptyLauncher.ts"
      to: "packages/daemon/src/adapters/claude/hookServer.ts"
      via: "hook relay + session identity continuity"
      pattern: "PTY Claude structured parity"
    - from: "packages/daemon/src/ws/server.ts"
      to: "packages/daemon/src/db/queries.ts"
      via: "workspace-backed memory/session hydration"
      pattern: "session workspace lookup"
---

<objective>
Repair the daemon/runtime contract so PTY Claude sessions once again feed the non-terminal tabs correctly.

Purpose: The PTY runtime should add terminal capability, not replace the structured event path. This plan fixes the source-of-truth layer: lifecycle metadata, structured event emission, replay/catch-up integrity, and approval state for PTY Claude sessions.
Output: daemon/runtime parity fixes that satisfy the RED contract tests from Plan 01.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and/30.1-CONTEXT.md
@.planning/phases/30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and/30.1-RESEARCH.md
@.planning/phases/30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and/30.1-01-PLAN.md
</execution_context>

<context>
<interfaces>
Invariant to preserve:
```typescript
// PTY is terminal transport only.
// Structured parity must still come from normalized events + session metadata.
```

Likely affected contracts:
```typescript
type SessionStartEvent = {
  type: 'session_start'
  provider: 'claude'
  workspacePath: string
  mode?: 'pty'
}

type ReplayExpectation = {
  approvals: 'same live vs replay'
  fileActivity: 'same live vs replay'
  memoryActivity: 'same live vs replay'
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden PTY Claude launch and lifecycle metadata for downstream tab consumers</name>
  <files>packages/daemon/src/adapters/claude/ptyLauncher.ts, packages/daemon/src/adapters/claude/hookServer.ts, packages/daemon/src/ws/server.ts, packages/daemon/src/db/queries.ts</files>
  <action>
    Implement the minimum daemon-side changes required to make PTY Claude sessions replay-safe and workspace-safe for the tabs.

    Target outcomes:
    1. PTY Claude launch registers enough session/workspace identity before downstream endpoints need it
    2. `session_start` parity is deterministic under PTY launch, including `mode: 'pty'`
    3. Approval queue hydration and memory endpoint lookups can resolve the correct PTY session/workspace state after reconnect or popup reopen

    Prefer repairing source metadata rather than adding fallback heuristics in panel code.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/daemon test --run launch-session hook-server memory-endpoints ws-catchup approval-queue approval-timeout</automated>
  </verify>
  <done>PTY Claude session metadata, approval state, and workspace lookup are reliable enough for the tabs.</done>
</task>

<task type="auto">
  <name>Task 2: Restore structured PTY Claude event parity for approvals, file activity, and memory activity</name>
  <files>packages/daemon/src/adapters/claude/hookServer.ts, packages/daemon/src/adapters/claude/hookParser.ts, packages/daemon/src/ws/server.ts, packages/daemon/src/ws/handlers.ts</files>
  <action>
    Ensure PTY Claude still produces the normalized events required by the five non-terminal tabs.

    This includes:
    - approval request / resolution continuity
    - file activity continuity (`file_change` and/or supported structured fallback inputs)
    - memory read / write / suggestion continuity
    - replay/catch-up parity for those events

    Do not derive these states from terminal output text. Keep the event pipeline explicit and testable.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/daemon test --run hook-server hookParser ws-catchup</automated>
  </verify>
  <done>RED daemon parity tests from Plan 01 pass with PTY Claude structured events behaving consistently live and on replay.</done>
</task>

</tasks>

<verification>
1. All new daemon parity tests from Plan 01 pass
2. `pnpm --filter @cockpit/daemon test --run launch-session hook-server hookParser ws-catchup memory-endpoints approval-queue approval-timeout` passes
3. PTY terminal behavior is not regressed while restoring structured parity
</verification>

<success_criteria>
- PTY Claude sessions once again behave like first-class structured sessions for non-terminal consumers
- Replay/catch-up and workspace-backed endpoints are reliable after reconnect
- Approval state remains actionable and not live-only
</success_criteria>

<output>
After completion, create `.planning/phases/30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and/30.1-02-SUMMARY.md`
</output>
