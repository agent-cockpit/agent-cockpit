---
phase: 30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and
plan: 04
type: tdd
wave: 3
depends_on:
  - 30.1-03-PLAN.md
files_modified:
  - packages/ui/src/components/panels/TerminalPanel.tsx
  - packages/ui/src/__tests__/TerminalPanel.test.tsx
  - packages/ui/src/components/office/InstancePopupHub.tsx
  - packages/ui/src/components/office/__tests__/InstancePopupHub.test.tsx
autonomous: true
requirements:
  - Phase 30 regression guard

must_haves:
  truths:
    - "Incoming PTY output still renders in the terminal surface after tab-parity fixes"
    - "User terminal input still emits `pty_input` with the correct session payload"
    - "Terminal resize behavior still emits `pty_resize` or otherwise preserves correct sizing behavior"
    - "PTY popup mode still renders `TerminalPanel` for chat while approvals, timeline, diff, memory, and artifacts remain reachable"
    - "PTY session capability state remains correct in the popup after parity fixes, including terminate availability and any user-facing disabled reason"
  artifacts:
    - path: "packages/ui/src/__tests__/TerminalPanel.test.tsx"
      provides: "Terminal stream/input/resize regression coverage"
    - path: "packages/ui/src/components/office/__tests__/InstancePopupHub.test.tsx"
      provides: "Regression coverage for PTY popup coexistence"
  key_links:
    - from: "packages/ui/src/components/panels/TerminalPanel.tsx"
      to: "packages/ui/src/hooks/useSessionEvents.ts"
      via: "PTY bus, pty_input, and pty_resize behavior"
      pattern: "terminal regression guard"
    - from: "packages/ui/src/components/office/InstancePopupHub.tsx"
      to: "packages/ui/src/components/panels/TerminalPanel.tsx"
      via: "PTY chat surface without breaking other tabs"
      pattern: "popup coexistence"
---

<objective>
Protect the original PTY terminal behavior while the tab-parity work lands.

Purpose: Phase 30 introduced the terminal surface; this follow-up plan makes sure the Phase 30.1 parity fixes do not quietly break streaming, input, resize, or popup coexistence.
Output: passing terminal-focused regressions for PTY popup mode.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and/30.1-CONTEXT.md
@.planning/phases/30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and/30.1-RESEARCH.md
@.planning/phases/30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and/30.1-03-PLAN.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add or update terminal regression tests for PTY stream/input/resize behavior</name>
  <files>packages/ui/src/components/panels/TerminalPanel.tsx, packages/ui/src/__tests__/TerminalPanel.test.tsx</files>
  <action>
    Add explicit automated coverage for the Phase 30 success criterion that the PTY terminal path remains intact.

    Cover at minimum:
    1. incoming PTY output still renders in the terminal surface
    2. user input still emits `pty_input` with the correct session payload
    3. resize handling still emits `pty_resize` or otherwise preserves terminal sizing behavior

    Make only the smallest production changes necessary to satisfy the tests.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run TerminalPanel</automated>
  </verify>
  <done>TerminalPanel has explicit regression protection for stream/input/resize behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Verify PTY popup coexistence after tab parity fixes</name>
  <files>packages/ui/src/components/office/InstancePopupHub.tsx, packages/ui/src/components/office/__tests__/InstancePopupHub.test.tsx</files>
  <action>
    Add focused regression coverage proving PTY popup mode still renders `TerminalPanel` for chat while the other tabs remain reachable and functional after the Phase 30.1 changes.

    Explicitly cover session capability state:
    - terminate control remains enabled/disabled according to PTY live session capability flags
    - any disabled reason text still renders correctly when capability is unavailable
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run InstancePopupHub</automated>
  </verify>
  <done>PTY popup coexistence and PTY capability-state rendering are regression-covered and stable.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @cockpit/ui test --run TerminalPanel` passes
2. `pnpm --filter @cockpit/ui test --run InstancePopupHub` passes
3. Terminal functionality remains intact while non-terminal tabs stay reachable
4. PTY capability state remains correct in popup controls and messaging
</verification>

<success_criteria>
- PTY terminal behavior remains regression-protected after Phase 30.1 parity work
- Popup PTY mode continues to expose both the terminal and the restored non-terminal tabs
- PTY capability-state behavior remains correct after the parity changes
</success_criteria>

<output>
After completion, create `.planning/phases/30.1-claude-pty-tab-parity-for-approvals-timeline-diff-memory-and/30.1-04-SUMMARY.md`
</output>
