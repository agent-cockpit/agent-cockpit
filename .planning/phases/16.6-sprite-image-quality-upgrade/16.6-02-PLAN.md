---
phase: 16.6-sprite-image-quality-upgrade
plan: 02
type: execute
wave: 2
depends_on:
  - 16.6-01
files_modified: []
autonomous: false
requirements:
  - sprite-quality

must_haves:
  truths:
    - "Astronaut and NPC sprites appear crisp with hard pixel edges at 2× zoom (no blur)"
    - "Tilemap terrain tiles remain smooth (bilinear), confirming the per-layer toggling works"
  artifacts: []
  key_links: []
---

<objective>
Visual QA checkpoint: confirm that the rendering fix from Plan 01 produces visually crisp sprites on the game canvas at 2× zoom. Human verification is the authoritative gate — automated tests confirmed code correctness but cannot verify pixel art aesthetics.

Purpose: jsdom canvas stubs cannot validate visual rendering. A human must look at the game and confirm sprites have crisp pixel edges with no bilinear blur.
Output: Human approval that sprites look sharp. Phase 16.6 complete.
</objective>

<execution_context>
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabiomissiaggiabrugnara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.6-sprite-image-quality-upgrade/16.6-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and confirm tests are green</name>
  <files></files>
  <action>
    Run the full test suite one final time to confirm all tests are green, then start the dev server so the human can inspect the canvas at 2× zoom.

    ```bash
    cd packages/ui && npx vitest run
    # Must exit 0
    ```

    Then start the dev server:
    ```bash
    pnpm dev
    # or: npm run dev from the repo root
    ```

    Navigate to the Office page in the browser (typically http://localhost:5173 or the configured dev port).
  </action>
  <verify>
    <automated>cd /Users/fabiomissiaggiabrugnara/Projects/Cockpit/agent-cockpit/packages/ui && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass. Dev server is running and Office page is accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual QA — confirm sprites are crisp at 2× zoom</name>
  <action>Human inspects the Office page canvas at 2× zoom to confirm sprite rendering quality.</action>
  <verify>
    <automated>MISSING — visual rendering cannot be tested automatically; human inspection required</automated>
  </verify>
  <done>Human confirms sprites have crisp pixel edges with no blur at 2× zoom.</done>
  <what-built>
    Axis A rendering fix: ctx.imageSmoothingEnabled = false set before all sprite drawImage calls; imageRendering: 'pixelated' added to the canvas element. Tilemap blit keeps imageSmoothingEnabled = true (bilinear). Implemented in OfficePage.tsx and AgentSprite.tsx.
  </what-built>
  <how-to-verify>
    1. Open the Office page in the browser at the dev server URL (typically http://localhost:5173).
    2. Confirm the game canvas renders. You should see the map and at least one sprite character (astronaut player character).
    3. Move the player character using WASD — confirm walking animation plays.
    4. Look at the sprites carefully: they should have CRISP, hard pixel edges. Each pixel in the sprite should be a clear solid-color square with no blurring or anti-aliasing between pixels.
    5. Compare the sprite rendering to the map tiles behind it — the map tiles should look SMOOTH (bilinear is correct for the pre-rendered composite map). Only the sprites should be pixelated/crisp.
    6. Check at the current zoom level (2×): characters should appear as sharp pixel art, not as blurry upscaled images.

    Expected: sprites look like classic pixel art characters with visible pixel grid.
    Not expected: soft/blurry edges that "bleed" between adjacent colors in the sprite.
  </how-to-verify>
  <resume-signal>Type "approved" if sprites look crisp and sharp. Describe any remaining blur or visual issues if not.</resume-signal>
</task>

</tasks>

<verification>
Human confirms sprites are visually crisp at 2× zoom with no bilinear blur.
Tilemap terrain confirmed smooth (bilinear preserved).
Full test suite green.
</verification>

<success_criteria>
1. Human confirms sprites appear crisp with hard pixel edges at 2× zoom
2. Human confirms map tiles remain smooth (bilinear not broken)
3. All automated tests pass
4. Phase 16.6 complete — sprites no longer blurry
</success_criteria>

<output>
After completion, create `.planning/phases/16.6-sprite-image-quality-upgrade/16.6-02-SUMMARY.md`
</output>
