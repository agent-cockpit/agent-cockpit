---
phase: 16.6-sprite-image-quality-upgrade
plan: 01
subsystem: ui
tags: [canvas, pixel-art, rendering, image-quality, nearest-neighbor, sprites]

# Dependency graph
requires:
  - phase: 16.5-walking-animation-rework
    provides: drawAgentSprite canvas function, OfficePage render loop with tilemap + sprite layers

provides:
  - ctx.imageSmoothingEnabled=false before all sprite drawImage calls (nearest-neighbor interpolation)
  - ctx.imageSmoothingEnabled=true before tilemap blit (bilinear preserved)
  - imageRendering: pixelated on canvas element (DPR scaling also nearest-neighbor)
  - Spy-based tests verifying imageSmoothingEnabled toggling and imageRendering CSS

affects: [16.7-wall-and-object-collision-physics, 16.8-sidebar-design-overhaul]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - imageSmoothingEnabled toggled per-layer: true for tilemap (bilinear), false for sprites (nearest-neighbor)
    - imageRendering:pixelated on canvas element ensures DPR scaling also uses nearest-neighbor
    - Defensive imageSmoothingEnabled=false in AgentSprite.tsx as belt-and-suspenders guard

key-files:
  created:
    - packages/ui/src/pages/__tests__/OfficePage.render.test.tsx
  modified:
    - packages/ui/src/pages/OfficePage.tsx
    - packages/ui/src/components/office/AgentSprite.tsx
    - packages/ui/src/components/office/__tests__/AgentSprite.test.ts

key-decisions:
  - "imageSmoothingEnabled=true before tilemap blit (bilinear preserved for pre-rendered composite map), false before sprites"
  - "imageRendering: pixelated on canvas element covers DPR scaling via CSS, not just Canvas 2D API"
  - "OfficePage.render.test.tsx uses .tsx extension (JSX required) despite plan specifying .ts"
  - "Defensive imageSmoothingEnabled=false in drawAgentSprite() as belt-and-suspenders in case callers forget to set it"

patterns-established:
  - "Per-layer smoothing toggle: imageSmoothingEnabled=true for vector/composite content, false for pixel art sprites"
  - "imageRendering: pixelated on canvas JSX element for CSS-level DPR coverage"

requirements-completed: [sprite-quality]

# Metrics
duration: 2min
completed: 2026-04-13
---

# Phase 16.6 Plan 01: Sprite Image Quality Upgrade Summary

**Two-line fix eliminates sprite blur at 2x zoom: nearest-neighbor interpolation via imageSmoothingEnabled=false and imageRendering:pixelated, with bilinear preserved for the tilemap layer**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-13T02:42:30Z
- **Completed:** 2026-04-13T02:45:12Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Canvas element now declares `imageRendering: 'pixelated'` so CSS/DPR scaling uses nearest-neighbor
- Render loop sets `imageSmoothingEnabled=true` before tilemap blit and `=false` before sprite draws
- `drawAgentSprite()` defensively sets `imageSmoothingEnabled=false` before its `ctx.drawImage` call
- Automated tests verify the DOM style assertion and mock-ctx imageSmoothingEnabled order

## Task Commits

Each task was committed atomically:

1. **Task 1: TDD RED — Create failing tests** - `6470d27` (test)
2. **Task 2: TDD GREEN — Implement imageSmoothingEnabled + imageRendering** - `081deed` (feat)

## Files Created/Modified
- `packages/ui/src/pages/__tests__/OfficePage.render.test.tsx` - Test A: canvas imageRendering:pixelated assertion
- `packages/ui/src/components/office/__tests__/AgentSprite.test.ts` - Extended with imageSmoothingEnabled=false before drawImage test
- `packages/ui/src/pages/OfficePage.tsx` - imageSmoothingEnabled toggles in render loop, imageRendering:pixelated on canvas JSX
- `packages/ui/src/components/office/AgentSprite.tsx` - Defensive imageSmoothingEnabled=false before ctx.drawImage

## Decisions Made
- imageRendering:pixelated on canvas JSX covers DPR scaling at the CSS level (Canvas 2D imageSmoothingEnabled alone is insufficient for HiDPI displays)
- Bilinear smoothing (imageSmoothingEnabled=true) preserved for tilemap layer — pre-rendered composite map benefits from smooth scaling
- Defensive imageSmoothingEnabled=false added to drawAgentSprite() as belt-and-suspenders guard in case future callers forget to toggle
- Test file renamed to .tsx (plan specified .ts but JSX render requires .tsx extension) — minor deviation, same test content

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Test file extension changed from .ts to .tsx**
- **Found during:** Task 1 (RED phase)
- **Issue:** Plan specified `OfficePage.render.test.ts` but the file uses JSX (`<OfficePage />`), which requires `.tsx` extension for esbuild to parse it
- **Fix:** Created file as `.tsx` (and corrected after initial `.ts` attempt caused transform error)
- **Files modified:** `packages/ui/src/pages/__tests__/OfficePage.render.test.tsx`
- **Verification:** Test file compiles and runs successfully
- **Committed in:** 6470d27 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Purely cosmetic filename change. No behavioral difference. All specified test content delivered.

## Issues Encountered
- 3 pre-existing test failures in the full suite (approval card riskLevel badge, applyEventToApprovals, applyEventToEvents dedup guard) — confirmed pre-existing via `git stash` test run before my changes. Out of scope.
- 3 pre-existing TypeScript errors (AgentSprite.tsx missing 'walk' in NPC_FRAME_COUNTS, ApprovalInboxPopup missing beforeEach, setupTests.ts Canvas type mismatch) — all confirmed pre-existing.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Sprite rendering is now crisp at 2x zoom — pixel art sharpness restored
- Canvas architecture unchanged — no impact on Phase 16.7 collision physics work
- All 8 target tests green; full suite at 335/338 (3 pre-existing failures unrelated to this phase)

---
*Phase: 16.6-sprite-image-quality-upgrade*
*Completed: 2026-04-13*
