---
phase: 22
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/events.ts
  - packages/daemon/src/ws/handlers.ts
  - packages/daemon/src/ws/server.ts
  - packages/daemon/src/adapters/codex/codexAdapter.ts
  - packages/daemon/src/adapters/claude/claudeLauncher.ts
  - packages/daemon/src/db/queries.ts
  - packages/ui/src/store/index.ts
  - packages/ui/src/hooks/useSessionEvents.ts
  - packages/ui/src/components/office/InstancePopupHub.tsx
  - packages/ui/src/components/panels/ChatPanel.tsx
  - packages/daemon/src/__tests__/ws-chat.test.ts
  - packages/ui/src/__tests__/ChatPanel.test.tsx
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
autonomous: true
must_haves:
  truths:
    - "Popup includes a Chat tab scoped to selected session"
    - "Daemon-managed sessions expose canSendMessage=true; external sessions expose canSendMessage=false"
    - "Blocked sends return explicit error and UI surfaces reason"
  artifacts:
    - path: "packages/ui/src/components/panels/ChatPanel.tsx"
      provides: "Session chat composer and history with capability-gated UI"
      contains: "canSendMessage"
    - path: "packages/daemon/src/ws/handlers.ts"
      provides: "session_chat message handling and validation"
      contains: "session_chat"
  key_links:
    - from: "packages/ui/src/components/panels/ChatPanel.tsx"
      to: "packages/ui/src/hooks/useSessionEvents.ts"
      via: "sendWsMessage"
      pattern: "sendWsMessage"
    - from: "packages/daemon/src/ws/handlers.ts"
      to: "packages/daemon/src/adapters/codex/codexAdapter.ts"
      via: "managed session runtime dispatch"
      pattern: "send"
---

<objective>
Deliver a unified session Chat flow in the popup with explicit capability split: daemon-managed sessions support message send, externally attached sessions are approval-only and cannot receive chat sends.
</objective>

<execution_context>
@/Users/leostuart/.codex/get-shit-done/workflows/execute-plan.md
@/Users/leostuart/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-unified-session-chat-with-daemon-vs-external-capability-split/22-RESEARCH.md
@.planning/phases/22-unified-session-chat-with-daemon-vs-external-capability-split/22-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add session capability contract and managed runtime registry in daemon</name>
  <files>packages/daemon/src/ws/server.ts, packages/daemon/src/db/queries.ts, packages/daemon/src/adapters/codex/codexAdapter.ts, packages/daemon/src/adapters/claude/claudeLauncher.ts</files>
  <action>
    Introduce a capability model per session (`managedByDaemon`, `canSendMessage`, `canTerminateSession`, optional `reason`) and expose it in session list payloads used by UI bootstrap.
    Build a runtime registry for managed sessions so WS handlers can route chat sends only when capability allows it.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/daemon test --run ws</automated>
  </verify>
  <done>Capability metadata is available for each session and managed runtime handles are discoverable by sessionId.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `session_chat` dispatch with capability guard</name>
  <files>packages/daemon/src/ws/handlers.ts, packages/shared/src/events.ts, packages/daemon/src/__tests__/ws-chat.test.ts</files>
  <action>
    Add `session_chat` message handling in WebSocket layer.
    If `canSendMessage=false`, reject with structured error event/response and do not dispatch to provider runtime.
    If `canSendMessage=true`, dispatch through provider-specific send path and emit normalized chat events for replay/UI.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/daemon test --run ws-chat</automated>
  </verify>
  <done>Blocked and allowed chat-send paths are deterministic, tested, and replay-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Chat tab and capability-gated composer in popup</name>
  <files>packages/ui/src/store/index.ts, packages/ui/src/components/office/InstancePopupHub.tsx, packages/ui/src/components/panels/ChatPanel.tsx, packages/ui/src/hooks/useSessionEvents.ts, packages/ui/src/__tests__/ChatPanel.test.tsx</files>
  <action>
    Extend popup tabs with `chat` and render `ChatPanel` bound to selected session.
    `ChatPanel` must show message history, send box for managed sessions, and explicit approval-only disabled state for external sessions.
    Surface daemon rejection reason in UI when send is denied.
  </action>
  <verify>
    <automated>pnpm --filter @cockpit/ui test --run ChatPanel && pnpm --filter @cockpit/ui test --run InstancePopupHub</automated>
  </verify>
  <done>Popup chat UX works for managed sessions and clearly blocks unsupported external sessions.</done>
</task>

</tasks>

<verification>
pnpm --filter @cockpit/daemon test --run ws-chat && pnpm --filter @cockpit/daemon test --run codexAdapter && pnpm --filter @cockpit/ui test --run ChatPanel && pnpm --filter @cockpit/ui test --run InstancePopupHub
</verification>

<success_criteria>
Chat send support exists only where runtime supports it. Capability truth is consistent between daemon and UI. Unsupported sessions remain approval-only with explicit messaging.
</success_criteria>

<output>
After completion, create `.planning/phases/22-unified-session-chat-with-daemon-vs-external-capability-split/22-01-SUMMARY.md`.
</output>
